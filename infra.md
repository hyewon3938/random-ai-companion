# 운영 구성 — 실행 환경과 외부 서비스

[README의 기술 선택](README.md#기술-선택)이 코드 안에서 무엇을 썼는지 다룬다면, 여기서는 코드 밖에서 정한 것을 모았다. 무엇이 어디서 돌아가고, 어떤 외부 서비스에 계정을 두고 있고, 그 자격증명을 어떤 기준으로 나눠 뒀는지다.

## 지금 돌아가는 구성

| 구성 요소 | 실행 위치 | 비고 |
| --- | --- | --- |
| 봇 프로세스 | 클라우드 VM 위 Docker Compose 컨테이너 1개 | `restart: unless-stopped` |
| 데이터베이스 | 같은 VM의 파일 1개, 컨테이너에 바인드 마운트 | SQLite WAL, [ADR-0007](docs/adr/0007-keep-sqlite-single-file.md) |
| 대화 채널 | Telegram Bot API, long polling | 인바운드 포트와 공개 도메인이 필요 없다 |
| 모델 | Claude API | 실시간 대화와 배치에 다른 모델을 쓴다 |
| 새벽 정리 | 기본은 VM 밖 스케줄러, 폴백은 봇 안 크론 | 기본 경로는 모델 API를 쓰지 않는다 |
| 백업 | VM 호스트 크론 → 다른 계정의 오브젝트 스토리지 | 6시간마다, [ADR-0008](docs/adr/0008-offsite-db-snapshot-backup.md) |
| 관측과 알림 | 슬랙 채널 1개 | 파이프라인 판단 근거 게시, 백업 실패 알림 |

봇 안의 스케줄은 `node-cron`에 `Asia/Seoul`을 지정하고, VM의 호스트 크론은 UTC로 돈다. 두 자리에 시각을 적을 때 기준이 다르므로, 호스트 크론에 넣는 시각은 KST에서 9시간을 빼서 적는다. 백업의 `0 5,11,17,23`은 KST 기준 08:00 · 14:00 · 20:00 · 02:00이다.

## 백업 저장소를 VM과 다른 계정에 두는 이유

VM을 빌린 곳에도 오브젝트 스토리지가 있고 그쪽이 설정할 것도 적지만, 백업은 다른 제공자의 계정에 만든 버킷으로 보낸다.

이 백업으로 대비하려는 것이 VM 전체를 잃는 상황이라서다. 서버를 통째로 잃는 경로는 대부분 계정 쪽에 있고, 계정 정지나 본인 확인 문제, 쓰지 않는 자원을 회수하는 정책은 전부 계정 단위로 걸린다. 백업을 같은 계정에 두면 그때 원본과 사본을 함께 잃어서, 가장 그럴듯한 시나리오를 막지 못한다.

자격증명을 좁히는 데 드는 작업량도 양쪽이 다르다. 고른 쪽은 토큰을 만들 때 대상 버킷을 지정하면 그 버킷 밖으로는 닿지 않는다. VM 제공자 쪽은 접근 키를 사용자에 붙이는 구조라, 같은 수준으로 좁히려면 이 용도로만 쓰는 사용자와 그룹, 그 버킷만 허용하는 정책을 따로 만들어야 한다. VM에 이미 `rclone`이 있고 고른 쪽과 같은 형식의 원격이 동작 확인까지 끝나 있어서, 설정 형식도 그대로 재사용했다.

보관 비용은 판단에 넣지 않았다. 지금 크기로 압축한 스냅샷 한 벌이 200KB 아래고 보존 정책이 남기는 세대를 전부 합쳐도 20MB 수준이라, 어느 쪽을 골라도 차이가 없다.

## 지우는 권한을 버킷 하나로 좁히는 이유

보존 기간이 지난 파일을 지우는 명령이 6시간마다 사람 없이 돈다. 그 명령이 경로를 잘못 잡았을 때 어디까지 닿는지는 자격증명의 범위가 정하므로, 이 프로젝트만 쓰는 버킷을 따로 만들고 그 버킷에만 닿는 토큰을 쓴다. 다른 용도의 버킷과 계정을 나눠 쓰면 설정할 것은 없어지지만, 실수 한 번이 관계없는 데이터까지 지운다.

스크립트에도 같은 방어를 하나 더 둬서, 백업 대상 주소에 버킷 이름이 빠져 있으면 아무것도 하기 전에 멈춘다.

## 새벽 정리를 VM 밖 스케줄러에 두는 이유

하루치 기억 정리와 일기, 다음 날 각본은 모델 호출이 여러 번 필요한 작업이다. 기본 경로에서는 VM 밖 스케줄러가 수집 도구로 필요한 데이터를 읽어 가서 직접 생성하고 적용 도구로 결과만 돌려주므로, 이 경로가 도는 동안 프로젝트의 API 사용량은 늘지 않는다. 그 스케줄러가 돌지 않은 날에만 봇 안의 05:40 크론이 API로 같은 일을 한다. 두 경로가 같은 쓰기 코드를 쓰고 일기가 이미 있으면 건너뛰므로, 둘 다 돌아도 같은 날짜를 두 번 처리하지 않는다.

## 알림을 슬랙 채널 하나로 모으는 이유

봇 밖에서 봐야 하는 것이 파이프라인 판단 근거와 백업 결과 2가지인데, 둘 다 이미 있는 슬랙 봇 토큰 하나로 같은 채널에 보낸다. 토큰과 채널 값이 없으면 게시 코드 전체가 아무 일도 하지 않으므로, 이 값 없이 배포해도 봇 동작에는 영향이 없다.

백업은 실패한 회차마다 알리고, 주 1회 정상 요약도 함께 보낸다. 실패 알림만 두면 크론 자체가 등록에서 빠지거나 조용히 멈췄을 때 아무 소리도 나지 않는데, 요약이 오지 않는 것으로 그 상태를 알아차릴 수 있다.

## 자격증명을 나눈 기준

- 값은 VM의 `.env`에만 둔다. repo에는 키 이름만 적고, 값이 필요한 확인은 존재 여부만 센다.
- 사람 없이 도는 자격증명은 그 작업이 닿아야 하는 범위로만 만든다. 백업 토큰의 범위는 버킷 1개다.
- 새 자격증명은 CLI 대신 각 서비스의 대시보드에서 만든다. 무엇을 만들고 있는지 화면으로 확인하기 위해서다.
- 설정 스크립트는 비밀 값을 표준 입력과 환경변수로만 받는다. 명령 인자로 넘긴 값은 프로세스 목록과 셸 기록에 남는다.

## 이 문서에 적지 않는 값

제공자 이름과 계정 식별자, 버킷 이름, 엔드포인트 주소, 서버 경로, 접속 별칭은 여기 적지 않는다. 이 repo는 공개되어 있어서, 실제 값은 커밋하지 않는 로컬 문서에 둔다.
