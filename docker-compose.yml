services:
  app:
    build: .
    container_name: random-ai-companion
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/app/data/companion.db
    volumes:
      - ./data:/app/data
