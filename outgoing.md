# 연락 발송 구조

캐릭터가 유저에게 보내는 말은 유저 메시지에 답하는 답장과 먼저 거는 선톡으로 나뉘어 있고, 두 갈래가 각자의 판단 코드·예약 방식·상한 계산을 따로 갖고 있어서 새 상황이 생길 때마다 한 갈래에 코드가 붙는다. 이 문서는 그 둘을 같은 파이프라인 하나로 모으는 설계를 정한다.

설계 원본은 이 문서다. 관계 설계는 `relationship.md`, 시간과 기억은 `time-and-memory.md`, 코드 영역 지도는 `areas.md`에 있다.

## 이 문서가 정하는 것

1. 캐릭터가 보내는 모든 연락이 밟는 단계 4개
2. 연락 한 건을 적는 표 하나와 거기에 적는 값 10개
3. 종류마다 다른 것을 동작 3개와 값 2개로 두는 방식
4. 모델이 대화 중에 새 연락을 만들 수 있는 범위와 그 검증 방법
5. 이 표 밖에 두는 것

## 용어

| 말 | 뜻 |
| --- | --- |
| 연락 | 캐릭터가 유저에게 내보내는 말 한 건. 답장도 선톡도 같은 연락 한 건으로 다룬다 |
| 연락 예약 표 | 연락 한 건을 한 줄로 적는 표. 지금 `pending_replies`를 넓혀 쓴다 |
| 연락 행 | 표의 한 줄. 연락 한 건에 필요한 값을 컬럼 10개에 나눠 담은 데이터이고, 보낼 시각·보낼 내용·발송 결과가 같은 줄에 있다. 줄여서 행으로 적는다 |
| 종류 | 답장·아침 한 통·약속 연락처럼 연락을 가르는 이름. 종류마다 다른 것은 동작 3개와 값 2개뿐이다 |
| 중복 방지 키 | 같은 연락이 두 건 생기지 않게 막는 값. 아침 한 통은 날짜, 자리 비움 예고는 블록 시각처럼 종류마다 단위가 다르다 |
| 표시 칸 | 답장 JSON에서 모델이 채우는 항목. DB의 컬럼과 구분해 칸으로 적는다 |

## 지금 구조에서 코드가 늘어나는 자리

### 같은 사실을 적는 코드 2개

붙잡기로 일정을 취소한 사실을 `day_actuals`에 적는 코드가 둘이다. 답장을 만들기 전에 도는 붙잡기 판정이 `reply-timing.ts`에서 적고, 답장 모델이 stay 표시를 켜면 `recordHold`가 또 적는다. 먼저 적은 쪽이 이기고 두 번째는 조용히 무효가 되며, 게시함에는 판정이 적었는지 답장이 적었는지로 갈라 찍힌다. 같은 판단을 두 주체가 내리니 둘이 서로 다른 값을 낼 때마다 맞추는 코드가 붙는다.

### 서로 반대를 지시하는 규칙 2개

`prompts/reply.ts`는 답장에서 이따 바빠질 예정이어도 연락 예고를 하지 말라고 금지하는데(#368), 같은 파일의 붙잡기 규칙은 공적·답장 불가 구간에서 지금은 어렵고 끝나고 바로 연락하겠다고 말하라고 지시한다. 그 지시로 나온 약속을 코드가 받을 자리는 없다. 금지가 들어간 까닭이 코드가 그 말을 지킬 수단이 없어서였으니, 코드의 한계가 캐릭터 성격으로 넘어오면서 규칙층 안에서 부딪히기까지 한 자리다. 답장 규칙층 295줄 가운데 99줄이 금지문이고, 그런 답장을 볼 때마다 한 줄씩 늘어나는 방식이라 증가 속도 자체가 문제다.

### 공존하는 시각 관리 방식 2개

`pending_replies` 표가 예약 행으로 관리하는 경로(답장·깨우기·구간 끝 표시·약속)와, 틱 4개가 매분 조건을 다시 계산하는 경로(자리 비움·틈새 한 줄·팔로업·발송)가 따로 있다. 그래서 새 연락 종류를 넣을 때 어느 방식을 쓸지부터 정해야 한다.

### 행에 남지 않는 결말

깨우기 행과 약속 행은 핸들러가 아무 말도 안 보내고 돌아와도 보냄으로 닫힌다. 그 행이 실제로 어떻게 끝났는지는 깨우기 10가지, 약속 8가지로 갈리는데 그 구분이 게시함에만 남아서, 게시가 실패하면 답장이 사라진 사실 자체가 없어진다.

### 프로세스 메모리에만 있는 상태 5개

안 보낸다고 답한 자리(`followup.ts`), 확인 말이 아니라고 판정한 결과(`glance.ts`), 만들어 두고 들고 있는 메시지(`proactive-policy.ts`), 디바운스 대기, 선톡 잠금이 전부 Map과 변수로만 있다. 재시작하면 안 보낸다고 답한 자리가 사라져 같은 물음이 다시 간다.

### 대화 기록 메모를 상태 저장소로 쓰는 자리

하루 1통 중복 검사, 상한 세기, 예고를 이미 했는지, 구간 끝에 무엇을 보낼지가 전부 `messages.meta_json`을 문자열 포함 검사로 읽는다. 연락의 상태를 연락 기록에서 역산하는 방식이라 검사마다 질의가 하나씩 늘어난다.

### 이미 하나로 모인 것

반면 세 군데는 이미 모여 있어서, 이 설계는 그 위에 쌓는 것이 된다.

- 프롬프트: `buildSystemBlocks` 3층을 답장·선톡 9곳·새벽 정리가 함께 쓴다. 종류별로 다른 것은 꼬리에 얹는 상황 문단 1개뿐이다
- 발송: `sendBubblesTo` 하나다
- 선톡 발송 뼈대: `sendProactiveDraft`가 종류별로 다른 것만 spec으로 받는다(#300). 지금 spec이 받지 않는 것은 시각을 정하는 방식이다

## 네 단계

캐릭터가 보내는 모든 연락이 같은 4단계를 밟는다.

```
계기 발생 → 시각 정하기 → 메시지 만들기 → 내보내기
```

- **계기 발생**: 무언가 말할 이유가 생겨 표에 행을 만든다. 행을 만드는 코드는 발송을 신경 쓰지 않는다
- **시각 정하기**: 그 행을 언제 내보낼지 정한다. 즉시일 수도 있다
- **메시지 만들기**: 3층 프롬프트에 그 종류의 상황 문단을 얹어 보낼 말을 만든다. 미리 만들어 행에 적어 두는 종류와 시각이 됐을 때 만드는 종류가 있다
- **내보내기**: 보낼 조건을 다시 보고, 맞으면 발송한 뒤 결과를 행에 적는다

### 계기 조건과 발송 조건

계기 단계의 조건을 발송 단계로 가져오면 안 되고, 발송 단계의 조건을 계기 단계로 옮겨도 안 된다. 둘이 보는 사실이 다르다.

계기 조건은 행을 만들 시점에 답이 나온다. 블록이 불가인가, 새벽 정리가 돌았는가, 모델이 약속을 말했는가.

발송 조건은 행을 만든 뒤에 벌어진 일을 본다. 그 사이에 유저가 말을 걸었는가, 다른 연락이 먼저 나갔는가, 대화가 다른 데로 갔는가. 새벽 05:40에 만든 아침 한 통이 보내기 직전에 유저가 최근 4시간 안에 말했는지를 보는 것이 이 조건이고, 만들 때는 알 수 없는 사실이라 계기 쪽으로 옮길 수 없다.

지금 틱 4개가 매분 다시 계산하는 것 가운데 계기 조건은 행을 만드는 쪽으로 옮기고, 발송 조건은 발송 직전에 한 번 보는 판정으로 남긴다.

### 지금 경로를 끼워 보기

| 연락 | 계기 | 예정 시각 | 메시지 만드는 시점 |
| --- | --- | --- | --- |
| 답장 | 유저 메시지, 디바운스 뒤 | 두 태그 표 | 계기 때 |
| 붙잡기 답장 | 붙잡기 판정의 요청 | 20초~2분 30초 | 계기 때 |
| 구간 끝 | 불가 블록 진입 | 블록 끝 | 시각이 됐을 때 |
| 약속 연락 | 모델이 말한 약속 | 모델이 말한 시각 | 시각이 됐을 때 |
| 자리 비움 예고 | 블록 시작 10분 전 창, 유저가 4시간 안에 말했을 때 | 창 안에서 하나 | 계기 때 |
| 틈새 한 줄 | 불가 구간에 온 말을 모델이 확인 말로 판정 | 5~10분 뒤 | 계기 때 |
| 아침 한 통 | 새벽 정리 | 창 안에서 하나 | 새벽에 미리 |
| 안부 | 새벽 정리 | 창 안에서 하나 | 새벽에 미리 |
| 근황·밤 인사·달래기·살피기 | 조건 충족 | 창 안에서 하나 | 계기 때 |

구간 끝이 한 종류다. 몰아 답장·사이 예고·복귀 인사를 따로 두지 않고, 불가 블록마다 구간 끝 행 하나를 걸고 시각이 됐을 때 무엇을 보낼지 정한다. 블록 시작 뒤 유저 말이 있으면 몰아 답장, 없고 다음 블록도 불가면 사이 예고, 아니면 복귀 인사다. 지금은 깨우기 행이 나중에 복귀 행을 다시 걸고 마지막 메시지의 메모를 문자열로 훑어 갈래를 고르는데, 종류를 하나로 두면 그 단계가 전부 없어진다.

## 연락 예약 표

표 하나에 연락 한 건이 행 하나로 들어간다. 크론은 시각이 된 행을 집어 발송 판정·메시지 작성·발송으로 넘기는 일만 한다.

### 연락 행의 컬럼 10개

| 컬럼 | 내용 |
| --- | --- |
| 종류 | 연락 종류 이름 |
| 대화·캐릭터 | 누구의 어느 대화인지 |
| 중복 방지 키 | 그 종류 안에서 같은 연락인지 가르는 값 |
| 보낼 수 있는 시각 | 이 시각부터 발송 대상 |
| 만료 시각 | 이 시각을 넘기면 버린다 |
| 종류별 값 | 미리 만든 말풍선, 약속 문장, 어느 블록 때문에 생긴 행인지, 보낸 뒤 적을 메모·관계 값 |
| 모델 호출 번호 | 이 연락을 만든 호출. 게시함이 그 연락의 스레드를 찾는 데 쓴다 |
| 상태 | 아래 절 |
| 사유 | 상태가 보냄이 아닐 때 정해진 목록에서 고른 값 |
| 시도 횟수·만든 시각·보낸 시각 | 재시도 간격 계산과 기록 |

### 중복 방지 키

`아침:2026-09-11`, `자리비움:14:00`, `구간끝:14:00`, `답장:<마지막 유저 메시지 시각>`, `약속:<호출 번호>` 같은 값을 넣고 대화·종류·키에 유일 제약을 건다. 단위는 종류마다 달라서, 아침 한 통은 하루에 한 통이라 날짜를 쓰고 자리 비움 예고와 구간 끝은 블록마다 한 통이라 블록 시작 시각을 쓰며 답장은 답하는 유저 메시지 시각을 쓴다.

이미 생긴 행을 찾아 지우는 방식이 아니라, 두 번째 행을 넣는 일 자체가 DB에서 막히는 방식이다. 지우는 쪽은 첫 행이 이미 나간 뒤일 수 있어 늦다. 이 컬럼 하나가 논리일·블록 신원·답하는 메시지 시각을 따로 두던 컬럼들을 대신하고, 지금 대화 기록 메모를 문자열로 훑는 중복 검사 6개를 제약 하나로 바꾼다.

답장 행의 키에 들어가는 값은 **묶음의 마지막 유저 메시지 시각**이다. 시작점이 아니다. 복구 표시가 이 값과 정확히 같은지 비교해서 실패한 행의 답장 책임을 풀기 때문에, 시작점으로 바꾸면 그 비교가 영원히 거짓이 되어 표시가 안 풀리고 복구 틱이 그 메시지를 이미 답한 것으로 본다.

창 끝과 유예 마감을 따로 두지 않고 만료 시각 하나로 둔다. 지금 창 끝은 늦게 나갔다는 메모 한 줄에만 쓰이고, 실제로 행을 버리는 경계는 유예 마감 하나다.

### 발송 상태와 사유

상태는 그 연락이 어떻게 끝났는지를 한 줄로 적는 컬럼이다.

| 상태 | 뜻 |
| --- | --- |
| 대기 | 보낼 시각을 기다리는 중. 미리 만든 메시지가 있으면 그 메시지도 행에 들어 있다 |
| 보냄 | 말풍선 전부가 나갔다 |
| 부분 발송 | 말풍선 3개 가운데 2개만 나갔다. 보냄도 실패도 아니면서 다시 보내면 안 되는 상태라 따로 둔다 |
| 건너뜀 | 시각이 됐는데 발송 판정이나 메시지 작성에서 접었다 |
| 폐기 | 만료 시각을 넘겨 버렸거나 조건이 사라져 버렸다 |
| 실패 | 발송이 끝까지 안 됐다 |

지금 보냄이 아무 말도 안 나갔는데 행만 닫힌 경우까지 덮고 있어서 건너뜀을 따로 둔다. 미리 만든 메시지를 행에 적어 두면 상태가 한 줄로 끝난다. 지금은 만들어 둔 메시지가 프로세스 메모리에 있어서 그것을 들고 다음 틱을 기다리는 상태를 따로 불러야 하는데, 메시지가 행에 있으면 그냥 대기다.

사유는 자유 문장이 아니라 목록에서 고른다. 운영에서 실제로 보는 값이 안 보낸 사유이고 그 분류가 20가지 가까이 되는데, 자유 문장으로 적으면 셀 수 없다. 목록에 반드시 들어가야 하는 값이 **다른 연락에 양보**다. 깨우기가 답장에 양보하고, 약속이 깨우기에 밀리고, 예고·틈새가 대화가 다른 데로 가서 접히는 것이 가장 흔한 사유인데 보냄·건너뜀·폐기 어디에도 안 들어간다.

## 종류마다 다른 것

종류마다 다른 것은 동작 3개와 값 2개다.

| 이름 | 역할 |
| --- | --- |
| 연락 계기 함수 | 한 틱에서 만들 행들을 돌려준다. 중복 방지 키·보낼 수 있는 시각·만료 시각·종류별 값. 답장과 약속처럼 이벤트가 만드는 종류는 이 함수가 없다 |
| 발송 판정 함수 | 시각이 됐을 때 보내도 되는지 본다. 보낼 수 있으면 빈 값, 아니면 접는 사유 |
| 메시지 작성 함수 | 종류별 값에 말풍선이 있으면 그것을, 없으면 모델을 불러 만든다. 모델이 안 보내기로 하면 건너뛸 사유를 돌려준다 |
| 하루 상한 | 선톡 상한에 세나 |
| 유저가 말을 더 보낼 때 | 버리나 그대로 두나 |

시각을 정하는 방식을 열거값으로 두지 않는다. 즉시·두 태그 표·블록 경계·창 안 랜덤·모델이 말한 시각을 값으로 적으면 그 값을 해석하는 분기가 스케줄러에 생기고, 그건 이 설계가 없애려는 모양이다. 연락 계기 함수가 시각을 계산해서 돌려준다.

메시지를 만드는 시점도 열거값이 아니다. 종류별 값에 말풍선이 있으면 모델을 안 부르고, 없으면 부른다.

선톡 틱을 막는 조건과 붙잡기 상태에서 도는지는 막히는 쪽의 발송 판정 함수에 한 줄로 적는다. 재시도 상한도 종류별로 두지 않고 만료 시각까지 고정 간격으로 다시 시도하며, 모델 호출이 실패한 것은 재시도 대상이 아니라 건너뜀으로 적는다.

한 종류 안에서 값이 갈리는 자리는 속성이 아니라 종류별 값에 적는다. 자리 비움 예고는 예고를 이미 말했는지와 직전 블록이 불가였는지로 창이 3가지로 갈린다.

이벤트가 만드는 종류는 어디까지를 한 계기로 묶을지도 같이 정한다. 유저가 문장을 끊어 보내면 지금은 `bot.ts`의 타이머가 20~40초를 세고 그 뒤에 답장을 만드는데, 직전 답장이 진지한 물음이었으면 유저가 더 오래 쓰느라 만드는 중에 새 말이 와서 그 답장이 폐기된다(9/11 관측). 기다릴 시간을 고정값 하나로 두지 않고, 이벤트를 받는 자리가 직전 답장이 무엇을 물었는지 보고 정한다.

## 모델이 만드는 연락

대화 중에 모델이 새 연락을 만들 수 있게 한다. 10분 뒤에 연락한다는 말이 각본에 근거가 없어도 실제로 10분 뒤에 나가는 것이 목표다.

### 검증할 수 있는 선언

지금 구조에서는 이것이 불가능하다. 유저가 읽는 선언은 말풍선 본문이고 코드가 보는 것은 답장 JSON의 표시 칸인데, 거기에 시각을 적을 칸이 없다. 표시 설명이 모델에게 시각은 적지 말라고 못 박아 두었고, 코드가 각본 블록 경계에서 시각을 고른다. 본문에 10분 뒤라고 쓰고 표시를 비운 답장은 검증할 대상 자체가 없어 그대로 나간다.

그래서 **표시 칸이 시각을 갖는다.** 모델이 말한 시각을 모델이 적고, 코드는 그 값을 읽어 행을 만든다. 본문 파싱은 하지 않는다.

시각 표현은 2종으로 시작한다.

| 종류 | 모델이 적는 값 | 코드가 하는 일 |
| --- | --- | --- |
| 상대 시각 | 분 수 | 지금 + n, 허용 범위 안으로 당기거나 늦춤 |
| 블록 경계 | 무엇이 끝나고인지 | 각본에서 경계 찾기 |

절대 시각은 상대 시각으로 적을 수 있어 따로 두지 않는다. 유저 일정의 한 시점을 가리키는 요청은 CLAUDE.md 남은 작업의 별개 항목이라 같은 단계에 넣지 않는다.

### 말한 시각과 지킬 수 있는 시각

재생성으로 막는 방식은 쓸 수 없다. 지금 재생성은 같은 프롬프트를 한 번 더 부르는 것이고 두 시도의 표시를 합치므로, 첫 시도의 못 지킬 약속이 두 번째 본문에 살아남는다. 답장 지연도 7~16초 늘고 그 사이 유저가 말을 보내면 대기가 누적된다.

그래서 모델이 적은 값을 받아들이고 코드가 조정한다.

1. 모델이 말할 수 있는 시각의 범위를 프롬프트에서 좁힌다. 코드가 거의 언제나 지킬 수 있는 범위만 말하게 한다
2. 범위를 벗어난 값은 코드가 가까운 쪽으로 당기고, 당겼다는 사실을 게시함에 남긴다. 약속을 깨는 것보다 10분이 15분이 되는 쪽이 낫다
3. 본문과 표시가 다른 답장은 막는 대신 게시함에 경고로 남긴다. 모델을 완벽하게 만들 수는 없어도, 본문과 표시가 다른 답장이 하루에 몇 건인지는 셀 수 있다

3번이 규칙층을 늘리는 방식을 대체한다. 지금은 그런 답장을 볼 때마다 금지 한 줄을 더하는데, 건수를 보고 정할 수 있다.

### 코드가 계속 정하는 것

모델에게 넘기지 않는다. 모델의 판단이 흔들려도 맞아야 하는 값들이다.

- 시각의 허용 범위와 잠 블록·논리일 경계 처리
- 하루 상한. 약속 행에 따로 상한을 두지 않으면 약속으로 선톡 상한을 우회할 수 있다
- 같은 시각의 다른 행과의 충돌. 표가 하나라 검사가 한 곳이다
- 턴을 넘는 기억. 모델에게 약속을 기억하라고 하지 않는다. 한 번 말하면 코드가 행으로 적고, 시각이 되면 코드가 다시 불러 그때 판단하게 한다

## 표 밖에 두는 것

**유저가 화면에서 기다리는 동안 입력을 주고받는 흐름.** 캐릭터 생성의 첫 인사와 새로 만나기가 그렇다. 생성이 끝나는 순간이 곧 보내는 순간이라 행을 만들어 크론에 넘기면 그 기다림에 틱 간격이 더 붙는다. 첫 인사는 캐릭터 행도 각본도 아직 없어서 3층 프롬프트에 넣을 내용 자체가 없기도 하다.

**디바운스 대기.** 유저가 이어 보내는 말을 20~40초 모아 한 답장으로 받는 단계는 표 앞에 있다. 이 대기 중에 프로세스가 죽는 경우를 복구 표시가 받으므로, 표로 옮기지 않고 그대로 둔다.

**이미 보낸 것을 고쳐 쓰는 자리.** 버튼 표시 갱신과 버튼 위 토스트는 새로 내보내는 말이 아니다.

## 반박에서 나온 제약

설계를 반박하게 해서 나온 것들이다. 구현 세션은 이 목록을 먼저 읽는다.

1. **지금 발송 경로가 예약 표를 항상 거치지 않는다.** 몰아 답장과 약속 답장은 메시지를 만든 직후 바로 내보내고 약속 등록이 그 뒤에 돈다. 그래서 표시를 읽는 시점에 말이 아직 안 나갔다는 성질은 지금 참이 아니고, 통합의 결과로 참이 되는 것이다
2. **예약 표 행은 말풍선이 비면 안 된다.** 그래서 시각이 됐을 때 메시지를 만드는 종류는 지금 두 번째 행을 넣고 자기는 보냄으로 닫는데, 그 사이에 유저가 말하면 유저가 본 적 없는 몰아 답장이 버려지고 첫 행은 이미 닫혀 있다. 행 모양을 먼저 고쳐야 발송 경로를 모을 수 있다
3. **약속 행이 그 약속을 담은 답장보다 먼저 울릴 수 있다.** 그때 코드가 그 답장을 버리고 아까 약속했다는 전제로 다시 만든다. 유저는 그 약속을 본 적이 없다. 표가 하나면 충돌 검사가 한 곳이라 이 창이 닫힌다
4. **깨우기 표시가 걸려 있으면 약속이 통째로 사라지고 행은 보냄으로 닫힌다.** 건너뜀을 따로 두는 이유가 이것이다
5. **답장을 버리기 전에 이미 DB에 쓰는 것들이 있다.** 말투 고정, 상대 상태, 관계 표시, 일정 취소가 폐기 전에 적히고 처음 기록과 일정 말함 표시는 폐기 뒤에도 남는다. 처음 기록은 종류마다 한 번만 들어가는 구조라 안 나간 답장이 그 자리를 영구 점유한다. 발송 전 검증을 넣으려면 쓰기 시점을 발송 뒤로 옮기는 일이 먼저 간다
6. **약속 시각의 후보가 지금은 블록 하나뿐이다.** 각본에 다음 블록이 없으면 약속을 못 걸고, 구멍 뒤 블록이 2시간 뒤면 곧 연락한다는 말이 2시간 뒤가 된다
7. **계기 단계에서 종류가 정해지지 않는 경로가 있다.** 구간 끝은 울리는 시점의 마지막 연락과 지금 블록으로 갈래 4개 가운데 하나를 고른다. 종류를 쪼개서 풀지 않고 종류는 하나로 두고 결과만 갈라서 푼다
8. **팔로업 6종은 한 틱에서 하나만 고른다.** 그 배타성과 순서가 코드 사다리의 부수 효과로만 있어서, 선톡 공통 발송 판정에 최근에 나간 선톡이 있으면 접는다는 한 줄로 옮겨야 한다
9. **답장 시각이 다음 불가 블록 안으로 넘어가는 것을 자르는 일(#381)이 이 설계와 같은 자리를 고친다.** 표가 하나면 시각을 넣는 자리가 한 곳이라 자르기도 한 곳이다

## 적용 순서

| 단계 | 내용 | 없어지는 것 |
| --- | --- | --- |
| 0 | 프롬프트 3층·발송 함수·선톡 발송 뼈대 공유 | 끝남(#300) |
| 1 | 블록 상태를 컬럼으로 두고 상황 문단을 그 컬럼에서 만들기 | 취소 사실이 프롬프트에서 사라지는 경로 |
| 2 | 일정 처분을 답장 모델이 정하고, 붙잡기 판정은 답할지만 판단 | 취소 기록 경로 2개, 맥락 없는 판정의 오판 대가 |
| 3 | 깨우기·약속 핸들러 400줄을 동작 그대로 메시지 작성 함수로 떼어내기 | 없음. 다음 단계들의 diff가 한 파일에 겹치는 것 |
| 4 | 행 모양과 발송 상태·사유를 위 표대로 맞추기 | 연락이 사라진 사실이 안 남는 자리, 만들어 둔 메시지를 들고 있는 Map |
| 5 | 모든 연락이 예약 표를 거치게 하기 | 즉시 발송 갈래, 약속이 발송 뒤에 걸리는 순서 |
| 6 | 선톡과 답장의 판단을 계기·발송 판정·메시지 작성으로, 틱 4개를 하나로. 답장을 한 계기로 묶기까지 기다릴 시간도 여기서 정한다 | 틱 4개, 메모리 상태 5개, 대화 기록 메모를 훑는 질의 6개 |
| 7 | 규칙층의 반대되는 규칙 2개를 한 방향으로 맞추고 쓰기 시점을 발송 뒤로 | 규칙층 안의 모순, 안 나간 답장이 기록을 점유하는 자리 |
| 8 | 표시 칸에 시각을 넣고 모델이 연락을 만들게 하기 | 연락 예고 금지(#368), 각본 경계에 묶인 약속 시각 |

3단계가 순수 리팩토링이다. 지금 깨우기·약속 핸들러와 디바운스·발송·온보딩이 한 파일에 있어서, 떼어내지 않으면 4단계부터 8단계까지가 모두 같은 파일을 다시 연다.

4단계가 5단계보다 먼저다. 행 모양을 안 고치고 발송 경로부터 모으면 제약 2번에 걸린다.

7단계가 8단계보다 먼저다. 금지를 걷는 일과 본문·표시 불일치를 세는 일을 한 단계에 같이 넣으면, 세는 장치가 붙기 전에 금지가 풀린다.

각 단계는 옛 경로를 지우면서 끝낸다. 취소 기록이 지금 2곳에서 나는 것이 절반만 옮기고 멈춘 흔적이라, 같은 방식으로 진행하면 단계마다 공존 경로가 하나씩 늘어난다.

## 작업 세션 나누기

세션마다 이슈 1개, 브랜치 1개, PR 1개다. 판단이 갈리는 세션은 opus로 돌리고, 동작을 그대로 두고 파일만 떼어내는 세션 2만 sonnet으로 돌린다.

| 세션 | 단계 | 하는 일 | 모델 | 머지 전 사람 확인 |
| --- | --- | --- | --- | --- |
| 1 | 1·2 | 블록 상태를 컬럼으로, 일정 처분을 답장 모델이 정하게. 붙잡기 판정은 답할지만 본다 | opus | 붙잡기 뒤 일정이 취소·복귀 가운데 맞는 쪽으로 보이는지 |
| 2 | 3 | 깨우기·약속 핸들러를 동작 그대로 떼어내기. 동작 변경 금지 | sonnet | 게시함의 깨우기 10단계·약속 8단계가 전과 같은 순서로 찍히는지 |
| 3 | 4 | 연락 행 모양·발송 상태·사유. 스키마 1판, 기존 행 옮기기, ADR 1건 | opus | 다음 날 예약 표에 상태·사유가 차는지 |
| 4 | 5 | 모든 연락이 예약 표를 거치게. 즉시 발송 갈래 제거 | opus | 몰아 답장·약속 답장이 전과 같은 시각에 나가는지 |
| 5 | 6-가 | 선톡 4틱을 계기·발송 판정·메시지 작성으로 옮기고 틱 하나로 합치기 | opus | 선톡 종류마다 하루 건수가 전과 같은지 |
| 6 | 6-나 | 답장·구간 끝·약속을 같은 3개로 옮기기. 답장 시각 자르기(#381)와 한 계기로 묶기까지 기다릴 시간을 여기서 한 곳에 넣는다. ADR 1건 | opus | 구간 끝이 몰아 답장·사이 예고·복귀 인사 가운데 맞는 쪽을 고르는지, 진지한 물음에 유저가 길게 답할 때 답장이 폐기되지 않는지 |
| 7 | 7-가 | 규칙층 모순 정리, 선톡 메시지 프롬프트의 규칙 중복 걷기(#89) | opus | 평가 통과율, 답장 말투 |
| 8 | 7-나 | 답장이 DB에 쓰는 시점을 발송 뒤로 | opus | 안 나간 답장이 처음 기록을 점유하지 않는지 |
| 9 | 8 | 표시 칸에 시각, 허용 범위 좁히기, 범위 밖 값 당기기, 본문·표시 불일치 세기 | opus | 모델이 말한 시각에 실제로 연락이 가는지, 당긴 건수와 불일치 건수 |

fable은 되돌리기가 가장 비싼 자리가 스키마라서 세션 3을 시작하기 전에 한 번만 쓴다. 컬럼 10개와 상태 6개, 사유 목록, 기존 행을 옮기는 계획을 한 묶음으로 놓고 반박하게 한다. 사유 목록은 20가지 가까운 값을 이 세션에서 처음 적는 것이라 빠진 값이 있으면 나중에 사유별 건수를 셀 수 없다. 설계 문서 자체는 fable이 이미 한 번 반박해서 컬럼이 16개에서 10개로 줄었고, 남아 있는 것은 사유 목록의 빠진 값과 기존 행을 옮기는 순서다.

세션 9의 허용 범위와 조정 규칙도 fable에 물을 만하지만, 평가 통과율과 게시함 건수로 수치가 나오는 자리라 사용량이 남을 때만 쓰고 나머지 세션은 opus로 충분하다.

세션 7과 9는 `prompts/reply.ts`·`reply-signal.ts`를 고치므로 PR에 `eval` 라벨을 붙여 평가를 돌린 뒤 머지한다. 세션 3은 스키마를 올리므로 운영 DB에서 값을 확인한다.

## 리드미에 옮길 요약

캐릭터가 보내는 말은 답장이든 먼저 거는 연락이든 같은 4단계를 밟는다. 말할 이유가 생기면 보낼 연락 한 건을 표에 적고, 언제 보낼지 그때 정하고, 시각이 되면 할 말을 만들고, 보내기 직전에 지금도 보낼 상황인지 한 번 더 본다. 마지막 확인을 따로 둔 까닭은 연락을 적을 때 알 수 없는 일이 있어서다. 새벽에 적어 둔 아침 인사는 유저가 아침에 먼저 말을 걸면 보내지 않아야 하는데, 적는 시점에는 그 일이 일어날지 알 수 없다.

연락 종류가 늘어도 코드가 같은 모양으로 남도록, 종류마다 정하는 것을 연락을 만드는 함수·보낼지 판정하는 함수·할 말을 만드는 함수 3개와 하루 상한 여부·유저가 말을 더 보냈을 때 버릴지 2개로 제한해 뒀다. 같은 연락이 두 건 생기는 일은 종류마다 단위를 정한 키로 막는다. 하루 한 통인 아침 인사는 날짜, 일정 블록마다 한 통인 자리 비움 예고는 블록 시각이 단위다.

캐릭터가 10분 뒤에 연락하겠다고 말하면 코드가 그 약속을 지킨다. 모델이 답장에 말한 시각을 함께 적고, 코드가 지킬 수 있는 범위인지 보고 벗어나면 가까운 시각으로 당겨 예약한다. 지킬 수 없는 말을 아예 못 하게 막는 대신 받아서 조정하는 쪽으로 만들어 둬서, 캐릭터가 할 수 있는 말이 코드 사정에 따라 줄지 않는다.

## 열어 둔 자리

- 시각이 됐을 때 모델에게 다시 묻는 방식을 어디까지 넓힐지. 코드가 미래를 예측해서 행을 지우는 분기가 사라지는 대신 호출이 늘어난다. 빈도가 높은 자리부터 옮기며 호출 수 증가분을 보고 정한다
- 선톡 메시지도 답장 표시 형식으로 답하게 할지. 그러면 복귀 인사에서 한 약속도 걸리는데, 한 줄 메시지에 JSON을 요구하면 출력 토큰이 붙는다. 종류별로 갈라 두고 하나씩 켠다
- 모델이 만드는 연락 종류를 약속 말고 어디까지 열지. 근거 종류가 코드의 목록에서 모델이 만드는 것으로 넓어지는 만큼 상한과 세는 장치가 먼저 있어야 한다
