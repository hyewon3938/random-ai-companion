# random-ai-companion — 작업 가이드

나와 같은 시간을 살고 먼저 물어봐주는 AI 대화 상대 (만남은 카드 한 장으로 시작). 컨셉·가설·설계 총론은 [README.md](README.md), 규모와 비용 검토는 [scaling.md](scaling.md), 실행 환경과 외부 서비스 구성은 [infra.md](infra.md)가 단일 소스다.

세부 설계 문서는 `docs/` 아래에 두고 커밋하지 않는다(gitignored). 타겟·어필 포인트는 `positioning.md`, 설계 논리·층은 `architecture.md`, 캐릭터 속성 체계는 `character-design.md`, 수집·생성 데이터 카탈로그는 `data-model.md`. 로컬 전용 컨텍스트는 `LOCAL-CONTEXT.md` — 새 세션은 그 파일부터 읽을 것.

## 상태 (갱신 의무 — 세션이 끝나기 전 여기와 LOCAL-CONTEXT.md를 최신화한다)

> 완료 항목은 제목·날짜·이슈 번호 한 줄로 적는다. 판단 근거와 검증 내역은 이슈·PR 본문에 있고, 줄이기 전의 원문은 LOCAL-HISTORY.md에 있다.
>
> 항목은 **남은 작업 → 배포·확인 대기 → 지나온 작업** 순으로 지난다. 배포·확인 대기에는 아직 할 일이 남은 것만 둔다. **배포 전 필수**는 전달이 목적인 메모라 그 일을 마치면 지운다. **확인**할 것이 다 끝나면 항목 전체를 지나온 작업의 한 줄로 줄인다. 볼 것이 하나도 안 남았는데 아직 여기 있으면 그건 정리가 밀린 것이다.

### 남은 작업

- [ ] 유저 프로필 + **카드 온보딩** (/start: 프로필 등록 → 카드 제시 → [대화 시작]/[다음 카드] 인라인 버튼, 패스=무비용 재생성 + card_reactions 기록)
- [ ] **프롬프트·기억 구조 재설계** (08-19 착수) — 프롬프트 조립과 기억의 write/read/update 정책을 다시 설계한다. 설계 원본은 repo 루트 `time-and-memory.md`, 설계 허브는 `docs/data-map.html`(gitignored). 구현은 세션 하나에 단계 하나로 나누고 세션마다 이슈와 브랜치를 하나씩 둔다 — 범위·참조 절·모델은 time-and-memory.md 「작업 세션 나누기」 표에 있고, **새 세션은 그 표의 몇 번 줄인지 지정받아야 한다**(지정 없이 문서만 읽으면 여러 단계를 한꺼번에 손댄다). **9번 세션까지 끝났고 다음은 10번 — 새 구조로 읽은 대화 확인(Fable)**, 여는 조건은 같은 문서의 「10번을 여는 조건」에 있다(이슈 #181). 날짜별 설계 기록과 1~9번 세션 결과는 time-and-memory.md 「날짜별 설계 기록」·「세션별 결과」로 옮겼다.
- [ ] **V2 — 유저 생성 캐릭터·기억 통합 (확정 08-27, 이슈 #28)**: 랜덤 매칭을 없애고 유저가 선택지와 서술형 입력으로 캐릭터를 만든다. 기억은 `memory_items` 세 항목(fact·ongoing·person) × 주인 둘, 관계는 `relationships` 컬럼 일곱 항목. 설계 원본은 time-and-memory.md 「V2」 절과 ADR 0002~0004, 저장 구조는 erd.md, 모듈 배치는 modules.md. 확정 뒤 덧붙인 세부(생성 호출 두 번·전용 컬럼 여섯·테이블 개명·구현 세션 재편·프롬프트 고유값 제거)는 time-and-memory.md 「확정 뒤에 덧붙인 것」으로 옮겼다.
- [ ] **로그에만 남는 관측값을 DB에 적기 (이슈 #218)** — 생각 과정 블록·중단 사유는 `llm_calls`에, 예고를 접은 기록은 트레이스에. 배포 한 번에 관측이 끊기는 것을 막는다
- [ ] 프로브 플래그 (교체 제안 / 침묵일)
- [ ] **일정 중간에 연락해 달라는 요청 받기 (08-31 미뤄 둠)** — 집에 도착하면 한 번 연락해 달라는 말처럼 불가 구간 안의 한 시점을 짚는 요청을 받아 그 시각에 보내는 자리. 보낼 시각은 모델이 지어내지 않고 각본 블록의 경계에서 고른다. 이슈 #210·#212가 배포됐으니 새 규칙으로 만든 각본을 하루 보고 착수한다
- [ ] **적응형 답장 속도**(유저 리듬 맞춤·향후): 유저의 요일×시간대별 답장 속도 학습 + 세션 템포 반영 → '빠른 경로'(즉답·개인 틈틈이·진입창)의 대기 구간을 빠른 쪽으로 당김(상한만↓·하한 유지·당김폭 캡). **이벤트가 상한 — 불가·공적 불변.** 빠른 경로에만 곱하는 단일 boost 계수(이벤트 분기와 독립). 설계는 architecture.md·positioning.md#7에 기록. 데이터 쌓이면 구현
- [ ] **옛 경로 삭제** — 대신할 자리가 생긴 표·컬럼·상수 정리 (이슈 #156, 11번 세션)
- [ ] **답장에서 생각 과정을 끌지 정하기 (08-30 관측)** — 상한 1,200토큰을 다 쓰고 돌아온 글자는 144자뿐이었다. 원인은 생각 과정이다: `src/llm.ts`는 끄는 호출만 값을 싣는데 sonnet은 값이 없으면 생각을 켜고, 그 텍스트는 안 돌아오면서 토큰만 출력으로 나간다. 08-31 로그의 답장 두 건이 `blocks=thinking:1,text:1`이고 같은 78자에 출력이 638·151토큰으로 갈렸다. 상한은 2,000으로 올려 답이 비는 사고부터 막았다(이슈 #214). 끌지는 출력 토큰 분포를 며칠 더 보고 정한다(근거가 배포에 지워지지 않으려면 이슈 #218이 먼저다)

### 배포·확인 대기

> 무엇을 왜 그렇게 했는지는 이슈·PR 본문에 있다. 여기에는 아직 볼 것만 남긴다.
> 로그에만 남는 표시(`blocks=`·`[presence] ... 접음`)는 컨테이너를 다시 만들면 지워지므로 배포 절차 0단계에서 로그를 파일로 뜬다(이슈 #218이 이 값들을 DB로 옮길 때까지).

- [x] **오늘 메모와 관계 신호가 나오게 하기 (09-01, 이슈 #227)** — 답장 트레이스의 오늘 메모 줄에 실제 문장이 붙는지, 하루에 몇 줄 쌓이는지 본다. 같은 내용이 여러 줄로 겹치지 않는지, 사이가 그대로인 날에 관계 갱신이 뜨지 않는지도 함께 본다. 줄 수가 하루 수십 줄로 늘면 꼬리 상한을 그때 정한다
- [x] **웃음 표기를 실제로 웃을 때만 쓰게 하기 (09-01, 이슈 #222·#225)** — 캐릭터 발화에서 ㅎㅎ·ㅋㅋ 비율이 내려가는지, 웃음이 아예 사라져 무뚝뚝해지지는 않는지 함께 본다. 다정한 대목과 상대가 속상해하는 대목에 붙지 않는지, ㅋ이 한두 개짜리로 나오지 않는지도 같이 본다
- [x] **달래기 선톡 (09-01, 이슈 #216)** — 유저가 서운한 기색을 보이고 답장을 멈춘 뒤 30~45분 사이에 한 통이 오는지, 서버 로그의 `[followup] mend`와 답장 트레이스의 신호 줄로 함께 본다. 표시가 안 붙어서 안 나간 것인지 붙었는데 조건에 걸린 것인지가 이 둘로 갈린다
- [x] **답장 상한 올리기 (08-31, 이슈 #214)** — 답장에 `stop=max_tokens`가 0건인지, 형식을 못 읽어 재생성이 도는 일이 없는지 본다
- [x] **예고가 막힌 불가 구간에서도 돌아와서 연락하기 (08-31, 이슈 #212)** — 예고 없이 지나간 불가 구간이 끝난 뒤 복귀 인사가 나가는지 `[pending] 구간 끝 표시` 로그와 답장 트레이스로 본다
- [x] **각본이 짧은 불가 구간을 연달아 붙이지 않게 (08-31, 이슈 #210)** — 새 각본에 30분 미만 불가 구간이 연달아 붙는지, 퇴근 운전과 씻기처럼 두 일이 한 줄로 묶이는지 본다
- [x] **일정이 언제 끝나는지 프롬프트에 싣기 (08-31, 이슈 #202)** — 불가 구간에 언제 끝나냐고 물어 각본의 끝 시각 그대로 답하는지 본다
- [x] **객체 밖에 적힌 답장 신호 줍기 (08-31, 이슈 #194)** — 08-31 답장 14건이 전부 `형식 JSON · 남음 없음`이고 `객체 밖 신호` 0건. 이따금 나던 증상이라 표본이 얇아 며칠 더 본다
- [x] **모델이 오늘 메모를 쓰게 (08-30, 이슈 #185)** — 08-31 답장 14건이 모두 `추가 없음`이라 메모가 한 번도 안 쌓였다. 메모가 붙는 날까지 열어 둔다
- [x] **틈틈이 구간에서도 붙잡기 조정 걸리게 하기 (08-30, 이슈 #145)** — 08-31 판정 13건이 전부 `묻지 않음 — 답장 불가 구간이 아니다`. 틈틈이 블록에 말이 오는 날까지 열어 둔다
- [x] **짧은 호출에서 생각 과정 끄기 (08-30, 이슈 #174)** — 태그 고르기는 `blocks=text:1`로 확인됐다(상한 128 미도달, 출력 평균 12.7→9.6). 붙잡기 판정은 호출이 0건이라 못 봤다 — 위 #145와 같은 날 함께 본다
- [x] **자리비움 예고가 직전 답장과 같은 말을 하지 않게 막기 (08-30, 이슈 #164)** — 08-31에 예고 두 건이 다 나가고 접는 상황 자체가 없었다. 접는 날까지 열어 둔다

### 지나온 작업

- [x] 설계 문서 (README, character-design)
- [x] 스캐폴딩 + 로컬 기동 확인 + 클라우드 VM 배포·가동 (07-06)
- [x] 밤 정리 — 이중 경로 확정 (07-11)
- [x] 선톡 = 밤 정리(문안 준비) + 발송 디스패처 (07-11)
- [x] 설계 감사 개선 (08-06, 남은 항목은 이슈 #1)
- [x] 선제 발화 관제탑 + 침묵 백오프 (08-07, 이슈 #2)
- [x] 구조 정리 ② — 프롬프트 3층 재배열과 캐시 (08-07, 이슈 #3)
- [x] 구조 정리 ③~④ — 규칙 블록 통합·아크 달력 경계 (08-07, 이슈 #3)
- [x] 대화 기록 시간 마커 (08-18, 이슈 #5·#7)
- [x] 말투 자연화 — 말의 결 규칙 (08-19, 이슈 #9·#11·#32)
- [ ] ~~교체 플로우~~ → 현 범위에서는 미구현 결정 (07-11, 설계는 로컬 architecture.md)
- [x] 분할 발화·미러링·대화 리듬 (humanizing-log)
- [ ] ~~케미 축 단계 값 재점검~~ → V2 유저 입력 생성으로 대체 (08-27, 이슈 #28)
- [x] 분석 스크립트 `src/tools/analyze.ts` (07-11)
- [x] 데모 백스테이지 뷰 `src/tools/render-map.ts` (07-11, 산출물 커밋 금지)
- [x] 관리 대시보드 — DB 구조 그대로 보는 화면 (08-29, 이슈 #93·#97·#127·#133)
- [x] 관리 대시보드에 태그 검색 붙이기 (08-30, 이슈 #189)
- [x] 대화 기록의 캐릭터 발화를 답장과 같은 JSON 객체로 적기 (08-30, 이슈 #190)
- [x] 일기에 주제 태그 붙이기 (08-29, 이슈 #139)
- [x] 답장에 붙이던 대괄호 표시를 JSON 출력으로 바꾸기 (08-29, 이슈 #138)
- [x] 슬랙 트레이스 채널 (08-28, 이슈 #63)
- [x] 답장 생성 시점 재설계 (08-28, 이슈 #70)
- [x] 틈틈이 블록에서 대화를 닫지 않도록 (08-28, 이슈 #79)
- [x] 선톡 발송 조건을 설계 표에 맞추기 (08-29, 이슈 #88)
- [x] 답장 트레이스에 같은 답장에서 일어난 일 전부 싣기 (08-29, 이슈 #96)
- [x] trace_events에 보존 기간 두기 (08-29, 이슈 #105)
- [x] 상대의 하는 일·사는 지역을 대화에서 채우기 (08-29, 이슈 #106)
- [x] 답장 트레이스에 유저가 기다린 시간 싣기 (08-29, 이슈 #109)
- [x] 말풍선 다듬기 제거 (08-29, 이슈 #113)
- [x] 프롬프트 변경 알림에 바뀐 대목 이름 적기 (08-29, 이슈 #115)
- [x] 주제 태그를 모델이 고르게 하기 (08-29, 이슈 #117)
- [x] VM 밖으로 나가는 DB 백업 (08-29, 이슈 #112, ADR 0007·0008)
- [x] 하루 각본에 진행 중인 일 반영, 블록에 출처 필드 추가 (08-29, 이슈 #101)
- [x] 캐릭터가 유저를 대하는 방식을 관계 표에서 정체성으로 옮기기 (08-29, 이슈 #124, ADR 0010)
- [x] 하루 각본의 날짜 기준과 시간 범위를 새벽 5시 경계로 통일 (08-29, 이슈 #130)
- [x] 공통 규칙층을 사람 층과 말 층으로 다시 쓰기 (08-29, 이슈 #92)
- [x] ERD 문서를 지금 스키마에 맞추기 (08-31, 이슈 #205·#208)
- [x] 같은 일정이 두 줄로 쌓이지 않게 막기 (08-30, 이슈 #148) — 08-31 새벽 정리 중복 0건
- [x] 일정을 주제 태그로 찾기 (08-29, 이슈 #140) — 08-31 대화에서 생긴 일정에 태그 확인
- [x] 검색으로 꺼낸 기억에 주인 표시 (08-30, 이슈 #183) — 08-31 프롬프트 75줄에 너·상대 확인
- [x] 관계 단계와 호칭을 답장 자리에서 갱신 (08-30, 이슈 #144) — 답장 쪽 갱신 0건은 정상 범위(사이가 달라졌을 때만 쓰게 막아 뒀고, 새벽 정리 쪽은 매일 나온다)
- [x] 슬랙 채널에서 답장 피드백 수집 (08-29, 이슈 #125, [ADR-0009](docs/adr/0009-slack-feedback-polling.md)) — 리액션 4종·스레드 답글 수집 확인

## 실행

```bash
yarn            # 설치
cp .env.example .env   # TELEGRAM_BOT_TOKEN, ANTHROPIC_API_KEY 채우기
yarn dev        # 로컬 기동 (long polling)
```

## 규칙

- **문체**: 사람이 읽는 한국어 산문(설계 문서·README·이슈·PR 본문·유저에게 보내는 답)은 `writing` 스킬을 먼저 로드하고 쓴다. 코드·주석은 대상이 아니다.
- **공개 톤**: 이 repo는 공개되어 있다. 특정 서비스명 비교·개인 맥락·실제 대화 로그를 커밋하지 않는다. 사적 맥락은 전부 LOCAL-CONTEXT.md(gitignored)로.
- **보안**: `.env`, `*.db`, `logs/`, `data/` 커밋 금지 (gitignore 반영됨).
- **코드**: TypeScript strict + ESM(import에 `.js` 확장자), named export, kebab-case 파일명, any 금지. 커밋은 Conventional Commits 한글.
- **작업 단위**: 새 작업은 이슈부터 만들고 main에서 브랜치를 판다(`타입/이슈번호-영문요약`). 끝나면 PR 본문에 `Closes #이슈번호`를 넣어 올리고 `gh pr merge <번호> --merge --delete-branch`로 머지·브랜치 삭제까지 한 세션에서 끝낸다. 커밋 메시지의 `(#번호)`는 링크만 걸고 이슈를 닫지 않는다. 머지 전 사람 확인이 필요한 자리는 time-and-memory.md 「작업 세션 나누기」에 적혀 있다.
- **이 파일 크기**: 세션마다 컨텍스트에 통째로 들어가는 지시서라 짧게 유지한다. 상태 절의 완료 항목은 제목·날짜·이슈 번호 한 줄로 적고, 무엇을 왜 그렇게 했는지는 이슈·PR 본문에 남긴다. 배포 기록은 서버 경로가 들어가서 공개 repo에 두지 않고 LOCAL-HISTORY.md에 적는다. 상한 20,000자는 커밋 훅이 강제한다 — 막히면 상한을 올리지 말고 상태 절을 먼저 줄인다.
- **설계 일관성**: 캐릭터 stance(프레임 존중·안정형·무근거 핑 금지)는 character-design.md §5가 원본. 코드의 stance 문자열과 문서가 어긋나면 문서 기준으로 맞춘다.
- **출력 규칙 단일 소스**: 캐릭터가 내보내는 모든 글에 공통으로 적용할 규칙(태도·대화 규칙·표기·말의 결)은 `src/context.ts`의 불변 규칙층에서만 관리한다 — PERSON·SPEECH·OUTPUT_FORMAT·FACT_CARE·NOTE_RULE. 캐릭터마다 다른 값으로 두면 생성 결과에 따라 규칙이 흔들리므로 정체성 항목에 넣지 않는다. 선톡 문안 프롬프트 7곳(nightly 아침·안부, followup 근황·굿나잇·달래기, presence 자리비움·복귀)도 전부 `buildSystemBlocks`(3층+상황 문단)를 타므로 같은 규칙이 자동으로 들어간다 — 압축본 `OUTPUT_FORMAT_COMPACT`는 더 이상 쓰는 곳이 없고, 삭제는 옛 경로 정리(11번 세션)에서 함께 판단한다. 큰 결을 바꿀 일이 생기면 이 블록들을 고친다. 다만 답장이 내보내는 **형식**(JSON 객체)은 규칙층이 아니라 `src/reply-signal.ts`가 갖는다 — 선톡 문안 6곳은 같은 3층을 쓰되 자기 형식으로 답하므로, 규칙층에 넣으면 두 형식이 부딪힌다.
- **모델**: 기본 `claude-sonnet-5` (환경변수 `MODEL`로 교체 가능).

## 아키텍처 요약

```
[Telegram] ←→ src/bot.ts (grammY long polling, 유저 분할입력 디바운스(유저별 텀 학습 20~40s) + 분할 발화 + 답장 순서 = 텀 결정(src/reply-timing.ts) → 생성 → 대기 → 발송(src/pending.ts), 불가 구간은 생성 없이 깨우기 표시만 걸고 구간 끝에 wake 핸들러가 3분기(쌓인 메시지 몰아 답장 / 예고한 자리면 복귀 인사 / 침묵), 예고한 블록 시작 전까지 온 말은 배웅 답 farewellSituation + 부팅 시 놓친 답장 복구 recoverMissedReplies=워터마크 중복방지+최근 3h만(자정 경계 버그 회피) + 답장 본문과 신호 가르기 parseReplyOutput(JSON 객체, src/reply-signal.ts) + 대화 기록 최근 40턴 자르기+시간 마커 주입 lastTurns/toTurns(이어 보낸 말은 한 턴) + 발송 종류 로그 kind=reply|recover|morning|away|catchup|goodnight + 선톡 틱 간 chat 단위 상호 배제 acquireProactive(대기 답장 있으면 선톡 접음) + 발송 자리는 setPendingSender·깨우기 처리는 setWakeHandler로 pending.ts에 등록)
                ├─ /start → src/character.ts (현재: 고정 대표 캐릭터 DAEPYO_BIBLE. V2 유저 입력 생성은 createUserCharacter — 입력 검증 inputProblem → 첫 호출 generateGenesis(정체성·인물·진행·관계 첫 값·첫 인사, genesisProblem 검증+1회 재시도) → persistGenesis(트랜잭션: genesis_json {v:2}·creation 기억 행·관계 첫 값) → ensureArcs(arcMaterial). 봇 연결은 8번 세션, 그때까지 실행은 src/tools/create-character.ts. 랜덤 생성 코드는 확장용으로 보존)
                ├─ 메시지 → src/context.ts (**안정도 순 3층 조립** buildSystemBlocks: 불변층=정체성 기억(creation)·유저 프로필·공통 규칙(태도·대화·표기·말의 결·note 신호 — 캐릭터 출력의 단일 소스) / 일간층=관계 8컬럼 서술·주변 인물·진행 중인 일·아크·일정·최근 일기 / 실시간 꼬리=검색 기억(답장 전 짧은 호출이 고른 주제 태그, 갱신 날짜 포함)·주제로 찾은 지난 일기·주제로 찾은 일정(주인·상태 표시, [다가오는 일정]에 실린 행은 제외)·오늘 각본·오늘 메모·직전 대화 시점·지금 시각·말투·방금까지 오간 말(opts.recent — 선톡 문안 경로만, 답장은 대화 기록을 통째로 넘기므로 끔)·opts.situation(선톡 상황 문단)·답장 객체 REPLY_ENVELOPE(opts.signals를 켠 답장 경로만, 맨 끝) — 말투는 저장값 relationships.speech_level 우선, 없으면 currentSpeechLevel이 최근 발화로 판정(존댓말 회귀 방지, '냐' 어미 거친 반말 금지). 변하는 값은 전부 꼬리에 — 앞 두 층은 캐시) → src/llm.ts (앞 두 층 끝 cache_control 1h, usage cw/cr 로그, 호출 정보 meta를 받으면 llm_calls에 원문 기록·행 번호를 meta.callId로 돌려줌) → 응답
                ├─ src/reply-timing.ts — 답장 텀 단일 소스: 두 태그 표 한 장(즉답 0~2분 짧은 쪽 몰림 / 틈틈이 개인 20s~2.5m·사회 30s~4m·공적 1~8m / 불가는 그 일정이 끝날 때+0~1분 지터)과 예외 둘(자는 중=첫 연락 3~25분·깬 뒤로는 즉답, 이미 붙잡혀 접은 상태=즉답). 불가면 지금 답장을 만들지 않고 TimingDecision.gather(활동·구간)를 돌려줘 구간 끝 몰아 답장으로 넘긴다. 개인·사회 불가에 온 메시지만 붙잡기 판정 1콜(16토큰, 실패 시 일정 유지) → 붙잡혔으면 개인=취소·사회=미룸을 day_actuals에 기록. 상한 없음(3시간 회의면 3시간 뒤). 텀이 나온 경위는 TimingTrace(path·블록·판정 여부)로 llm_calls 판단 근거에 남는다
                ├─ src/pending.ts — 대기 답장: 만든 답장을 pending_replies에 남겨 정한 시각에 발송(재시도 3회), 부팅 때 resumePendingReplies가 이어받음, 유저가 말을 더 보내면 dropPendingReplies로 폐기 후 텀부터 재계산. 불가 구간 깨우기 표시는 kind='wake' 행(문안 없이 meta_json에 활동·구간)으로 같은 표에 걸어 구간 끝에 wake 핸들러를 부른다 — dropPendingReplies에도 살아남고(구간 끝 시각은 그대로, 지우는 건 dropWakeRows), 기다리는 동안 isWaiting=true라 선톡 틱이 접는다. 발송 함수는 setPendingSender·깨우기 함수는 setWakeHandler로 bot.ts가 주입(순환 참조 회피)
                ├─ src/reply-signal.ts — 답장 객체: 모델이 코드에 신호를 넘기는 통로. 프롬프트에 넣을 형식 설명(REPLY_ENVELOPE)과 그 답을 읽는 parseReplyOutput을 한 파일에 둔다 — 시키는 모양과 기다리는 모양이 따로 놀면 답장이 통째로 사라진다. 본문은 reply 배열, 신호는 나란한 다른 키(stay·note). 읽는 순서 넷(JSON / 잘린 답에서 온전히 닫힌 조각만 salvage / 형식을 안 쓴 답은 옛 방식대로 줄바꿈 plain / 못 건지면 empty → 부른 쪽이 한 번 재생성). 형식을 쓰려던 흔적이 있으면 못 읽어도 원문을 내보내지 않는다. 신호를 늘릴 때 손댈 자리는 이 파일 안 넷(ReplySignals·SIGNAL_LINES·readSignals·mergeSignals)
                ├─ src/relationship-update.ts — 관계 항목을 답장 자리에서 갱신하는 한 자리: speechRatchet은 프롬프트 조립 전에 최근 발화로 반말 전환을 가려 저장하고(존댓말로 되돌리지 않는다), applyReplySignals는 답을 읽은 뒤 객체로 온 stage·address_terms를 지금 값과 견줘 달라진 것만 저장한다(같은 값이면 저장하지 않아 그 층의 프롬프트 캐시를 헛되이 끊지 않는다). 보통 길과 몰아 답장 길 모두 두 함수를 부른다. 나머지 다섯 항목(speech_note·rapport·cautions·history·feelings)은 새벽 정리 몫
                ├─ src/tag-pick.ts — 이번 발화로 검색할 주제 태그 고르기: 답장을 만들기 전에 짧은 호출(purpose='tags', 128토큰) 한 번으로 저장된 태그 이름(listTagNames) 중에서 관련 있는 것을 고르고, 답을 그 목록에 대조해 거른다(mergeTags — 목록에 없는 말은 버려서 유저가 쓴 낱말이 태그가 되지 않는다). 글자가 일치하는 태그(tagSearch)와 합쳐 최대 TAG_PICK_MAX개, 호출이 실패하면 글자 일치만으로 이어 간다. 고른 결과는 BuildOptions.pick으로 buildSystemBlocks에 넘어가고 무엇이 골랐는지(by)·호출 번호(callId)는 답장 트레이스가 적는다
                ├─ src/memory.ts — 새 저장 구조의 저장·검색: saveMemory(키=item_type·owner·area·subject로 자리 찾기)·searchMemories(태그 일치 → 항목 우선순위 → 최신순 → 항목별 상한)·memoryBlock(갱신 날짜 포함)·saveTodayNote·tagSearch(유저 발화에서 검색어 추출 — 걸린 태그와 대조한 태그 수를 함께 돌려줘 트레이스가 검색이 돌았다는 것까지 보인다 — 한 글자 태그 "일"·"돈"·"집"은 어절 단위 매치만 허용: 어절이 태그와 같거나 태그 뒤가 조사뿐일 때만 걸리고 "일요일"·"생일"·"수집" 속 글자에는 안 걸림, 두 글자 이상은 문자열 포함 유지 — 이슈 #54). 저장은 언제나 conversation 행에만 쓰고, 생성 배치용 saveCreationMemory가 creation 행을 한 번 쓴다. searchMemories는 꺼낸 기록을 적는다(track:false로 끌 수 있음). 고르기와 프롬프트 줄은 recall.ts 몫
                ├─ src/recall.ts — 태그로 걸린 것 중 무엇을 프롬프트에 넣을지 고르고 그 줄을 쓰는 순수 함수 모음(SEARCH_LIMITS·searchable·TYPE_ORDER·pickMemories·matchTagNames·memoryLine·memoryBlock·memorySection·oldDiarySection·scheduleSearchSection). DB를 열지 않아 memory.ts·context.ts와 관리 대시보드의 태그 검색(tools/db-tag-search.ts)이 같은 코드를 쓴다 — 대시보드는 DB를 읽기 전용으로 따로 열어야 해서(db.ts는 쓰기로 열고 마이그레이션까지 돌린다) 고르는 규칙을 베끼면 화면과 프롬프트가 갈라진다. 옛 이름은 memory.ts가 다시 내보낸다
                ├─ src/thresholds.ts — 「고정된 기준값」 표와 1:1로 맞춘 상수 모음(답장 텀·선톡·하루 경계·프롬프트 개수 상한). 참조하는 곳은 memory.ts·context.ts·life-plan.ts·day-plan.ts와 선톡 네 모듈(proactive-policy·dispatch·presence·followup), 나머지 모듈 리터럴 통합은 그 모듈을 다시 쓰는 세션 몫
                ├─ src/labels.ts — 닫힌 목록 값의 영어 식별자 ↔ 한국어 이름 단일 표(답장 여건·활동 성격·저장 항목·주인·출처·유저 인지). plan_json 안 두 태그는 DB CHECK가 못 닿아 3중 방어(읽는 자리 toResponsiveness/toActivityCategory + 생성 직후 normalize + v3 마이그레이션 migratePlanTags)
                ├─ src/day-plan.ts — 하루 각본: 밤 정리가 새 날 것을 사전 생성(대화가 먼저면 lazy). 담는 하루는 논리일 05:00~28:59, 자정을 넘긴 시각은 24를 더해 적고(00:30=24:30) 사람이 읽는 자리에서 clockLabel이 되돌린다. 각본에 그 시각 블록이 없으면 context.ts의 sleepGap이 잠 블록으로 메운다(자정 이후만). 그날 컨디션 시드(월 리듬)를 읽어 컨디션→기상→활동 연결, 어제 일기 실제 여파가 시드보다 우선. 블록별 **답장 여건(instant/intermittent/unavailable) × 활동 성격(personal/social/official)** — 두 축은 직교. 저장은 영어 식별자, 한국어 이름은 src/labels.ts 한곳(RESPONSIVENESS_NAME·ACTIVITY_CATEGORY_NAME). category는 옵셔널, 없으면 activity로 추론(blockCategory: 회의·시험·발표·업무=공적 / 친구·가족·병원·학원·회식=사회 / 나머지 솔로=개인)
                ├─ src/life-plan.ts — 월 리듬(중간 지평): 한 달치 이벤트 + 매일 컨디션 시드를 미리 생성(ensureMonthPlan), 항상 ~1개월 런웨이 롤링(ensureRhythmRunway). 이벤트↔컨디션 인과(회식 다음날 피곤 등), 급변 없는 파도. Opus, 밤 정리 배치가 생성
                ├─ src/nightly.ts — 밤 정리(새벽 5시 컷오프): gather/apply 분리. 일기를 저장할 때 기억과 같은 어휘의 주제 태그(DIARY_TAG_MAX=8)를 함께 넣어 지난 일기도 태그로 걸린다 — 생성 프롬프트에 [이미 쓰는 태그] 목록 주입. 기본=외부 스케줄러가 tools/nightly-read·write로 수행, 폴백=봇 내 05:40 크론(API·opus). 월 리듬(rhythmNeeded 신호→rhythm 출력)도 이 배치가 생성, 아크는 달력 경계(월요일=주·1일=달)에서 기존 흐름+최근 일기로 이어쓰기 갱신. 침묵 quiet/dormant면 일기·시드·리듬만(각본·선톡 생략), reconnect면 저녁 재연결 문안만 — gather가 silenceTier/silenceDays를 노출하고 apply가 게이트를 강제(외부 경로가 백오프를 몰라도 안전)
                ├─ src/proactive-policy.ts — 선제 발화 관제탑: silenceState(무응답 논리일 수→normal/quiet/checkin/dormant)·proactiveAllowed·**dailySendPlan(그날 무엇을 보낼지 단일 판단: morning|lunch|checkin|none — 새벽 정리 문안 준비·반영 게이트·발송 직전 재확인이 같은 함수를 부른다)**. 유저 메시지가 오면 즉시 평상 복귀
                ├─ src/dispatch.ts — 아침·점심·안부 디스패처(3분 틱, LLM 0): 밤에 준비된 문안을 창 안 발송, 유저가 최근 4시간 안에 먼저 연락했으면 skip, dailySendPlan이 그날 보낼 종류로 지목하지 않으면 skip. 점심 선톡은 발송 창만 12:05~12:50인 morning 종류 행(스키마 불변, 트레이스는 purpose='lunch'). 창 놓치면 유예(창 종료 +90분, 시간대별 상한 11/14/22시) 내 발송, 넘기면 폐기 사유+시도 횟수(attempts/last_error) 기록
                ├─ src/followup.ts — 침묵 팔로업(15분 틱, 크론 0-4,8-23시): 관제탑 통과 시에만. 낮=유저 발화 기준 4h 무응답이면 근황 하루 1통(보낸 뒤에도 답이 없으면 그날은 물러남), 밤(자정~새벽 5시)=유저가 잔다 말 없이 1h+ 잠수하면 밤 인사 1회. 답장이 유저의 서운함을 표시했는데(reply-signal의 userUpset) 그 뒤로 답이 끊기면 30분 뒤 달래기 1통 — 한 발현에 한 통이고 잠 블록에도 나간다. 문안은 대화와 같은 3층(buildSystemBlocks)+상황 문단 — 앞 두 층 캐시 공유. 경과시간은 Date.now 기준(getKstNow().getTime()은 +9h라 오산)
                ├─ src/presence.ts — 자리 비움 예고(10분 틱, 크론 7-23시, 유저 발화 ≤4h일 때만 — 자기 발화로 창이 열리던 체인 차단): 긴 불가(운동·샤워·외출, ≥30분)를 PlanBlock.advance_known으로 갈라 미리 아는 일정은 시작 10분 전~3분 후에 예고, 닥친 일은 시작 시점~12분 후에 알리고 바로 조용, 연속 불가 사이 경계는 2분 전~12분 후(블록당 1회, 하루 3회 상한=예고만 계산·복귀 인사 제외, 하루 각본 생성 프롬프트도 같은 상한으로 불가 구간 수를 제한). 선톡 하루 총량 캡에서는 제외. 공적 불가(회의·시험)는 "끝나고 연락" 톤. 다녀온 뒤는 이 파일 몫이 아니다 — 구간 끝의 wake 핸들러(bot.ts)가 몰아 답장과 복귀 인사를 한 자리에서 처리한다. **개인·사회 불가는 유저가 붙잡으면 reply-timing이 판정 한 콜로 가려 개인=취소·사회=미룸을 day_actuals에 기록→그 블록 즉답, 공적은 못 접음.** 접은 블록에는 예고 스킵(isHeldNow). 캐릭터가 방금 말했으면 예고를 접는다 — 기준은 최소 AWAY_QUIET_MIN분이되 알릴 일정이 이미 시작했으면 그 시작 시각까지 넓힌다(구간 끝 몰아 답장이 이미 같은 전환을 알린 뒤라, 그 답장 자리에서 다음 일정까지 함께 알리고 예고는 나가지 않는다). 문안은 대화와 같은 3층(buildSystemBlocks)+상황 문단 — 앞 두 층 캐시 공유. 막연한 침묵→'알고 하는 기다림'
                ├─ src/reply-trace.ts — 답장 트레이스(1분 틱, LLM 0): llm_calls·prompt_blobs를 읽어 게시함에 쌓는다. 본문=대상 발화·텀 경로·검색 태그와 기억·모델이 쓴 답, 스레드=실시간 꼬리 전문(발송·폐기 결과는 pending.ts가 traceReplyOutcome로 같은 스레드에 붙인다). 하루 고정 두 덩이는 그날 첫 호출에서 한 번만, 하루 중에 바뀌면 바뀐 대목 이름(changedSections)과 바뀐 줄(lineDiff)만. 판단 근거를 5분 기다리고 3시간 지난 호출은 표시만(게시 안 함), 새벽 정리 쪽 purpose는 구간 3 몫이라 건너뛴다. 오늘 몫을 지워 다시 보내는 도구는 src/tools/retrace.ts
                ├─ src/nightly-trace.ts — 새벽 정리 트레이스(LLM 0): applyNightlyOutput 래퍼가 반영 전에 손댈 키의 지금 값을, 반영 뒤에 결과를 읽어 게시함에 쌓는다(트랜잭션 바깥이라 게시 실패가 반영을 되돌리지 않는다). 본문=오늘 메모·각본과 달라진 하루·관계 갱신·새 일정, 스레드=기억 신규와 덮어쓰기(이전→새 값·태그 차이)·일기 전문과 그날 일기 태그(없으면 '없음')·내일 선톡 문안과 발송 창·그 밤의 모델 호출 원문(2시간 안, 올린 뒤 traced 표시). 침묵 단계로 일부만 만든 날은 건너뛴 것을 한 줄, skip으로 끝난 날은 미게시
                ├─ src/feedback.ts — 슬랙 채널에 남긴 표시 수집(10분 틱, LLM 0): conversations.history로 최근 3일 글의 리액션을, reply_count가 있는 글은 conversations.replies로 스레드 답글을 읽어 call_feedback에 쌓는다. 리액션 4종이 분류(❌ 사실 오류·💬 말투·⏰ 타이밍·👍 좋음, toFeedbackKind), 스레드 답글이 이유. 표시가 달린 글의 slack_ts로 trace_events를 찾아 dedupe_key `call:<id>`(스레드 자식은 `call:<id>:<꼬리>`)에서 호출 번호를 꺼내 함께 적고, 호출과 이어지지 않는 게시에 달린 표시는 번호 없이 둔다. 리액션을 뗀 것은 지우지 않고 removed_at 표시(다시 붙이면 되살림), 같은 표시는 dedupe_key UNIQUE로 한 번만. 답장 파이프라인에 갈고리를 걸지 않고 게시 기록을 읽어 따로 돈다 — 수집한 표시는 프롬프트에도 기억에도 들어가지 않는다(ADR 0009). SLACK_BOT_TOKEN·SLACK_TRACE_CHANNEL 없으면 전체 no-op
                └─ src/trace.ts — 슬랙 트레이스 게시(1분 틱, LLM 0): 파이프라인이 recordTraceEvent로 trace_events에 쌓은 기록을 runTraceTick이 슬랙 chat.postMessage(네이티브 fetch, SDK 없음)로 발송. dedupe_key UNIQUE + INSERT OR IGNORE 중복 방지, thread_key/parent_key 스레드(부모 게시 전 자식은 다음 틱으로 미룸, 부모 영구 실패 시 자식 skipped), 영구 오류(invalid_auth·not_in_channel 등)는 즉시 failed·나머지 3회 재시도. 아침 각본 알림은 이 틱이 7시부터 직접 enqueue(본문=시드+블록별 두 태그·답장 텀 범위, 스레드=시간대별 정리+생성 프롬프트 재조립, ondemand→nightly 교체 구분). SLACK_BOT_TOKEN·SLACK_TRACE_CHANNEL 없으면 전체 no-op — 표 정의는 `src/db.ts`의 `TABLES` 한곳, 지금 `SCHEMA_VERSION` 5. 표·컬럼의 뜻과 관계는 erd.md, 모듈 배치는 modules.md가 갖는다
[모델] 실시간 대화=sonnet / 일기·추출·각본·월 리듬·아크·캐릭터 생성=opus(MODEL_DEEP) / 밤 정리 기본 경로는 외부 스케줄러라 API 미사용
```

핵심 설계 불변: 캐릭터는 유저 입력으로 생성(V2)하고 생성 때 정한 큰 정체성은 불변(origin=creation 행은 저장 함수가 수정 거부) — 자잘한 디테일은 대화로 쌓이되(누적 정체성) 한번 나온 것은 일관 유지(어긋나는 발명·유저 훼이크 추종 금지) / 잘 통하는 것은 관계 컬럼(rapport)으로 캐릭터에게 공개하고, user_preferences는 유저 단위 선호 자리 / 선제 연락은 근거 있을 때만 하루 1통, 유저가 오래 무응답이면 물러난다(3일 조용→14일차 저녁 재연결 1통→침묵) / 이별은 비가역 / 컨디션은 매일 독립 주사위가 아니라 월 단위로 미리 깔린 리듬(이벤트 인과+파도), 실제 산 하루가 그 위를 덮어씀 / **답장 텀은 블록의 두 태그(답장 여건 × 활동 성격) 표 한 장에서만 나온다 — 이벤트별 예외처리 금지(스파게티 방지). 개인=붙잡으면 취소, 사회=양해 구해 미룸, 공적=못 접음.** / 답장은 즉답·틈틈이면 텀을 정한 뒤 바로 만들어 정한 시각에 내보내고, 불가 구간이면 만들지 않고 구간 끝에 깨어나 쌓인 메시지를 한 번에 읽고 몰아 답한다(몰아 답장은 만드는 시점=보내는 시점이라 일정을 막 끝내고 이제 봤다는 전제가 사실이 된다)
