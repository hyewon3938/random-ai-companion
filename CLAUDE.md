# random-ai-companion — 작업 가이드

나와 같은 시간을 살고 먼저 물어봐주는 AI 대화 상대 (만남은 카드 한 장으로 시작). 컨셉·가설·설계 총론은 [README.md](README.md), 규모와 비용 검토는 [scaling.md](scaling.md), 실행 환경과 외부 서비스 구성은 [infra.md](infra.md)가 단일 소스다.

세부 설계 문서는 `docs/` 아래에 두고 커밋하지 않는다(gitignored). 타겟·어필 포인트는 `positioning.md`, 설계 논리·층은 `architecture.md`, 캐릭터 속성 체계는 `character-design.md`, 수집·생성 데이터 카탈로그는 `data-model.md`. 로컬 전용 컨텍스트는 `LOCAL-CONTEXT.md` — 새 세션은 그 파일부터 읽을 것.

## 상태 (갱신 의무 — 세션이 끝나기 전 여기와 LOCAL-CONTEXT.md를 최신화한다)

- [x] 설계 문서 (README, character-design)
- [x] 스캐폴딩 + 로컬 기동 확인 + **클라우드 VM 배포·가동** (07-06. 재배포 = 로컬에서 동기화 후 `docker compose up -d --build`)
- [ ] 유저 프로필 + **카드 온보딩** (/start: 프로필 등록 → 카드 제시 → [대화 시작]/[다음 카드] 인라인 버튼, 패스=무비용 재생성 + card_reactions 기록)
- [x] **밤 정리(구 밤 일기) — 이중 경로 확정** (07-11): 기본 = **외부 스케줄러(매일 새벽 KST, API 비용 없음)** — `src/tools/nightly-read.ts`(수집)→자체 생성→`nightly-write.ts`(적용). 폴백 = 봇 내 05:40 크론(API, opus) — 일기 중복 체크로 이중 실행 방지. 산출 = 일기 응고(각본 vs 실제·상대 감정·내부 온도) + 유저기억·누적정체성·일정·인물 추출 + 새 날 각본 + 선톡 문안
- [x] **선톡 = 밤 정리(문안 준비) + 발송 디스패처(3분 틱, LLM 콜 없음)** — scheduled_sends 하루 1통, 근거 있을 때만, 유저가 그날 먼저 연락했으면 미발송. 무응답 시 익일 후속은 밤 정리 판단에 위임 (07-11). 창 놓치면 유예 발송(창 종료 +90분·시간대 상한) 후 폐기 사유·시도 횟수 기록, 텔레그램 연결 보온으로 발송 실패 자체를 예방 (08-06)
- [x] **설계 감사 개선 (08-06)** — 밤 정리 원자화(트랜잭션)+결번 백필, lazy 각본을 정식 각본이 교체(day_plans.source), 선톡 틱 3종 상호 배제+발송 직전 재확인, '하루' 정의 통일(새벽 5시 논리일), 선제 발송 총량 캡 공유, state_json 파싱 가드, 컨텍스트 주입 상한, followup·presence 실패 영속 기록(send_failures). 나머지 항목은 이슈 #1
- [x] **선제 발화 관제탑 + 침묵 백오프 (08-07, 이슈 #2)** — src/proactive-policy.ts가 "먼저 말 걸어도 되는가"의 단일 판단: 무응답 0~2일=평상 / 3~13일=전면 조용(문안 생성도 스킵, 밤 정리는 일기·시드·리듬만) / 14일차=저녁 재연결 1통(kind=reconnect) / 이후=유저 복귀까지 침묵. presence '최근 대화' 판정을 유저 발화 기준으로 수정(발송이 발송을 부르던 체인 차단). 구조 정리 ②~④는 이슈 #3
- [x] **구조 정리 ② (08-07, 이슈 #3)** — 시스템 프롬프트를 안정도 순 3층(불변=바이블·규칙 / 일간=관계·인물·아크·일정·일기 / 실시간 꼬리=오늘 각본·시각·말투)으로 재배열 + 앞 두 층에 cache_control(1h). 관계 상태는 JSON 통주입(9.8K자, 프롬프트의 42%) → 압축 서술로. 실측: 프롬프트 23.5K→17.2K자, 캐시 히트 시 16K토큰이 0.1× 과금(입력비 ~85%↓) — llm 로그의 cw/cr로 관측
- [x] **구조 정리 ③~④ (08-07, 이슈 #3)** — ③ 프롬프트 규칙 10블록→성격별 4블록(원문 보존·중복만 제거), presence 20분 미만 간헐 예고 제거, capture 10→15턴, 아크 달력 경계 갱신(월요일=주, 1일=달·분기 계절·연초 올해 — 기존 흐름·최근 일기 이어쓰기, 침묵 중 생략) / ④ README를 실측 데이터(침묵일 선톡·캐시 히트·프롬프트 크기) 기반으로 갱신
- [x] **대화 기록 시간 마커 (08-18, 이슈 #5·#7)** — 날짜 없는 대화 기록이 전부 '오늘'로 수렴해 며칠 전 이야기를 방금 일처럼 받던 문제 수정. 기록의 첫 발화·논리일이 바뀌는 지점·앞 발화와 1시간 이상 벌어진 지점에 `[어제 22:10]` 같은 마커를 코드가 계산해 붙이고(`timeMarkerFor`), 실시간 꼬리에 직전 대화 시점 한 줄을 주입한다. 마커는 messages에만 들어가 프롬프트 캐시 경계는 그대로. 이어서 답장 앞머리 태그 파싱을 `parseReplyTags` 한 곳으로 통합 — 태그가 겹쳐도 [남음] 신호를 놓치지 않는다
- [x] **말투 자연화 — 말의 결 규칙 (08-19, 이슈 #9)** — 모델 기본 버릇(직전 발화 명사구 되받기·문어체 은유·상담사식 감정 라벨링 공감)을 억제하는 `SPEECH_TEXTURE` 블록을 공통 규칙층(불변층)에 신설, buildSystemBlocks를 타지 않는 선제 발화 문안 프롬프트 6곳(followup·presence·nightly)에는 압축판 `SPEECH_TEXTURE_COMPACT` 주입. 사색적 취향은 유지하고 표현만 입말로(내용/표현 분리), 무심함 회귀 방지 가드 포함 — humanizing-log #18. **후속 보정(같은 날, 이슈 #11)**: 물음표 보정 판정 보수화(의문사 위치·굳은 꼴·어미 가드 — `src/bubble-polish.ts`로 분리, 단독 회귀 검증 가능), 쉼표 억제(규칙+한글 사이 쉼표 제거 방어선), 감성 과잉 멘트 금지 규칙 추가 — humanizing-log #19. **표기 규칙 통합(08-27, 이슈 #32)**: 이모지 금지가 선톡 문안 6곳에 손으로 적혀 있고 대화 쪽은 캐릭터 정체성 값(`voice.emoji_level`)에 기대던 것을 공통 규칙층으로 올렸다 — `OUTPUT_FORMAT`(이모지·큰따옴표·리스트/마크다운) 한 블록을 규칙층에 넣고 선톡 문안에는 `OUTPUT_FORMAT_COMPACT`를 주입, 캐릭터 생성 스키마에서 `emoji_level` 삭제, 발송 직전 `stripEmoji`가 새어 나온 이모지를 지운다(글자로 쓰는 기호·웃음 자음은 보존)
- [ ] ~~교체 플로우~~ → **현 범위에서는 미구현 결정**(첫 캐릭터 정착 시나리오). 설계는 로컬 architecture.md의 이별과 교체 절에 문서화 완료 (07-11)
- [x] 분할 발화·미러링·대화 리듬 (디바운스 15~30s·프레즌스 지연·타이핑 텀·잠/기상 — humanizing-log 참조). 반말 전환은 우진 바이블에 반영(성향 다양화 `banmal_style`은 랜덤 생성 복원 시)
- [ ] **프롬프트·기억 구조 재설계** (08-19 착수) — 프롬프트 조립과 기억의 write/read/update 정책을 다시 설계한다. 설계 원본은 repo 루트 `time-and-memory.md`, 설계 허브는 `docs/data-map.html`(gitignored). 구현은 세션 하나에 단계 하나로 나누고 세션마다 이슈와 브랜치를 하나씩 둔다 — 범위·참조 절·모델은 time-and-memory.md 「작업 세션 나누기」 표에 있고, **새 세션은 그 표의 몇 번 줄인지 지정받아야 한다**(지정 없이 문서만 읽으면 여러 단계를 한꺼번에 손댄다). **9번 세션까지 끝났고 다음은 10번 — 새 구조로 읽은 대화 확인(Fable)**, 그 사이에 새벽 정리가 몇 번은 돌아야 한다. 날짜별 설계 기록과 1~9번 세션 결과는 time-and-memory.md 「날짜별 설계 기록」·「세션별 결과」로 옮겼다.
- [ ] **V2 — 유저 생성 캐릭터·기억 통합 (확정 08-27, 이슈 #28)**: 랜덤 매칭을 없애고 유저가 선택지와 서술형 입력으로 캐릭터를 만든다. 기억은 `memory_items` 세 항목(fact·ongoing·person) × 주인 둘, 관계는 `relationships` 컬럼 일곱 항목. 설계 원본은 time-and-memory.md 「V2」 절과 ADR 0002~0004, 저장 구조는 erd.md, 모듈 배치는 modules.md. 확정 뒤 덧붙인 세부(생성 호출 두 번·전용 컬럼 여섯·테이블 개명·구현 세션 재편·프롬프트 고유값 제거)는 time-and-memory.md 「확정 뒤에 덧붙인 것」으로 옮겼다.
- [ ] ~~케미 축 단계 값 재점검~~ → ~~MBTI 우선으로 확정(08-21)~~ → **V2 유저 입력 생성으로 대체(08-27, 위 V2 항목)**: 코드가 정하는 건 성별(유저 반대)·나이 범위(유저 ±4살, 성인 가드)·MBTI(16종 무작위)·애착유형(안정형 고정) 넷뿐이고, 대화 성격은 MBTI에 맞춰 생성 콜이 채운다. 선호 학습 대상도 케미 축(243조합)에서 MBTI로 이동. character-design §2·§7은 대체 표기. 이어서 확정: 성별=유저 반대 / 나이=유저 ±4살(성인 가드) / MBTI는 16종 안 추림, 애착유형과 묶지 않고 생성 콜이 한 사람으로 빚음 / 말투는 성격에서 도출하되 값으로 저장 / 취향→취미 / **뼈대는 세 층에서 두 층으로**(자리만 만들어두는 층 폐지, 학번·유년 장면 자리 삭제 — 나이에서 계산되는 값은 생년월일에서 계산해 말한다는 규칙으로 대체)
- [ ] 프로브 플래그 (교체 제안 / 침묵일)
- [ ] **적응형 답장 속도**(유저 리듬 맞춤·향후): 유저의 요일×시간대별 답장 속도 학습 + 세션 템포 반영 → '빠른 경로'(즉답·개인 틈틈이·진입창)의 대기 구간을 빠른 쪽으로 당김(상한만↓·하한 유지·당김폭 캡). **이벤트가 상한 — 불가·공적 불변.** 빠른 경로에만 곱하는 단일 boost 계수(이벤트 분기와 독립). 설계는 architecture.md·positioning.md#7에 기록. 데이터 쌓이면 구현
- [x] 분석 스크립트 — `src/tools/analyze.ts` 일별 신호 집계(선제시작·활동시간대·감정어·선톡 반응) + `--tag` LLM 정밀 태깅 (07-11)
- [x] 데모 백스테이지 뷰 — `src/tools/render-map.ts` 실DB→HTML(관계도·기억·온도·일기). **산출물은 사생활 데이터 포함, 커밋 금지** (07-11)
- [x] **관리 대시보드 — DB 구조 그대로 보는 화면 (08-29, 이슈 #93, 브랜치 refactor/93-db-structure-dashboard)** — 하루 전 만든 캐릭터 생성 화면(이슈 #90, `src/tools/render-character.ts`)은 값을 의미별로 다시 묶어 카드로 보여줘서 어느 표에 무엇이 들어 있는지가 오히려 가려졌고, 데이터를 관리하기에도 불편해 갈아엎었다. 새 화면 `src/tools/render-db.ts`는 표 하나가 한 구획이고 컬럼은 `PRAGMA table_info` 그대로, 행은 전부 싣고 빈 표도 감추지 않는다 — 비어 있다는 사실 자체가 정보라서(오늘 메모·각본과 달라진 기록이 지금 0행). 표 26개 2,457행이 자립형 HTML 한 장에 들어간다. 태그는 별도 표에 있어서 `memory_items` 마지막 열에만 조인해 붙이고, 태그를 누르면 그 태그로 검색한다. 검색·정렬·펼치기는 만들어진 화면 안에서 돌아가 다시 실행할 필요가 없다. **배포는 필요 없다** — `config.dbPath`가 `DB_PATH`를 읽으므로 운영 DB 사본을 로컬에 두고 그린다(사본은 컨테이너 안에서 `db.backup()`으로 뜬 `data/prod-snapshot.db`, 실행은 `DB_PATH=data/prod-snapshot.db npx tsx src/tools/render-db.ts > docs/dashboard.html`). 사본과 산출물 모두 실제 대화가 담겨 gitignored. #90에서 인물·진행 카드가 비어 보이던 이유도 여기서 드러난다 — `origin='creation'`은 사실 23건뿐이고 주변 인물 13·진행 중인 일 4는 이관 때 `saveMemory`를 타서 대화 쪽 행으로 들어갔다(`saveMemory`는 설계상 대화 쪽에만 쓴다). 값 수정은 넣지 않았고 읽기 전용이다. **옛 자리·안 쓰는 자리 표시 (08-29, 이슈 #97, 브랜치 feat/97-dashboard-legacy-marks)** — 저장 구조를 다시 짜면서 대신할 자리가 생겼는데 아직 지우지 않은 표가 섞여 있어 화면만으로는 지금 쓰는 데이터인지 구분할 수 없어서, `render-db.ts` 안 `NOTES` 표에 손으로 적고 표 머리·컬럼 머리글·왼쪽 목록 세 곳에 배지와 점으로 표시한다. 옛 자리 5곳(`cast_members`→`memory_items` person 행 / `attention_override`→`day_actuals` / `capture_marks`→`today_notes` / `relationships.legacy_state_json`→관계 일곱 항목 컬럼 / `user_profile.age_band`→`birth_year`, 다만 이 컬럼만 아직 프롬프트가 읽는다), 안 쓰는 자리 6곳(`user_preferences` + `user_profile`의 `preferred_name`·`gender`·`birth_year`·`job`·`region`). **함께 확인된 공백**: `user_profile`은 런타임에서 한 번도 저장되지 않는다 — 지금 1행은 손으로 넣은 값이다. 다섯 컬럼은 두 갈래다. 부르는 이름·성별·출생연도는 가입할 때 받기로 했는데 가입 절차가 없어서 넣는 코드도 없고, 하는 일·사는 지역은 대화에서 알게 되면 새벽 정리가 채우기로 한 값인데 8번 세션에서 새벽 정리를 V2로 다시 쓸 때 학습 출력이 빠져 채우는 코드가 없다 — 남은 세션 셋 어디에도 없어서 이슈 #106으로 세웠다. 대시보드 컬럼 설명도 두 갈래를 갈라 적는다. 자동 판정을 넣지 않은 이유는 기준이 "런타임에서 부르는 곳이 있는가"라 정의만 남은 코드와 구분되지 않아서고, 옛 경로를 지우는 11번 세션에서 이 표도 함께 줄인다. **새로고침으로 다시 그리기 (08-29, 이슈 #127, 브랜치 feat/127-dashboard-live-view)** — 화면을 최신 상태로 보려면 운영 DB 사본을 로컬로 가져오고 렌더 스크립트를 돌리는 두 단계를 손으로 쳐야 했고, 손이 가는 쪽은 사본이라 화면만 다시 그려도 같은 데이터를 다시 볼 뿐이었다. 그리는 코드를 `src/tools/db-view.ts`의 `renderDbHtml(file)` 하나로 옮기고 부르는 곳을 둘로 뒀다. `render-db.ts`는 전처럼 파일로 뽑고(인자를 주면 그 경로에 쓰고, 없으면 그대로 내보내 기존 사용법이 그대로 동작한다), 새로 만든 `serve-db.ts`는 요청마다 DB를 다시 읽어 응답으로 내보내서 브라우저 새로고침이 곧 갱신이 된다. 화면 코드가 한 벌이라 두 경로가 갈라지지 않는다. **접근 경로는 좁게 고정했다** — 바인드 주소는 `127.0.0.1`이고, Host 헤더가 루프백 이름이 아니면 403으로 막아 다른 사이트가 브라우저를 통해 화면을 읽어 가지 못한다. 받는 것은 GET 하나뿐이고(POST는 405, 없는 주소는 404) DB는 읽기 전용으로 열며 쓰는 길을 두지 않았다. 파비콘 요청은 204로 돌려보내 새로고침 한 번에 DB를 두 번 읽지 않게 했고, 응답에 `cache-control: no-store`를 붙여 이전 응답이 남지 않게 했다. 원격 DB를 볼 때도 포트를 공개하지 않고 `ssh -L 8787:127.0.0.1:8787`로 이어서 본다. 실행은 `yarn dashboard`(파일로 뽑기)·`yarn dashboard:serve`(서버). 검증 = 타입체크 + 바뀌기 전 코드와 출력 대조(20,986줄로 같고 공백을 무시한 diff 0줄, 늘어난 624바이트는 함수 안으로 들어가며 붙은 들여쓰기뿐) + 스모크 10건(GET 200·Host 403·POST 405·없는 주소 404·파비콘 204·응답 헤더 3종·루프백에만 열림·LAN 주소로는 연결 거부·행을 넣고 새로고침하니 2에서 3으로 바뀜·파일 인자). **VM 배포는 아직이다** — 원격에서 쓰려면 코드가 그쪽에 있어야 한다. **컨테이너 안에서 띄우기 (08-29, 이슈 #133, 브랜치 feat/133-dashboard-container-bind)** — 원격 DB를 호스트에서 그대로 띄운다고 적어 뒀던 것을 바로잡았다. 그 경로는 두 가지 이유로 성립하지 않는다. 호스트에 node는 있지만 프로젝트 의존성이 컨테이너 이미지 안에서만 설치되고, DB 파일과 그 디렉터리의 권한이 봇과 같은 계정에 맞춰져 있어 다른 계정으로는 열리지 않는다(WAL 모드 DB는 읽기 전용으로 열 때도 옆의 `-shm` 파일에 쓸 수 있어야 한다). 그래서 봇과 같은 컨테이너 안에서 띄우고 포트는 호스트 루프백에만 공개한다 — `docker compose run --rm -p 127.0.0.1:8787:8787 -e DASHBOARD_HOST=0.0.0.0 app npx tsx src/tools/serve-db.ts`. 바인드 주소를 `DASHBOARD_HOST`로 받되 기본값이 `127.0.0.1`이라 값을 주지 않으면 로컬 동작은 그대로고, 루프백이 아닌 주소로 뜰 때는 포트를 호스트 루프백에만 공개하라는 줄을 시작 로그에 남긴다 — 어디까지 닿는지는 포트를 어떻게 내보냈느냐가 정한다. `docker-compose.yml`은 고치지 않았다. 볼 때만 일회성 컨테이너로 띄우고 끄므로 상시 공개하는 포트를 만들지 않는다. SSH 터널을 거쳐 오는 요청의 Host는 `localhost`라 기존 검사를 그대로 통과한다. 검증 = 타입체크 + 스모크 18건(기본값은 루프백에만 열리고 LAN 주소는 연결 거부·터널 안내 줄·`::1`은 루프백이라 경고 없이 IPv6로만 열림·`0.0.0.0`이면 경고와 공개 방법 안내·Host 위조 403·행을 넣고 새로고침하니 1에서 2로 바뀜)
- [x] **슬랙 트레이스 채널 (08-28, 전체 트랙 이슈 #63 — 구간 셋 전부 완료)** — 캐릭터 파이프라인의 판단 근거를 슬랙 채널에 게시해 개선 대상을 눈으로 확인한다. 게시용 모델 호출은 없고 전부 DB 값과 코드 계산이다. 모듈 구성과 outbox·내용 해시 방식은 modules.md 「기록 경로」, 호출 원문을 남기기로 한 근거는 ADR 0006, 구간별 결정과 검증 기록은 이슈 #58·#61·#62·#65·#105에 있다. **운영에서 챙길 것 둘** — `SLACK_BOT_TOKEN`·`SLACK_TRACE_CHANNEL`이 둘 다 있어야 켜지고 하나라도 없으면 전체 no-op이라 배포는 값 없이도 안전하다. VM `.env`의 키 이름은 `SLACK_TRACE_CHANNEL`이고, 옛 이름 `SLACK_VIZ_CHANNEL`이 남아 있으면 게시가 조용히 전체 no-op이 된다. 보존 기간은 `trace_events` 30일·`prompt_blobs` 본문 90일, 게시량은 가장 많던 날 기준 하루 약 95행이다.
- [x] **답장 생성 시점 재설계 (08-28, 이슈 #70, 브랜치 refactor/70-reply-generation-timing, PR #74)** — 불가 구간의 몰아 답장과 자리비움 예고를 갈랐다. 즉답·틈틈이는 기존 흐름(텀 결정 → 즉시 생성 → 대기 → 발송) 그대로, 답장 불가 구간에 온 메시지는 답장을 만들지 않고 깨우기 표시(pending_replies kind='wake', meta_json에 활동·구간)만 걸어 구간이 끝날 때 깨어난다 — 쌓인 메시지가 있으면 그때 프롬프트를 조립해 한 번에 답하고(만드는 시점=보내는 시점이라 일정을 막 끝내고 이제 봤다는 전제가 사실이 된다), 없어도 예고하고 나간 자리면 복귀 인사를 보낸다. presence.ts의 복귀 경로는 삭제 — 구간 끝을 챙기는 자리는 bot.ts의 wake 핸들러 하나다. 자리비움 예고는 PlanBlock.advance_known으로 가른다 — 미리 아는 일정은 시작 10분 전~3분 후(기존 15분 전)에 예고하고 예고 뒤 시작 전까지 온 말은 배웅 답(farewellSituation, 곧 나간다는 걸 아는 채 짧게 받음), 닥친 일은 시작 시점~12분 후에 알리고 바로 조용, 연속 불가 사이 경계는 2분 전~12분 후. 붙잡기 판정(askHold)은 유지. sendDelayLine(대기 90초 넘으면 도착 예정 시각을 꼬리에 적던 줄)은 삭제 — 긴 대기가 몰아 답장으로 바뀌어 쓸 자리가 없다. pending_replies의 wake 지원(kind CHECK의 'wake'·meta_json 컬럼)은 상태 감지 이관이라 버전을 쓰지 않고, #65의 v5 이관과 어느 순서로도 공존한다. 검증 = 타입체크 + 스크래치 DB 스모크 26건(옛 스키마 이관·wake 행이 추가 발화에도 유지·깨우기 3분기·재기동 멱등·새 DB 생성). askHold 실응답과 예고 분기 실동작은 운영 관찰 몫
- [x] **틈틈이 블록에서 대화를 닫지 않도록 (08-28, 이슈 #79, 브랜치 fix/79-intermittent-keep-open, PR #80)** — 답장 여건이 '틈틈이'인 블록에서 캐릭터가 답장은 잘 하면서도 끝나고 연락하겠다며 대화를 닫던 것을 고쳤다. 프롬프트 세 자리가 답장 여건을 안 보고 있었다. ① 붙잡기 규칙(CATEGORY_RULE)이 활동 성격만 보고 걸려서 공적 항목의 "접을 수 없다, 끝나고 연락한다"가 틈틈이 블록까지 끌려왔다 — 규칙 전체를 '불가'일 때만 적용하도록 한정하고, 즉답·틈틈이면 이미 답하는 중이라 접을 것이 없으니 건너뛰라는 줄을 앞에 뒀다. ② 실시간 꼬리가 여건 이름만 적어 줘서 모델이 같은 줄의 '공적'을 보고 마무리 인사를 만들었다 — 이름 옆에 행동 정의를 붙였다(틈틈이=하던 일을 하면서 사이사이 답하니 대화는 그대로 이어간다, 즉답=손이 자유로우니 네가 먼저 닫지 않는다). '불가'에는 줄을 두지 않는다 — 그 구간에 답을 쓰는 세 경우(붙잡혀 접었을 때·구간 끝 몰아 답장·자리 비움 예고)는 붙잡기 규칙이나 상황 문단이 이미 정해 준다. ③ 자리 비움 예고 서사(PRESENCE_NARRATION)에 알리는 시점 조건을 넣어, 즉답·틈틈이 구간에서 이따 바빠진다며 미리 대화를 정리하지 않도록 했다(예고 시점은 presence.ts가 정한다). 검증 = 타입체크 + 스크래치 DB 스모크 20건(여건별 꼬리 줄 유무·붙잡기 규칙 한정·예고 조건) + 배포 후 운영 프롬프트 재조립 확인. 배포하며 #70 동작도 처음 관측됐다 — 18:00에 자리 비움 예고(미리 아는 18:10 일정, 10분 전), 불가 구간 진입 후 깨우기 행(kind='wake', 18:51 발송 예정), 붙잡기 판정 호출(purpose='hold') 실행
- [x] **선톡 발송 조건을 설계 표에 맞추기 (08-29, 이슈 #88, 브랜치 fix/88-proactive-schedule, PR #94)** — 선톡 다섯 종류의 발송 조건이 모듈마다 따로 적혀 있어 설계 표와 어긋나 있던 것을 맞췄다. ① 무응답 일수별 분기를 `proactive-policy.ts`의 `dailySendPlan` 한 함수로 모았다(어제 대화함·1일째=아침 / 2일째=점심 한 통 / 3~13일째=없음, 유저 일정이 있는 날만 아침 / 14일째=저녁 안부 / 15일째부터=없음) — 새벽 정리의 문안 준비·반영 게이트·발송 직전 재확인이 같은 판단을 쓰고, 봇 밖 스케줄러도 같은 분기를 탈 수 있게 수집 결과에 `sendPlan`·`sendPlanReason`을 실었다(**스케줄러 지시서는 repo 밖이라 커밋 대상이 아니어서 같은 분기·필드로 따로 고쳤다**). 점심 선톡은 `scheduled_messages.kind`를 늘리지 않고 발송 창만 12:05~12:50인 `morning` 행으로 저장해 마이그레이션 없이 붙였고, 트레이스는 `purpose='lunch'`로 가른다 ② 근황 선톡은 해시로 100~139분을 흩고 연속 무응답이면 간격을 늘리던 조건을 걷어내고 마지막 유저 발화 기준 4시간 무응답 하나로, 하루 한 통 ③ 밤 인사 선톡 시간대를 새벽 2~5시 → 자정~새벽 5시, 1시간 무응답 판정도 유저 발화 기준 ④ **자리비움 선톡은 하루 3회**(예고만 계산, 복귀 인사 제외) — 실행 중에만 걸면 각본의 예고가 소리 없이 사라지므로 하루 각본을 만들 때부터 20분 넘는 답장 불가 구간을 3개까지만 두도록 생성 프롬프트에 규칙을 넣었다. 자리비움은 선톡 하루 총량 캡에서 제외 ⑤ 기준값 정리 — `PROACTIVE_DAILY_MAX` 중복 선언을 `thresholds.ts` 한곳으로, 읽는 코드가 없던 `CATCHUP_SILENCE_MS`를 세 자리가 함께 쓰는 `RECENT_USER_MS`로, `GOODNIGHT_WINDOW`·`LUNCH_WINDOW`·`AWAY_DAILY_MAX`를 실제 참조로. 검증 = 타입체크 + 스크래치 DB 스모크 51건(기준값·분기표 9종·발송 카운터 3종·유저 일정 조회·디스패처 게이트 6종·각본 프롬프트 상한 규칙). **배포 완료(08-29)** — 백업 `/home/ubuntu/db-backups/companion-2026-08-29-pre-deploy.db`(호스트·컨테이너 어디에도 `sqlite3` CLI가 없고 WAL이 4MB라 `cp`는 안전하지 않아, 컨테이너 안에서 better-sqlite3의 `db.backup()`으로 떴다) → rsync → `docker compose up -d --build`. **배포는 커밋을 골라 낼 수 없다** — Dockerfile이 `src`를 통째로 COPY하고 rsync는 작업 트리 스냅샷을 그대로 보내므로, 같은 시점의 main에 들어 있던 #108(trace_events 보존 기간)도 이 배포로 함께 나갔다(두 세션이 합의). rsync 제외 목록에 `.claude`를 추가했다 — 다른 세션이 만든 워크트리 사본 94MB가 VM으로 넘어갈 뻔했고, itemize 옵션(`-i`) 없는 dry run은 아무것도 출력하지 않아 그 사실이 보이지 않았다. 확인 = 부팅 로그 정상(마이그레이션 줄 없음, 대기 답장 1건 이어받음), user_version 5·integrity ok·fk 0, `dailySendPlan` 참조 3곳(proactive-policy 1·nightly 6·dispatch 2)·`sendPlanReason` 2곳·`pruneTraceEvents` 3곳(db.ts 1·index.ts 2), `src` 지문 `2465faa1688e`가 main(`cc09369`)의 25개 파일 해시와 일치
- [x] **답장 트레이스에 같은 답장에서 일어난 일 전부 싣기 (08-29, 이슈 #96, 브랜치 feat/96-reply-trace-detail, PR #104)** — 답장 한 건의 트레이스가 텀·검색·다듬기까지만 보여 줘서, 붙잡기 판정이 돌았는지·오늘 메모에 뭘 남겼는지·각본을 접었는지는 DB를 따로 열어야 알 수 있었다. 같은 답장에서 일어난 일을 한 화면에 모았다 — ① *지금 하는 일*을 텀 줄에서 떼어 따로 적는다(전에는 텀 경로 이름 '표' 옆에 일정이 붙어 일정이 곧 표인 것처럼 읽혔다. 경로 이름도 '텀 표'로) ② *붙잡기 판정*은 항상 적는다 — 물었으면 결과와 판정 호출 번호를, 묻지 않았으면 왜 건너뛰었는지(답장 불가 구간이 아니다·공적이라 접을 수 없다·자는 중·이미 접어 둔 일정) ③ 검색은 걸린 태그가 없을 때도 대조한 태그 수를 적어 검색이 돌았다는 것을 보인다(`tagsInText`→`tagSearch`가 pool을 함께 돌려줌) ④ *답장 표시*로 [남음]·[메모] 부착 여부, *오늘 메모*로 이번 메시지가 남긴 한 줄, *각본과 달라진 하루*로 접힌 블록과 그 출처(붙잡기 판정 / [남음] 표시), *관계 갱신*은 실제로 바뀐 값이 있을 때만 ⑤ *모델 호출*은 두 번 이상 불렸을 때만 목록으로(붙잡기 판정·답장·재생성 각각 번호·시각·모델) — 첫 답이 비어 다시 부른 호출은 판단 근거가 없어 「근거가 붙지 않았다」로 뜨던 것을 `partOf`로 표시해 원래 답장 스레드의 자식으로 붙인다 ⑥ 발송 예정 시각을 텀 줄에, 몰아 답장은 그 자리에서 바로 나가므로 *발송* 줄을 거기서 직접 남긴다(이 길은 pending을 타지 않아 발송 결과가 안 붙었다). 모델 호출은 늘지 않는다 — 전부 DB 값과 코드 계산이고 슬랙 메시지 수도 그대로다. 검증 = 타입체크 + 스크래치 DB 스모크 42건(길 4종·판정 유무와 이유·태그 0건·재생성 스레드·발송 줄·관계 갱신 조건부)
- [x] **trace_events에 보존 기간 두기 (08-29, 이슈 #105, 브랜치 chore/105-trace-events-retention, PR #108)** — 운영 DB 사본을 표별로 재 보니 관측용 세 표(`prompt_blobs` 389KB·`trace_events` 213KB·`llm_calls` 25KB)가 전체 1.57MB의 40%인데 담긴 것은 23시간치였고, 같은 DB의 `messages`는 258KB로 50일치였다. 셋 중 `trace_events`만 보존 기간이 없어서(하루 약 91행, 행당 약 2.3KB) `TRACE_EVENT_RETENTION_DAYS = 30`과 `pruneTraceEvents`를 `src/db.ts`의 `pruneLlmCalls` 옆에 두고 `src/index.ts`의 05:50 크론에서 함께 실행한다. **30일로 잡은 근거** — 이미 올라간 글은 슬랙이 들고 있고 이 표에 남는 몫은 같은 글을 두 번 올리지 않게 막는 `dedupe_key`뿐인데, 그 몫이 필요한 기간이 게시 자리마다 길어야 하루다(아침 각본 알림은 오늘 날짜만 보고, 프롬프트 고정 두 덩이는 논리일마다 키가 갈리고, 호출 게시는 `llm_calls.traced` 표시와 3시간 상한이 막고, 새벽 정리는 그 날짜 일기가 있으면 게시까지 건너뛴다). 하루치를 지워 다시 보내는 `src/tools/retrace.ts`가 그보다 뒤 날짜를 볼 수 있어 가장 긴 장치의 15배로 여유를 뒀고, 하루 약 210KB가 쌓이므로 6.3MB 언저리에서 멈춘다. **남기는 행은 `pending`뿐이다** — `failed`·`skipped`까지 빼 두면 슬랙 토큰·채널이 잘못 잡힌 채로 오래 갔을 때 모든 행이 `failed`가 되어 표가 다시 끝없이 커진다(무엇이 왜 실패했는지는 게시를 포기하는 자리의 로그에 남는다). 지시서에 없던 조건 하나를 더 넣었다 — 아직 못 올라간 자식이 매달린 부모도 남긴다. 부모를 먼저 지우면 자식이 30분 뒤 스레드를 잃고 `skipped`가 되어 pending을 남긴다는 규칙이 조용히 깨진다. 제외 조건은 `TRACE_EVENT_KEEP` 배열 한곳에 모아 줄 하나를 더하는 것으로 늘린다(다음 작업에서 피드백이 달린 `llm_calls` 행을 90일 정리에서 빼야 한다). 조건은 NULL을 내지 않게 쓴다 — `NOT (거짓 OR NULL)`은 NULL이라 그 행이 조용히 안 지워진다. 검증 = 타입체크 + 스크래치 DB 스모크 16건(기간 지난 sent·failed·skipped 삭제, pending 보존, 29일 된 sent 보존, pending 자식이 매달린 부모 보존, 11행 중 5건 삭제, 재실행 0건, `days=0`이면 pending과 부모만 남음, integrity ok·fk 0) + 운영 사본 리허설(91행이 전부 sent이고 23시간치라 30일 정리로는 0행, 0일로 돌리면 10행 삭제·VACUUM 후 208KB→176KB). **배포 완료(08-29)** — #94와 한 번에 나갔다(위 항목의 배포 기록). 스키마 변경 없음, 운영에서 `pruneTraceEvents` 참조 3곳(db.ts 1·index.ts 2)·user_version 5·integrity ok·fk 0 확인. 첫 정리는 다음 05:50 크론이고 지울 행이 0건이면 로그를 남기지 않으므로, 실제로 줄이 보이기 시작하는 것은 게시가 30일 쌓인 9월 말부터다
- [x] **상대의 하는 일·사는 지역을 대화에서 채우기 (08-29, 이슈 #106, 브랜치 feat/106-user-profile-learning, PR #111)** — `user_profile.job`·`region`은 대화에서 드러나면 새벽 정리가 채우고 각본·선톡이 읽기로 한 값인데 채우는 코드도 읽는 코드도 없어 계속 비어 있었다(운영 1행은 손으로 넣은 값). 읽는 자리는 `renderUserBlock` 한 곳 — 대화와 선톡 문안 여섯 곳이 `buildSystemBlocks`를 함께 타므로 한 번 고치면 둘 다 닿고, 값이 없으면 줄을 넣지 않는다. 값이 있으면 쓰임 한 줄(연락 닿을 시간대·거리 감각 참고, 굳이 꺼내 보이거나 다시 캐묻지 않기)을 덧붙인다. **각본 생성에는 주입하지 않는다** — 하루 각본은 캐릭터 자신의 삶이고 생활 리듬 밖 일정을 지어내지 말라는 규칙이 이미 있어서, 유저 시간표에 맞춰 블록을 짜면 전제가 무너진다. 두 값은 불변층이라 바뀌는 날 캐시를 새로 쓰지만 하루 한 번이라 수용. 쓰는 자리는 수집이 지금 값을 넘기고(`NightlyGathered.userProfile`) 추출 출력에 `user_profile`(job·region) + 규칙 4줄(분명히 드러났을 때만·같으면 생략·짧게·기억으로 중복 저장 금지), `saveUserProfile`은 넘긴 항목만 고쳐 빈 값·공백이 아는 값을 덮지 않고 이름·생년 컬럼도 보존한다. 새벽 정리 트레이스에 *상대 프로필 갱신*(이전→새 값) 추가. 낡은 주석 셋(`db.ts`·`user-profile.ts`·`config.ts`)과 대시보드의 「채우는 코드 없음」 표시도 정리. 검증 = 타입체크 + 스크래치 DB 스모크 38건. **배포 전 필수** — 봇 밖 스케줄러 지시서(repo 밖)에 수집 필드와 추출 출력 항목을 넣지 않으면 기본 경로에서 두 값이 조용히 안 채워진다 **배포 완료(08-29)** — 배포 전에 다른 세션 다섯 곳에 물어 VM에서 도는 작업이 없는 것을 확인했고, 작업 트리에 남아 있던 다른 세션의 미커밋 파일이 딸려 나가지 않도록 main 커밋(`8b56958`)만 담은 별도 워크트리에서 보냈다. 순서는 컨테이너 안 `db.backup()`으로 백업(`/home/ubuntu/db-backups/companion-2026-08-29-pre-106.db`) → rsync → `docker compose up -d --build`. 확인 = 컨테이너 안 `src` 지문이 main과 일치(`369199ad6cb0`), user_version 5·integrity ok·fk 0, 운영 프롬프트를 다시 조립해 하는 일·사는 곳 두 줄이 들어간 것까지 봤다. 봇 밖 스케줄러 지시서에도 수집의 상대 프로필과 추출 출력의 `user_profile`을 같은 날 넣어 뒀다(repo 밖이라 커밋 대상이 아니다). 대화에서 실제로 값이 갱신되는지는 다음 새벽 정리부터 확인한다.
- [x] **답장 트레이스에 유저가 기다린 시간 싣기 (08-29, 이슈 #109, PR #110)** — 트레이스가 답장 텀만 보여 줘서 유저가 실제로 기다린 시간이 보이지 않았다. 텀은 유저 말이 다 도착한 뒤부터 재는 값이라 그 앞에 붙는 도착 대기(20~40초)만큼 어긋나고, 몰아 답장 쪽은 구간이 끝나 바로 보냈다는 것만 적혀 첫 메시지가 몇 시간을 기다렸는지 알 수 없었다. 두 줄을 더했다 — *도착 대기*(기다린 시간·그동안 온 메시지 수·첫 메시지로부터 걸린 시간)와 *쌓인 메시지*(그 구간에 쌓인 수·첫 메시지가 온 지 얼마 만에 답하는지). `bot.ts`가 대화방별로 도착 대기 기록을 들고 있다가 답장을 만들기 시작할 때 호출 기록에 붙이고(생성에 걸린 시간이 섞이지 않도록 그 시점에 계산), 깨우기 경로는 깨우기 표시를 건 메시지 시각을 기준으로 잰다. 대기가 한 시간을 넘으면 시간 단위로 적는다. 모델 호출은 늘지 않는다 — 전부 DB 값과 코드 계산이다. 검증 = 타입체크 + 스크래치 DB 스모크 26건
- [x] **말풍선 다듬기 제거 (08-29, 이슈 #113, PR #114)** — 발송 직전에 정규식으로 손보던 후처리를 없앴다. 평서문인지 가리는 패턴이 `요`를 선택 항목으로 둬서 `~는데?` 꼴의 반말 의문문까지 평서문으로 판정했고, 모델이 붙인 물음표가 마침표로 바뀌는 사례가 관측됐다. 정규식으로 한국어 말투를 가리는 데 한계가 있어 표기 규칙은 프롬프트 한곳에서만 관리한다 — 네 필터(이모지·큰따옴표·한글 사이 쉼표 제거, 물음표 보정)가 지우던 것은 이미 불변 규칙층 `OUTPUT_FORMAT`·`SPEECH_TEXTURE`가 담고 있어 프롬프트에 새로 넣은 줄이 없다. **이모지가 가끔 새어 나가는 것은 받아들인다**(사용자 결정 — 프롬프트로 막고, 하나쯤 새어 나와도 대화에 지장이 없다). `src/bubble-polish.ts` 삭제, `bot.ts`의 발송 두 자리(대기 답장·몰아 답장)에서 호출 제거, 답장 트레이스의 *다듬기* 줄과 `llm_calls` 판단 근거의 `polished` 필드 제거. `OUTPUT_FORMAT_COMPACT`·`SPEECH_TEXTURE_COMPACT`는 부르는 곳이 없는 채로 남아 있고 삭제는 11번 세션 몫이다. 검증 = 타입체크 + 스크래치 DB 스모크 20건. **함께 정한 것 둘** — `relationships.last_contact_at`은 쓰는 자리도 읽는 자리도 없고(`saveRelationshipState`는 호출자 0) 무응답 일수는 `messages`에서 세므로 별도 이슈 없이 11번 세션 옛 경로 정리에서 함께 지운다 / 답장 트레이스의 「일간층이 바뀌었다」 표시는 이름만 보고 무엇이 바뀌었는지 알 수 없어, 바뀐 대목의 이름(오늘 각본·다가오는 일정·최근 일기·관계)을 적는 쪽으로 고친다
- [x] **프롬프트 변경 알림에 바뀐 대목 이름 적기 (08-29, 이슈 #115, PR #116)** — 답장 트레이스가 하루 고정 두 덩이의 변경을 「일간층이 바뀌었다」로만 알려서, 이름만 보고는 무엇이 달라졌는지 알 수 없었다. 코드가 프롬프트를 대괄호 머리글(`[오늘/내일]`·`[다가오는 일정 — 알고 있는 것]` 등)로 갈라 대목 이름과 본문으로 만든 뒤 앞 호출의 같은 덩이와 대목별로 견주고, 달라진 대목의 이름을 가운뎃점으로 이어 적는다(`바뀐 부분 — 다가오는 일정 · 너의 최근 일기`). 머리글의 부제(` — ` 뒷부분)는 이름에서 빼고, 3개가 넘으면 3개까지 적은 뒤 나머지는 `외 N곳`으로 줄인다. 머리글 앞의 글만 달라졌을 때는 댈 이름이 없어 덩이 이름으로 돌아가는데, 그 이름도 설계 문서에서 쓰는 말로 바꿨다 — 불변층·일간층 대신 **잘 바뀌지 않는 데이터·하루 동안 같은 데이터**. 중복 방지에 쓰는 `dedupe_key`의 `fixed`·`daily`는 그대로 둬서 이미 올라간 글과 어긋나지 않는다. 모델 호출도 슬랙 메시지 수도 늘지 않는다 — 대목을 가르고 견주는 일은 전부 DB에 보관해 둔 프롬프트 본문을 읽는 코드 계산이다. 검증 = 타입체크 + 스크래치 DB 스모크 24건(대목 나누기 8종·라벨 1·2·4개·덩이 이름 폴백·바뀐 줄 스레드·앞 덩이 변경)
- [x] **주제 태그를 모델이 고르게 하기 (08-29, 이슈 #117, 브랜치 feat/117-model-picked-tags)** — 검색 태그를 코드가 글자 일치로만 찾아서, 새벽 정리가 붙여 둔 태그와 똑같은 낱말이 유저 입에서 나와야만 기억이 걸렸다. 그대로 두면 태그가 주제로 묶이지 않고 유저가 쓰는 낱말 목록이 되기도 한다. 답장을 만들기 전에 짧은 호출을 한 번 돌려 저장된 태그 이름 중에서 이번 발화와 관련 있는 것을 고르게 한다(`src/tag-pick.ts`, `purpose='tags'`). **고르는 자리는 답장 앞이다** — 답장 프롬프트에서 주제를 함께 뽑아 다음 턴에 쓰면 이번 답장이 그 주제를 못 본다. 목록에 있는 이름만 고르게 하고 답도 목록에 대조해 걸러서, 유저가 쓴 낱말이 그대로 태그가 되지 않는다(태그를 새로 짓는 자리는 새벽 정리 하나뿐이다). 모델이 고른 것과 글자가 일치하는 것을 합쳐 검색한다 — 글자 일치는 태그 이름이 발화에 그대로 들어 있는 경우라 근거가 확실해서, 모델이 놓친 것을 받쳐 준다. 호출이 실패하면 글자 일치만으로 이어 가 검색이 통째로 비지 않는다. 한 번에 고르는 상한은 `TAG_PICK_MAX = 8`이고 저장 항목별 상한(`SEARCH_LIMIT`)이 뒤에서 한 번 더 줄인다(「고정된 기준값」 표에 등재). **답장 한 건의 모델 호출은 1번에서 2번으로 는다**(붙잡기 판정이 도는 구간이면 3번). 대화 기록은 고르기 호출에 넣지 않아 붙잡기 판정만큼 짧게 유지한다. 트레이스는 슬랙 메시지를 늘리지 않고 답장 스레드 안 *모델 호출* 줄에 주제 고르기 호출을 함께 적고, 호출이 실패해 글자 일치로 물러난 경우만 *검색* 줄에 그 사실을 남긴다(`purpose='tags'` 행은 따로 게시하지 않아 `traced`가 0으로 남는다). 검증 = 타입체크 + 스크래치 DB 스모크 34건(목록 대조·합침·상한·호출 없이 끝나는 두 자리·실패 시 물러남·프롬프트 주입과 트레이스 네 갈래). 배포는 아직이다
- [x] **VM 밖으로 나가는 DB 백업 (08-29, 이슈 #112, 브랜치 chore/112-offsite-db-backup)** — DB가 VM 안 파일 하나뿐인데 백업은 배포할 때 손으로 뜬 스냅샷이 전부여서, VM을 잃으면 그 사이 대화가 함께 사라지는 상태였다. [ADR-0007](docs/adr/0007-keep-sqlite-single-file.md)에서 단일 파일 구성을 유지하기로 하며 조건으로 걸어 둔 항목이다. 방식은 WAL을 이어 복제하는 쪽 대신 6시간마다 일관된 스냅샷을 떠서 올리는 쪽으로 정했다([ADR-0008](docs/adr/0008-offsite-db-snapshot-backup.md)). 상시 프로세스를 늘리지 않고 이미지도 그대로 두며, 6시간이라는 유실 폭이 이 서비스에서 받아들일 만해서다. `src/tools/backup-db.ts`가 컨테이너 안에서 `db.backup()`으로 스냅샷을 뜨고 그 자리에서 무결성·외래키·표별 행 수를 찍으면, 호스트 크론이 도는 `scripts/backup-db.sh`가 그 파일을 꺼내 압축해 다른 계정의 버킷으로 올린다(1.7MB → 193KB, 첫 실행 16초). 보존은 3단(최근 7일 · 일 30일 · 주 26주)이고 승격은 오늘·이번 주 몫이 아직 없을 때만 하므로 한 회차를 걸러도 다음 실행이 메운다. 실패한 회차마다 슬랙으로 알리고 주 1회 정상 요약도 보내서, 크론이 조용히 멈추면 요약이 끊기는 것으로 드러난다. **복원 리허설이 이 작업의 검증이다.** `scripts/restore-db.sh`가 올라간 객체를 내려받아 압축을 풀고, 컨테이너 안에서 읽은 값을 백업을 뜰 때 로그에 남은 값과 대조한다. 실제로 올라간 객체로 돌려서 user_version 5 · integrity ok · fk 0 · 2,529행 · 표 26개가 전부 일치하는 것을 확인했다. 삭제 명령이 사람 없이 6시간마다 도는 자리라 토큰 범위를 버킷 1개로 좁혔고, 버킷 목록 조회가 거부되는 것까지 봤다. 호스트 크론은 UTC로 돌아서 `0 5,11,17,23`(KST 08 · 14 · 20 · 02시)으로 등록했다. 실행 환경과 외부 서비스 구성은 [infra.md](infra.md)에 따로 모았다.
- [x] **하루 각본에 진행 중인 일 반영, 블록에 출처 필드 추가 (08-29, 이슈 #101, PR #126)** — 각본 생성 쪽에서 코드가 설계에 못 미치던 자리 2건을 고쳤다. ① **진행 중인 일**: 각본 생성 프롬프트가 그날 컨디션 시드·어제 일기·예정된 일·매주 루틴·아크까지 읽으면서 며칠에 걸쳐 이어 하는 일만 읽지 않아, 여러 날에 나눠 하는 일이 하루 흐름에 한 번도 드러나지 않았다. `buildPlanPrompt`가 캐릭터 쪽 진행 중인 일을 최근에 손댄 것부터 `PLAN_ONGOING_MAX`(3)만큼 읽어 확정 일정과 아크 사이에 절 하나로 넣고, 끝나는 조건을 값과 함께 적어 조건이 이미 채워진 일을 오늘 또 하는 각본이 나오지 않게 했다. 항목이 0건인 날에는 머리말째로 빠지는데, `(없음)`을 적어 두면 오늘 몫이 없다는 것과 아예 없다는 것이 같은 글자로 보이기 때문이다. 상한은 `thresholds.ts`에 두고 「고정된 기준값」 표에 한 줄 넣었다 ② **출처 필드**: `PlanBlock`이 시각·활동·답장 여건·미리 아는 일정인지·활동 성격 5가지만 담아 어느 원본을 그날치로 펼친 것인지 가리키지 못했다. `source`는 예정된 일 한 건을 가리키는 `"schedule"`이거나 매주 루틴에서 나왔다는 `"routine"` 둘 중 하나고, `"schedule"`이면 `source_id`가 그 행 번호를 들고 루틴은 되짚을 행이 없어 번호가 없다. 어느 원본에서도 나오지 않은 블록에는 붙지 않는다. 프롬프트가 그날 일정을 한 줄에 하나씩 행 번호를 앞에 붙여 싣고, JSON 예시에도 두 값을 넣어 생성이 채우게 한다. `normalizePlan`이 모르는 값을 지운다. `plan_json` 안이라 DB가 검사해 주지 않고 스프레드로 딸려 오면 그대로 살아남기 때문이다. `"schedule"`인데 쓸 수 있는 번호가 없으면 출처째로 버리는데, 없어도 되는 값이라 아무거나 채우면 거짓이 된다 ③ **붙잡기 판정**: 각본 블록에 유저가 그 일정을 아는지가 없어서 판정이 알고 보낸 말과 모르고 보낸 말을 같은 무게로 받았다. 블록의 출처를 따라 원본 일정을 읽어 판정 프롬프트에 상대가 이 일정을 아는지 한 줄을 얹는다. `getScheduleById`는 캐릭터 번호를 함께 걸어 각본에 엉뚱한 번호가 적혀도 다른 캐릭터의 일정에 닿지 않게 하고, 취소·미룸으로 표시된 일정도 그대로 돌려준다. 유저가 아는지는 그 상태와 무관하기 때문이다. 출처가 없는 옛 각본·원본이 지워진 경우·읽다 예외가 난 경우에는 줄만 빠지고 판정은 그대로 돈다. `TimingTrace.block`에 출처 두 칸을 실어 텀 기록에서도 되짚는다. 두 칸 모두 옵셔널이라 필드가 없는 옛 각본과 봇 밖 스케줄러가 만든 각본에서도 그대로 돌아간다. 검증 = 타입체크 + 스크래치 DB 스모크 68건(진행 중인 일 13·출처 필드 31·붙잡기 판정 24, 붙잡기 쪽은 모델을 부르지 않고 로컬 스텁으로 판정 프롬프트 본문을 받아 확인). **배포는 아직이다.** 이번 변경이 실제로 도는 자리는 새벽 정리의 각본 생성이라 배포 전까지는 스모크가 확인한 프롬프트 조립까지만 검증된 상태고, 생성이 출처 두 칸을 실제로 채우는지는 각본 트레이스에서, 유저가 아는지에 따라 판정이 갈리는지는 답장 트레이스의 붙잡기 판정 줄에서 본다
- [x] **슬랙 채널에서 답장 피드백 수집 (08-29, 이슈 #125, 브랜치 feat/125-slack-feedback)** — 답장 한 건이 슬랙 트레이스 채널에 부모 글과 스레드로 올라가는데 읽기만 되고 표시를 남길 자리가 없어서, 지금 시각을 잘못 말한 답장 같은 것을 보고도 따로 적어 두었다가 나중에 다시 찾아야 했다. 채널에서 그 자리에 바로 남기고, 그 표시가 어느 모델 호출에 달린 것인지까지 함께 저장한다. 방식은 상시 연결 대신 10분마다 다시 읽는 쪽으로 정했다([ADR-0009](docs/adr/0009-slack-feedback-polling.md)). **분류는 리액션 4가지**(❌ 사실 오류 · 💬 말투 · ⏰ 타이밍 · 👍 좋음)이고 그렇게 본 이유는 스레드 답글로 적는다. `src/feedback.ts`가 `conversations.history`로 최근 3일 글의 리액션을, 답글이 달린 글은 `conversations.replies`로 스레드를 읽어 `call_feedback` 표에 쌓는다. **되짚기는 이미 있는 경로를 탄다** — 표시가 달린 글의 `slack_ts`로 `trace_events` 행을 찾고 그 행의 `dedupe_key`가 `call:<번호>` 꼴이라 호출 번호까지 이어진다(스레드 자식 `call:<번호>:<꼬리>`도 같은 번호로). 호출과 이어지지 않는 게시(아침 각본 알림 같은)에 달린 표시는 번호 없이 그대로 둔다. **리액션을 뗀 것은 행을 지우지 않고** `removed_at`에 확인한 시각을 적는다 — 폴링이라 뗀 것은 다음 회차에 없어진 것으로 드러나는데, 행을 지우면 무엇이 있다가 없어졌는지가 남지 않는다. 다시 붙이면 같은 행이 되살아난다. **표시가 붙은 호출은 90일 정리에서 뺀다**(`pruneLlmCalls`) — 답장이 왜 그렇게 나왔는지 사람이 판단한 기록이라 프롬프트와 출력이 함께 있어야 채점표의 정답지로 쓸 수 있고, 표시를 뗀 호출은 다시 정리 대상이 된다. 새 표는 `TABLES`에 한 줄 더하는 것이라 마이그레이션도 `SCHEMA_VERSION` 증가도 없고, 대시보드(`render-db.ts`)가 표를 열거해 그리므로 그대로 화면에 나온다. **만들지 않은 것 셋** — 요약 도구·자동 분류(데이터가 모이기 전에 규칙을 정하면 그 규칙에 맞는 것만 보게 된다), 답장 파이프라인 안의 갈고리(게시가 그렇듯 기록을 읽어 따로 돈다), 피드백이 캐릭터 프롬프트나 기억으로 가는 경로(고칠 자리를 찾는 데만 쓴다). 검증 = 타입체크 + 스크래치 DB 스모크 53건(이모지 7종 매핑·리액션 4종 저장·재수집 중복 없음·취소와 되살림·스레드 답글 저장과 빈 답글 제외·되짚기 4갈래·`dedupe_key` UNIQUE·정리 제외와 뗀 뒤 재포함·integrity ok·fk 0). **슬랙 실호출은 미검증** — 토큰과 채널 값이 VM에만 있어 로컬에서 부를 수 없다. **운영 준비 둘**: 슬랙 앱에 `reactions:read`·`channels:history` 스코프를 더하고 재설치해야 하며, `SLACK_TRACE_CHANNEL`이 채널 이름(`#…`)이면 `conversations.history`가 거부하므로 채널 ID로 바꿔야 한다(게시는 이름으로도 되지만 읽기는 ID만 받는다)
- [x] **캐릭터가 유저를 대하는 방식을 관계 표에서 정체성으로 옮기기 (08-29, 이슈 #124, PR #131)** — 관계 표의 `texture`(관계의 결)는 이름만으로 무엇을 넣는 자리인지 알기 어려웠고 담긴 내용도 `history`(지나온 이야기)와 겹쳤다. 그 자리에 넣으려던 것은 캐릭터가 유저를 어떻게 대하는지인데, 유저가 캐릭터를 만들며 적어 낸 문장을 여기 두면 새벽 정리가 매일 다시 써서 며칠 지나면 남지 않는다. 그래서 컬럼을 지우고 정체성 쪽 `creation` 행으로 옮겼다([ADR-0010](docs/adr/0010-character-attitude-in-identity.md)) — 생성 때 정한 값은 저장 함수가 고치지 않아 그 자리라야 유지된다. 공통 규칙층에 둘 수 없는 이유도 같다. 유저가 츤데레나 불안형 캐릭터를 만들 수 있어서 시스템이 태도를 못 박으면 그렇게 만든 캐릭터가 나오지 않는다. ① 관계는 여덟 항목에서 일곱으로 줄었고(`texture` 삭제), 지우기 전에 그 내용을 `history` 뒤에 이어 붙인다 ② `IDENTITY_KEYS`는 25개에서 27개로 — `태도 / 상대를 대하는 방식`과 `태도 / 애착 성향` ③ 마이그레이션은 `SCHEMA_VERSION`을 올리지 않고 표 모양을 보고 판단한다(`migratePendingWake` 선례). 같은 시기에 도는 다른 세션과 번호를 다투지 않고 배포 순서가 어느 쪽이어도 안전하다 ④ 이미 있는 캐릭터는 `src/tools/backfill-attitude.ts`가 두 행을 채운다(`--dry` 지원). 검증 = 타입체크 + 스크래치 DB 스모크 33건. **배포 완료(08-29)** — 백업(`/home/ubuntu/db-backups/companion-2026-08-29-pre-124.db`) → rsync → `docker compose up -d --build`. 배포는 커밋을 골라 낼 수 없어 같은 시점 main에 있던 #125·#134도 함께 나갔다. 확인 = 부팅 로그의 이관 줄, `relationships`에 `texture` 없음과 `history` 321자(옛 159자 + 옛 `texture` 161자), user_version 5·integrity ok·fk 0·2,551행, 백필 뒤 `creation` 행 23→25건, 운영 프롬프트를 다시 조립해 불변층에 태도 두 줄이 들어가고 관계의 결·말투의 결이 사라진 것. **원격 배포 디렉터리에 지운 파일이 남아 있었다** — rsync는 삭제를 옮기지 않아 #113에서 지운 `src/bubble-polish.ts`가 그대로 있었고, 이미지에 딸려 들어가 컨테이너 `src` 지문이 main과 달랐다(`8749294e156c` vs `bba1a1cc376e`). 지우고 다시 빌드해 맞췄다. 지문은 `src` 바로 아래 `.ts`만 해싱하므로 `src/tools/` 변경은 지문에 잡히지 않는다

## 실행

```bash
yarn            # 설치
cp .env.example .env   # TELEGRAM_BOT_TOKEN, ANTHROPIC_API_KEY 채우기
yarn dev        # 로컬 기동 (long polling)
```

## 규칙

- **문체**: 사람이 읽는 한국어 산문(설계 문서·README·이슈·PR 본문·유저에게 보내는 답)은 `writing` 스킬을 먼저 로드하고 쓴다. 코드·주석은 대상이 아니다.
- **공개 톤**: 이 repo는 공개되어 있다. 특정 서비스명 비교·개인 맥락·실제 대화 로그를 커밋하지 않는다. 사적 맥락은 전부 LOCAL-CONTEXT.md(gitignored)로.
- **보안**: `.env`, `*.db`, `logs/`, `data/` 커밋 금지 (gitignore 반영됨).
- **코드**: TypeScript strict + ESM(import에 `.js` 확장자), named export, kebab-case 파일명, any 금지. 커밋은 Conventional Commits 한글.
- **작업 단위**: 새 작업은 이슈부터 만들고 main에서 브랜치를 판다(`타입/이슈번호-영문요약`). 끝나면 PR 본문에 `Closes #이슈번호`를 넣어 올리고 `gh pr merge <번호> --merge --delete-branch`로 머지·브랜치 삭제까지 한 세션에서 끝낸다. 커밋 메시지의 `(#번호)`는 링크만 걸고 이슈를 닫지 않는다. 머지 전 사람 확인이 필요한 자리는 time-and-memory.md 「작업 세션 나누기」에 적혀 있다.
- **설계 일관성**: 캐릭터 stance(프레임 존중·안정형·아부 금지·무근거 핑 금지)는 character-design.md §5가 원본. 코드의 stance 문자열과 문서가 어긋나면 문서 기준으로 맞춘다.
- **출력 규칙 단일 소스**: 캐릭터가 내보내는 모든 글에 공통으로 적용할 규칙(태도·대화 규칙·표기·말의 결)은 `src/context.ts`의 불변 규칙층에서만 관리한다 — STANCE·RULES·OUTPUT_FORMAT·SPEECH_TEXTURE·FACT_CARE. 캐릭터마다 다른 값으로 두면 생성 결과에 따라 규칙이 흔들리므로 정체성 항목에 넣지 않는다. 선톡 문안 프롬프트 6곳(nightly 아침·안부, followup 근황·굿나잇, presence 자리비움·복귀)도 전부 `buildSystemBlocks`(3층+상황 문단)를 타므로 같은 규칙이 자동으로 들어간다 — 압축본 `SPEECH_TEXTURE_COMPACT`·`OUTPUT_FORMAT_COMPACT`는 더 이상 쓰는 곳이 없고, 삭제는 옛 경로 정리(11번 세션)에서 함께 판단한다. 큰 결을 바꿀 일이 생기면 이 블록들을 고친다.
- **모델**: 기본 `claude-sonnet-5` (환경변수 `MODEL`로 교체 가능).

## 아키텍처 요약

```
[Telegram] ←→ src/bot.ts (grammY long polling, 유저 분할입력 디바운스(유저별 텀 학습 20~40s) + 분할 발화 + 답장 순서 = 텀 결정(src/reply-timing.ts) → 생성 → 대기 → 발송(src/pending.ts), 불가 구간은 생성 없이 깨우기 표시만 걸고 구간 끝에 wake 핸들러가 3분기(쌓인 메시지 몰아 답장 / 예고한 자리면 복귀 인사 / 침묵), 예고한 블록 시작 전까지 온 말은 배웅 답 farewellSituation + 부팅 시 놓친 답장 복구 recoverMissedReplies=워터마크 중복방지+최근 3h만(자정 경계 버그 회피) + 답장 앞머리 시스템 태그 일괄 제거 parseReplyTags(겹친 태그도 [남음] 인식) + 대화 기록 시간 마커 주입 toTurns/timeMarkerFor + 발송 종류 로그 kind=reply|recover|morning|away|catchup|goodnight + 선톡 틱 간 chat 단위 상호 배제 acquireProactive(대기 답장 있으면 선톡 접음) + 발송 자리는 setPendingSender·깨우기 처리는 setWakeHandler로 pending.ts에 등록)
                ├─ /start → src/character.ts (현재: 고정 대표 캐릭터 DAEPYO_BIBLE. V2 유저 입력 생성은 createUserCharacter — 입력 검증 inputProblem → 첫 호출 generateGenesis(정체성·인물·진행·관계 첫 값·첫 인사, genesisProblem 검증+1회 재시도) → persistGenesis(트랜잭션: genesis_json {v:2}·creation 기억 행·관계 첫 값) → ensureArcs(arcMaterial). 봇 연결은 8번 세션, 그때까지 실행은 src/tools/create-character.ts. 랜덤 생성 코드는 확장용으로 보존)
                ├─ 메시지 → src/context.ts (**안정도 순 3층 조립** buildSystemBlocks: 불변층=정체성 기억(creation)·유저 프로필·공통 규칙(태도·대화·표기·말의 결·[메모] — 캐릭터 출력의 단일 소스) / 일간층=관계 8컬럼 서술·주변 인물·진행 중인 일·아크·일정·최근 일기 / 실시간 꼬리=검색 기억(답장 전 짧은 호출이 고른 주제 태그, 갱신 날짜 포함)·오늘 각본·오늘 메모·직전 대화 시점·지금 시각·말투·opts.situation(선톡 상황 문단, 맨 끝) — 말투는 저장값 relationships.speech_level 우선, 없으면 currentSpeechLevel이 최근 발화로 판정(존댓말 회귀 방지, '냐' 어미 거친 반말 금지). 변하는 값은 전부 꼬리에 — 앞 두 층은 캐시) → src/llm.ts (앞 두 층 끝 cache_control 1h, usage cw/cr 로그, 호출 정보 meta를 받으면 llm_calls에 원문 기록·행 번호를 meta.callId로 돌려줌) → 응답
                ├─ src/reply-timing.ts — 답장 텀 단일 소스: 두 태그 표 한 장(즉답 0~2분 짧은 쪽 몰림 / 틈틈이 개인 20s~2.5m·사회 30s~4m·공적 1~8m / 불가는 그 일정이 끝날 때+0~1분 지터)과 예외 둘(자는 중·이미 붙잡혀 접은 상태=즉답). 불가면 지금 답장을 만들지 않고 TimingDecision.gather(활동·구간)를 돌려줘 구간 끝 몰아 답장으로 넘긴다. 개인·사회 불가에 온 메시지만 붙잡기 판정 1콜(16토큰, 실패 시 일정 유지) → 붙잡혔으면 개인=취소·사회=미룸을 day_actuals에 기록. 상한 없음(3시간 회의면 3시간 뒤). 텀이 나온 경위는 TimingTrace(path·블록·판정 여부)로 llm_calls 판단 근거에 남는다
                ├─ src/pending.ts — 대기 답장: 만든 답장을 pending_replies에 남겨 정한 시각에 발송(재시도 3회), 부팅 때 resumePendingReplies가 이어받음, 유저가 말을 더 보내면 dropPendingReplies로 폐기 후 텀부터 재계산. 불가 구간 깨우기 표시는 kind='wake' 행(문안 없이 meta_json에 활동·구간)으로 같은 표에 걸어 구간 끝에 wake 핸들러를 부른다 — dropPendingReplies에도 살아남고(구간 끝 시각은 그대로, 지우는 건 dropWakeRows), 기다리는 동안 isWaiting=true라 선톡 틱이 접는다. 발송 함수는 setPendingSender·깨우기 함수는 setWakeHandler로 bot.ts가 주입(순환 참조 회피)
                ├─ src/tag-pick.ts — 이번 발화로 검색할 주제 태그 고르기: 답장을 만들기 전에 짧은 호출(purpose='tags', 128토큰) 한 번으로 저장된 태그 이름(listTagNames) 중에서 관련 있는 것을 고르고, 답을 그 목록에 대조해 거른다(mergeTags — 목록에 없는 말은 버려서 유저가 쓴 낱말이 태그가 되지 않는다). 글자가 일치하는 태그(tagSearch)와 합쳐 최대 TAG_PICK_MAX개, 호출이 실패하면 글자 일치만으로 이어 간다. 고른 결과는 BuildOptions.pick으로 buildSystemBlocks에 넘어가고 무엇이 골랐는지(by)·호출 번호(callId)는 답장 트레이스가 적는다
                ├─ src/memory.ts — 새 저장 구조의 저장·검색: saveMemory(키=item_type·owner·area·subject로 자리 찾기)·searchMemories(태그 일치 → 항목 우선순위 → 최신순 → 항목별 상한)·memoryBlock(갱신 날짜 포함)·saveTodayNote·tagSearch(유저 발화에서 검색어 추출 — 걸린 태그와 대조한 태그 수를 함께 돌려줘 트레이스가 검색이 돌았다는 것까지 보인다 — 한 글자 태그 "일"·"돈"·"집"은 어절 단위 매치만 허용: 어절이 태그와 같거나 태그 뒤가 조사뿐일 때만 걸리고 "일요일"·"생일"·"수집" 속 글자에는 안 걸림, 두 글자 이상은 문자열 포함 유지 — 이슈 #54). 저장은 언제나 conversation 행에만 쓰고, 생성 배치용 saveCreationMemory가 creation 행을 한 번 쓴다. searchMemories는 꺼낸 기록을 적는다(track:false로 끌 수 있음). 읽는 쪽 연결(context.ts·nightly.ts)은 8번 세션에서 완료
                ├─ src/thresholds.ts — 「고정된 기준값」 표와 1:1로 맞춘 상수 모음(답장 텀·선톡·하루 경계·프롬프트 개수 상한). 참조하는 곳은 memory.ts·context.ts·life-plan.ts·day-plan.ts와 선톡 네 모듈(proactive-policy·dispatch·presence·followup), 나머지 모듈 리터럴 통합은 그 모듈을 다시 쓰는 세션 몫
                ├─ src/labels.ts — 닫힌 목록 값의 영어 식별자 ↔ 한국어 이름 단일 표(답장 여건·활동 성격·저장 항목·주인·출처·유저 인지). plan_json 안 두 태그는 DB CHECK가 못 닿아 3중 방어(읽는 자리 toResponsiveness/toActivityCategory + 생성 직후 normalize + v3 마이그레이션 migratePlanTags)
                ├─ src/day-plan.ts — 하루 각본: 밤 정리가 새 날 것을 사전 생성(대화가 먼저면 lazy). 그날 컨디션 시드(월 리듬)를 읽어 컨디션→기상→활동 연결, 어제 일기 실제 여파가 시드보다 우선. 블록별 **답장 여건(instant/intermittent/unavailable) × 활동 성격(personal/social/official)** — 두 축은 직교. 저장은 영어 식별자, 한국어 이름은 src/labels.ts 한곳(RESPONSIVENESS_NAME·ACTIVITY_CATEGORY_NAME). category는 옵셔널, 없으면 activity로 추론(blockCategory: 회의·시험·발표·업무=공적 / 친구·가족·병원·학원·회식=사회 / 나머지 솔로=개인)
                ├─ src/life-plan.ts — 월 리듬(중간 지평): 한 달치 이벤트 + 매일 컨디션 시드를 미리 생성(ensureMonthPlan), 항상 ~1개월 런웨이 롤링(ensureRhythmRunway). 이벤트↔컨디션 인과(회식 다음날 피곤 등), 급변 없는 파도. Opus, 밤 정리 배치가 생성
                ├─ src/nightly.ts — 밤 정리(새벽 5시 컷오프): gather/apply 분리. 기본=외부 스케줄러가 tools/nightly-read·write로 수행, 폴백=봇 내 05:40 크론(API·opus). 월 리듬(rhythmNeeded 신호→rhythm 출력)도 이 배치가 생성, 아크는 달력 경계(월요일=주·1일=달)에서 기존 흐름+최근 일기로 이어쓰기 갱신. 침묵 quiet/dormant면 일기·시드·리듬만(각본·선톡 생략), reconnect면 저녁 재연결 문안만 — gather가 silenceTier/silenceDays를 노출하고 apply가 게이트를 강제(외부 경로가 백오프를 몰라도 안전)
                ├─ src/proactive-policy.ts — 선제 발화 관제탑: silenceState(무응답 논리일 수→normal/quiet/checkin/dormant)·proactiveAllowed·**dailySendPlan(그날 무엇을 보낼지 단일 판단: morning|lunch|checkin|none — 새벽 정리 문안 준비·반영 게이트·발송 직전 재확인이 같은 함수를 부른다)**. 유저 메시지가 오면 즉시 평상 복귀
                ├─ src/dispatch.ts — 아침·점심·안부 디스패처(3분 틱, LLM 0): 밤에 준비된 문안을 창 안 발송, 유저가 최근 4시간 안에 먼저 연락했으면 skip, dailySendPlan이 그날 보낼 종류로 지목하지 않으면 skip. 점심 선톡은 발송 창만 12:05~12:50인 morning 종류 행(스키마 불변, 트레이스는 purpose='lunch'). 창 놓치면 유예(창 종료 +90분, 시간대별 상한 11/14/22시) 내 발송, 넘기면 폐기 사유+시도 횟수(attempts/last_error) 기록
                ├─ src/followup.ts — 침묵 팔로업(15분 틱, 크론 0-4,8-23시): 관제탑 통과 시에만. 낮=유저 발화 기준 4h 무응답이면 근황 하루 1통(보낸 뒤에도 답이 없으면 그날은 물러남), 밤(자정~새벽 5시)=유저가 잔다 말 없이 1h+ 잠수하면 밤 인사 1회. 문안은 대화와 같은 3층(buildSystemBlocks)+상황 문단 — 앞 두 층 캐시 공유. 경과시간은 Date.now 기준(getKstNow().getTime()은 +9h라 오산)
                ├─ src/presence.ts — 자리 비움 예고(10분 틱, 크론 7-23시, 유저 발화 ≤4h일 때만 — 자기 발화로 창이 열리던 체인 차단): 긴 불가(운동·샤워·외출, ≥20분)를 PlanBlock.advance_known으로 갈라 미리 아는 일정은 시작 10분 전~3분 후에 예고, 닥친 일은 시작 시점~12분 후에 알리고 바로 조용, 연속 불가 사이 경계는 2분 전~12분 후(블록당 1회, 하루 3회 상한=예고만 계산·복귀 인사 제외, 하루 각본 생성 프롬프트도 같은 상한으로 불가 구간 수를 제한). 선톡 하루 총량 캡에서는 제외. 공적 불가(회의·시험)는 "끝나고 연락" 톤. 다녀온 뒤는 이 파일 몫이 아니다 — 구간 끝의 wake 핸들러(bot.ts)가 몰아 답장과 복귀 인사를 한 자리에서 처리한다. **개인·사회 불가는 유저가 붙잡으면 reply-timing이 판정 한 콜로 가려 개인=취소·사회=미룸을 day_actuals에 기록→그 블록 즉답, 공적은 못 접음.** 접은 블록에는 예고 스킵(isHeldNow). 문안은 대화와 같은 3층(buildSystemBlocks)+상황 문단 — 앞 두 층 캐시 공유. 막연한 침묵→'알고 하는 기다림'
                ├─ src/reply-trace.ts — 답장 트레이스(1분 틱, LLM 0): llm_calls·prompt_blobs를 읽어 게시함에 쌓는다. 본문=대상 발화·텀 경로·검색 태그와 기억·모델이 쓴 답, 스레드=실시간 꼬리 전문(발송·폐기 결과는 pending.ts가 traceReplyOutcome로 같은 스레드에 붙인다). 하루 고정 두 덩이는 그날 첫 호출에서 한 번만, 하루 중에 바뀌면 바뀐 대목 이름(changedSections)과 바뀐 줄(lineDiff)만. 판단 근거를 5분 기다리고 3시간 지난 호출은 표시만(게시 안 함), 새벽 정리 쪽 purpose는 구간 3 몫이라 건너뛴다. 오늘 몫을 지워 다시 보내는 도구는 src/tools/retrace.ts
                ├─ src/nightly-trace.ts — 새벽 정리 트레이스(LLM 0): applyNightlyOutput 래퍼가 반영 전에 손댈 키의 지금 값을, 반영 뒤에 결과를 읽어 게시함에 쌓는다(트랜잭션 바깥이라 게시 실패가 반영을 되돌리지 않는다). 본문=오늘 메모·각본과 달라진 하루·관계 갱신·새 일정, 스레드=기억 신규와 덮어쓰기(이전→새 값·태그 차이)·일기 전문·내일 선톡 문안과 발송 창·그 밤의 모델 호출 원문(2시간 안, 올린 뒤 traced 표시). 침묵 단계로 일부만 만든 날은 건너뛴 것을 한 줄, skip으로 끝난 날은 미게시
                ├─ src/feedback.ts — 슬랙 채널에 남긴 표시 수집(10분 틱, LLM 0): conversations.history로 최근 3일 글의 리액션을, reply_count가 있는 글은 conversations.replies로 스레드 답글을 읽어 call_feedback에 쌓는다. 리액션 4종이 분류(❌ 사실 오류·💬 말투·⏰ 타이밍·👍 좋음, toFeedbackKind), 스레드 답글이 이유. 표시가 달린 글의 slack_ts로 trace_events를 찾아 dedupe_key `call:<id>`(스레드 자식은 `call:<id>:<꼬리>`)에서 호출 번호를 꺼내 함께 적고, 호출과 이어지지 않는 게시에 달린 표시는 번호 없이 둔다. 리액션을 뗀 것은 지우지 않고 removed_at 표시(다시 붙이면 되살림), 같은 표시는 dedupe_key UNIQUE로 한 번만. 답장 파이프라인에 갈고리를 걸지 않고 게시 기록을 읽어 따로 돈다 — 수집한 표시는 프롬프트에도 기억에도 들어가지 않는다(ADR 0009). SLACK_BOT_TOKEN·SLACK_TRACE_CHANNEL 없으면 전체 no-op
                └─ src/trace.ts — 슬랙 트레이스 게시(1분 틱, LLM 0): 파이프라인이 recordTraceEvent로 trace_events에 쌓은 기록을 runTraceTick이 슬랙 chat.postMessage(네이티브 fetch, SDK 없음)로 발송. dedupe_key UNIQUE + INSERT OR IGNORE 중복 방지, thread_key/parent_key 스레드(부모 게시 전 자식은 다음 틱으로 미룸, 부모 영구 실패 시 자식 skipped), 영구 오류(invalid_auth·not_in_channel 등)는 즉시 failed·나머지 3회 재시도. 아침 각본 알림은 이 틱이 7시부터 직접 enqueue(본문=시드+블록별 두 태그·답장 텀 범위, 스레드=시간대별 정리+생성 프롬프트 재조립, ondemand→nightly 교체 구분). SLACK_BOT_TOKEN·SLACK_TRACE_CHANNEL 없으면 전체 no-op — TABLES 표 한곳에 정의, SCHEMA_VERSION 5(v1=새 테이블·컬럼 이관 / v3=pending_replies.kind·areas.note + plan_json 두 태그 식별자 치환 / v4=V2 기억 구조·관계 컬럼·scheduled_messages 개명 / v5=llm_calls·prompt_blobs 신설 + viz_events→trace_events 개명) / characters / relationships(관계 일곱 항목 컬럼 여덟=stage·speech_level·speech_note·address_terms·rapport·cautions·history·feelings + updated_at — 캐릭터가 유저를 대하는 방식은 이 표가 아니라 memory_items의 태도 영역 creation 행) / memory_items(기억 한 건, 항목=fact|ongoing|person × owner=char|user, 키=item_type·owner·area·subject·origin, origin=creation|conversation, 항목별 전용 컬럼 relation·contact_mode·region·last_mentioned_at·end_condition·interest + 꺼낸 기록 last_retrieved_at·retrieval_count) / memory_items_legacy(옛 모양 73건, 7번 세션이 이관 확인 후 삭제) / tags(태그→기억·일기·일정) / areas(캐릭터가 쓰는 영역 이름) / today_notes(그날만 쓰는 메모) / day_actuals(각본과 달라진 블록) / pending_replies(만들어 둔 답장을 정한 시각에 발송, status=waiting|sent|failed|superseded, call_id=이 답장을 만든 호출 — 발송·폐기를 그 호출의 트레이스 스레드에 잇는다) / user_preferences(V2 재정의: 캐릭터와 무관한 유저 단위 선호 자리, 지금은 자리만) / user_profile(preferred_name·gender·birth_year·job·region + 옛 age_band) / diary_entries / day_plans(하루 각본, made_by=nightly|ondemand) / day_seeds(컨디션 시드: energy·wake_hint·mood·reason) / schedules(일정, owner=char|user, origin·user_knows·parent_kind) / cast_members(관계도, owner=char|user, relation_label·recent_note) / arcs(period=year|season|month|week) / scheduled_messages(미리 만든 선톡, kind=morning|checkin) / send_failures(kind=away|catchup|goodnight) / recovery_marks(복구 중복 방지 워터마크) / messages(기록, role=user|assistant, sent_at, meta.kind=reply|recover|morning|away|catchup|goodnight) / trace_events(슬랙 트레이스 게시함, status=pending|sent|failed|skipped, dedupe_key UNIQUE·thread_key·parent_key) / llm_calls(모델 호출 한 건=행 한 건, 메타+context_json 판단 근거, purpose에 CHECK 없음, traced=슬랙에 올렸는지 표시) / prompt_blobs(프롬프트·출력 본문, 키=내용 해시 sha256 앞 16자, 90일 뒤 고아 행 삭제) / call_feedback(슬랙 채널에 사람이 남긴 표시, source=reaction|reply, kind=fact|tone|timing|good, dedupe_key UNIQUE·removed_at 소프트 삭제 — 살아 있는 표시가 붙은 llm_calls 행은 90일 정리에서 제외)
[모델] 실시간 대화=sonnet / 일기·추출·각본·월 리듬·아크·캐릭터 생성=opus(MODEL_DEEP) / 밤 정리 기본 경로는 외부 스케줄러라 API 미사용
```

핵심 설계 불변: 캐릭터는 유저 입력으로 생성(V2)하고 생성 때 정한 큰 정체성은 불변(origin=creation 행은 저장 함수가 수정 거부) — 자잘한 디테일은 대화로 쌓이되(누적 정체성) 한번 나온 것은 일관 유지(어긋나는 발명·유저 훼이크 추종 금지) / 잘 통하는 것은 관계 컬럼(rapport)으로 캐릭터에게 공개하되 아부 금지는 stance가 지킴, user_preferences는 유저 단위 선호 자리 / 선제 연락은 근거 있을 때만 하루 1통, 유저가 오래 무응답이면 물러난다(3일 조용→14일차 저녁 재연결 1통→침묵) / 이별은 비가역 / 컨디션은 매일 독립 주사위가 아니라 월 단위로 미리 깔린 리듬(이벤트 인과+파도), 실제 산 하루가 그 위를 덮어씀 / **답장 텀은 블록의 두 태그(답장 여건 × 활동 성격) 표 한 장에서만 나온다 — 이벤트별 예외처리 금지(스파게티 방지). 개인=붙잡으면 취소, 사회=양해 구해 미룸, 공적=못 접음.** / 답장은 즉답·틈틈이면 텀을 정한 뒤 바로 만들어 정한 시각에 내보내고, 불가 구간이면 만들지 않고 구간 끝에 깨어나 쌓인 메시지를 한 번에 읽고 몰아 답한다(몰아 답장은 만드는 시점=보내는 시점이라 일정을 막 끝내고 이제 봤다는 전제가 사실이 된다)
