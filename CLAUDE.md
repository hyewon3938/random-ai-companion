# random-ai-companion — 작업 가이드

나와 같은 시간을 살고 먼저 물어봐주는 AI 대화 상대 (만남은 카드 한 장으로 시작). 컨셉·가설·설계 총론은 [README.md](README.md), 규모와 비용 검토는 [scaling.md](scaling.md)가 단일 소스다.

세부 설계 문서는 `docs/` 아래에 두고 커밋하지 않는다(gitignored). 타겟·어필 포인트는 `positioning.md`, 설계 논리·층은 `architecture.md`, 캐릭터 속성 체계는 `character-design.md`, 수집·생성 데이터 카탈로그는 `data-model.md`. 로컬 전용 컨텍스트는 `LOCAL-CONTEXT.md` — 새 세션은 그 파일부터 읽을 것.

## 상태 (갱신 의무 — 세션이 끝나기 전 여기와 LOCAL-CONTEXT.md를 최신화한다)

- [x] 설계 문서 (README, character-design)
- [x] 스캐폴딩 + 로컬 기동 확인 + **클라우드 VM 배포·가동** (07-06. 재배포 = 로컬에서 동기화 후 `docker compose up -d --build`)
- [ ] 유저 프로필 + **카드 온보딩** (/start: 프로필 등록 → 카드 제시 → [대화 시작]/[다음 카드] 인라인 버튼, 패스=무비용 재생성 + card_reactions 기록)
- [x] **밤 정리(구 밤 일기) — 이중 경로 확정** (07-11): 기본 = **외부 스케줄러(매일 새벽 KST, API 비용 없음)** — `src/tools/nightly-read.ts`(수집)→자체 생성→`nightly-write.ts`(적용). 폴백 = 봇 내 05:40 크론(API, opus) — 일기 중복 체크로 이중 실행 방지. 산출 = 일기 응고(각본 vs 실제·상대 감정·내부 온도) + 유저기억·누적정체성·일정·인물 추출 + 새 날 각본 + 선톡 문안
- [x] **선톡 = 밤 정리(문안 준비) + 발송 디스패처(3분 틱, LLM 콜 없음)** — scheduled_sends 하루 1통, 근거 있을 때만, 유저가 그날 먼저 연락했으면 미발송. 무응답 시 익일 후속은 밤 정리 판단에 위임 (07-11). 창 놓치면 유예 발송(창 종료 +90분·시간대 상한) 후 폐기 사유·시도 횟수 기록, 텔레그램 연결 보온으로 발송 실패 자체를 예방 (08-06)
- [x] **설계 감사 개선 (08-06)** — 밤 정리 원자화(트랜잭션)+결번 백필, lazy 각본을 정식 각본이 교체(day_plans.source), 선톡 틱 3종 상호 배제+발송 직전 재확인, '하루' 정의 통일(새벽 5시 논리일), 선제 발송 총량 캡 공유, state_json 파싱 가드, 컨텍스트 주입 상한, followup·presence 실패 영속 기록(send_failures). 나머지 항목은 이슈 #1
- [x] **선제 발화 관제탑 + 침묵 백오프 (08-07, 이슈 #2)** — src/proactive-policy.ts가 "먼저 말 걸어도 되는가"의 단일 판단: 무응답 0~2일=평상 / 3~13일=전면 조용(문안 생성도 스킵, 밤 정리는 일기·시드·리듬만) / 14일차=저녁 재연결 1통(kind=reconnect) / 이후=유저 복귀까지 침묵. presence '최근 대화' 판정을 유저 발화 기준으로 수정(발송이 발송을 부르던 체인 차단). 구조 정리 ②~④는 이슈 #3
- [x] **구조 정리 ② (08-07, 이슈 #3)** — 시스템 프롬프트를 안정도 순 3층(불변=바이블·규칙 / 일간=관계·인물·아크·일정·일기 / 실시간 꼬리=오늘 각본·시각·말투)으로 재배열 + 앞 두 층에 cache_control(1h). 관계 상태는 JSON 통주입(9.8K자, 프롬프트의 42%) → 압축 서술로. 실측: 프롬프트 23.5K→17.2K자, 캐시 히트 시 16K토큰이 0.1× 과금(입력비 ~85%↓) — llm 로그의 cw/cr로 관측
- [x] **구조 정리 ③~④ (08-07, 이슈 #3)** — ③ 프롬프트 규칙 10블록→성격별 4블록(원문 보존·중복만 제거), presence 20분 미만 간헐 예고 제거, capture 10→15턴, 아크 달력 경계 갱신(월요일=주, 1일=달·분기 계절·연초 올해 — 기존 흐름·최근 일기 이어쓰기, 침묵 중 생략) / ④ README를 실측 데이터(침묵일 선톡·캐시 히트·프롬프트 크기) 기반으로 갱신
- [x] **대화 기록 시간 마커 (08-18, 이슈 #5·#7)** — 날짜 없는 대화 기록이 전부 '오늘'로 수렴해 며칠 전 이야기를 방금 일처럼 받던 문제 수정. 기록의 첫 발화·논리일이 바뀌는 지점·앞 발화와 1시간 이상 벌어진 지점에 `[어제 22:10]` 같은 마커를 코드가 계산해 붙이고(`timeMarkerFor`), 실시간 꼬리에 직전 대화 시점 한 줄을 주입한다. 마커는 messages에만 들어가 프롬프트 캐시 경계는 그대로. 이어서 답장 앞머리 태그 파싱을 `parseReplyTags` 한 곳으로 통합 — 태그가 겹쳐도 [남음] 신호를 놓치지 않는다
- [x] **말투 자연화 — 말의 결 규칙 (08-19, 이슈 #9)** — 모델 기본 버릇(직전 발화 명사구 되받기·문어체 은유·상담사식 감정 라벨링 공감)을 억제하는 `SPEECH_TEXTURE` 블록을 공통 규칙층(불변층)에 신설, buildSystemBlocks를 타지 않는 선제 발화 문안 프롬프트 6곳(followup·presence·nightly)에는 압축판 `SPEECH_TEXTURE_COMPACT` 주입. 사색적 취향은 유지하고 표현만 입말로(내용/표현 분리), 무심함 회귀 방지 가드 포함 — humanizing-log #18. **후속 보정(같은 날, 이슈 #11)**: 물음표 보정 판정 보수화(의문사 위치·굳은 꼴·어미 가드 — `src/bubble-polish.ts`로 분리, 단독 회귀 검증 가능), 쉼표 억제(규칙+한글 사이 쉼표 제거 방어선), 감성 과잉 멘트 금지 규칙 추가 — humanizing-log #19
- [ ] ~~교체 플로우~~ → **현 범위에서는 미구현 결정**(첫 캐릭터 정착 시나리오). 설계는 로컬 architecture.md의 이별과 교체 절에 문서화 완료 (07-11)
- [x] 분할 발화·미러링·대화 리듬 (디바운스 15~30s·프레즌스 지연·타이핑 텀·잠/기상 — humanizing-log 참조). 반말 전환은 우진 바이블에 반영(성향 다양화 `banmal_style`은 랜덤 생성 복원 시)
- [ ] **프롬프트·기억 구조 재설계** (08-19 착수) — 프롬프트 조립과 기억의 write/read/update 정책을 다시 설계한다. 설계 원본은 repo 루트 `time-and-memory.md` — 키·태그 체계, 13칸 저장 구조와 칸별 주인, 저장 세 묶음, 프롬프트 쌓는 순서(캐시 경계 둘), 세 층 생성(타고나는 성향은 케미 축에서 도출해 1층), 유저 기본 프로필 다섯까지 확정. 리드미용 데이터 지도 도식은 `docs/readme-data-map` 브랜치(assets/data-map.svg·html, 미푸시). **설계 마감(08-21)**: 남은 설계 셋 확정 — 생성 순서(코드 재료 → 사람 한 콜 → 진행 중인 일·예정된 일 → 아크 → 월 리듬 → 각본. 순서가 "아크는 결만"을 강제한다) / 주변 인물 필드(월 리듬용 갈래·만나는 결·어디 + 프롬프트용 관계·지금·유저가 아는가 + 코드용 누구 쪽·마지막 등장, 이 칸은 이름 하나가 키인 예외) / 바이블 내려보내기는 절차를 만드는 대신 바이블 자체를 없앰(생성 출력 스키마 = 칸 목록). 구현 계획(스키마·이관·배포 5단계 이중 기록)까지 time-and-memory.md에 기록. **순서 재조정(08-21 밤)**: 바로 이관하지 않고 2단계(하루 각본·오늘 칸 / 월 리듬·시드 / 판정 한곳=답장 텀·잠·조정·선제 문안 / 우리 사이 속·유저 선호 처분)를 마저 설계한 뒤 한 번에 구현·이관 — 밤 정리·프롬프트 조립을 두 번 갈아엎지 않기 위해. 그 뒤 배포·실사용 → 채점 하네스. 칸 사이 연결은 태그가 담당(연결 테이블 없음, 이름=태그)(`src/tools/eval.ts`, 채점표는 time-and-memory.md 「채점표」 절). 현 구조는 그대로 두고 병행 개발. 확인된 개선 대상 3종: 규칙층이 금지형 위주라 말투 지시가 희석됨 / 접은 일정이 각본에 남아 실제로 한 일처럼 주입됨(`attention_override`가 `until_ts`만 보관) / 경과 시간 줄이 실시간 꼬리에 없어 모델이 추측함. **2단계 ① 확정(08-21 밤)**: 작업 순서는 사용자 결정(월 리듬·컨디션 → 하루 각본·오늘 → 답장 텀 → 우리 사이·유저 선호). 월 리듬은 저장소 없음(산출 셋=이벤트→예정된 일 / 시드 / 의향 착수 판정→진행 중인 일, 한 콜) / 예정된 일에 필드 5 추가(누구와=이름 태그, 영역, 유저가 아는가, 출처=대화·리듬·진행 중인 일, 부모=맥락 사슬) — 두 자리 키 두 번째 예외 / 시드 4값 유지 / 아크 갱신 재료에 진행 중인 일·예정된 일 추가, 같은 밤엔 아크 먼저. 동명이인은 같은 쪽 안에서만 문제 — 이름 늘려 가르고(회사 민수) 태그는 둘 다 유지. 이관 8단계 추가: 기억은 옮기고 삶 재료(미래 시드·유저 모르는 리듬 이벤트)는 재생성, 산 기록은 불변. **2단계 ② 확정(08-24)**: 하루 각본·오늘 칸 모양 — 오늘 칸=계획(각본)·실제·오늘 메모 셋 / 각본 블록 5필드(시각·활동·답장 여건·활동 성격은 생성이 값으로 적음·출처=예정된 일/루틴 가리킴 — 원본 규칙의 각본 쪽 답) / 생성 재료 6종(시드·어제 일기·예정된 일·매주 루틴·진행 중인 일·아크) / 실제는 어긋난 블록만 3필드(하려던 것·어떻게 됐나·왜), 접기·미루기 결정은 즉시 실제에 기록(until_ts만 남기던 attention_override 대체), 대화로 드러난 어긋남은 오늘 메모 경유. 설계 허브는 `docs/data-map.html`(gitignored, 08-25 스크래치패드에서 이동) — 예시가 대표 캐릭터 실데이터라 로컬 전용, 공개·커밋 시 예시 교체 필수. **개편 전후 비교 항목 신설(08-25)**: 문서 본문에서 "지금은 이런데 새 구조에서는" 식 서술을 걷어내고 time-and-memory.md 「개편 전후 비교」 절에 표로 모았다(정체성 저장·활동 성격 태그·취소한 일정·경과 시간·진행 중인 일·아크·예정된 일 필드·즉시 기억·유저 프로필 아홉 줄). 배포 순서에 6단계를 추가 — 5단계에서 옛 경로를 지우면 비교 대상이 사라지므로, 며칠 써 본 뒤 확인 열을 반드시 채운다. **2단계 ③ 일부 확정(08-25)**: 답장 텀 파생 표를 설계로 명문화 — 답이 나가기까지 = 유저 말이 다 도착할 때까지 기다리기(20~40초, 유저별 학습, **답장 텀에 포함하지 않음**) + 답장 텀(두 태그 표: 즉답 0~2분 짧은 쪽 몰림·0초 허용 / 틈틈이 개인 20s~2.5m·사회 30s~4m·공적 1~8m / 불가 일정이 끝날 때, 개인·사회 일정은 시작 10분 안 예외). **구현에 있던 35분 상한은 폐기** — 세 시간짜리 회의면 세 시간 뒤가 맞고, 대신 대기 중인 답장을 저장해 재시작 뒤에도 이어가게 만든다(현 복구는 최근 3h만 봄). 표를 따르지 않는 예외 둘(붙잡아 취소·미룬 상태 / 자는 시간에 온 연락) = 답장 텀 0. 잠 정책 단일 판정(새벽엔 깨워도 일어나 유저와 있는다), 붙잡기 판정(개인 취소·사회는 만나기로 한 상대에게 양해·공적 불가)과 실제 기록까지 같은 절에. **가운데 태그 이름 짬짬이 → 틈틈이**(문서 전부 반영, 코드 문자열 19곳은 구현 때 함께). **2단계 ③ 마감(08-25)**: 선제 발화 여섯 곳(아침·재연결·근황·굿나잇·예고·복귀)도 대화와 같은 3층을 쌓고 맨 끝 상황 문단·출력 형식만 다르게 — 앞 두 층이 같아야 캐시가 붙는다. 손으로 잇던 셋(정체성 JSON 주입·SPEECH_TEXTURE_COMPACT·미리 계산한 openLoops) 폐기. 규칙층은 52줄 중 33줄이 금지형이라 말투 지시가 묻힘 → 하는 말로 다시 쓰고 금지는 재발 근거 있는 것만 남긴다(줄 추가 시 근거 대화 명시 의무). RESPONSE_TIMING 5줄 삭제(코드가 태그 값을 안 쓴 지 오래 + 텀은 표가 정함) — 대신 이 답장이 언제쯤 나가는지를 코드가 꼬리에 적어준다. 설계 허브에 04 답장 텀 카드 추가. **2단계 ④ 확정(08-25, 2단계 전체 마감)**: 우리 사이 칸 = 서로 부르는 말 / 지금 말투(존댓말·반말을 값으로 저장)와 말 놓은 날·먼저 꺼낸 쪽 / 관계의 결(라벨 아닌 짧은 서술) / 우리끼리 생긴 표현 / 조심할 것 다섯. 뒤 둘은 RelationshipState에 필드·프롬프트 자리가 있는데 밤 정리 추출 스키마에 없어 **한 번도 채워진 적 없음** → 이관 때 추출 출력에 추가하고 지난 대화에서 초기값 채운다(태도 규칙 1번이 재료 없이 돌던 상태). 만난 날수·연락 빈도는 계산값이라 미저장. **유저 선호 추출은 중단**(읽는 코드 없음 + 케미 축→MBTI로 축이 바뀌어 지금 값은 재사용 불가), 테이블·기존 값은 보존하고 매칭은 나중에 산 기록(대화 원문·카드 반응·관계 지속)에서 다시 뽑는다. **설계 허브 여덟 카드 완성(08-25)**: 05 먼저 말 걸기(여섯 종류·침묵 백오프·발송 직전 재확인) / 06 프롬프트 쌓는 순서(네 층·재사용 표시 둘·꺼내는 양 셋) / 07 새벽 정리(재료→칸 이동→일기→아크·월 리듬→내일 각본·문안→한 번에 반영, 하루 경계 새벽 5시) / 08 대화 한 번이 지나가는 길(도착→텀 먼저 결정→조립→생성→다듬어 발송→오늘 메모, 모델 호출 1회). 01 카드도 우리 사이 다섯·유저 선호 중단에 맞춰 수정. **남은 설계는 1단계 잔여 하나뿐** — 운영 DB의 `relationships.state_json` → `char_facts`를 뼈대 두 표 키에 대조해 빠진 키 찾기(로컬 절반은 08-25 완료, 씨앗이 못 채우는 키 11개 확인). 그 뒤 구현 착수. **허브 05~08 피드백 1차 반영(08-25)**: 말투를 판정만 하고 버리던 것을 값 저장으로 변경(반말 사이가 된 뒤 유저가 존댓말을 섞으면 최근 발화 판정이 되돌아가고, 말 놓은 날짜만 남기면 지금 상태를 알 수 없음) / 04 답장 텀 표의 막대·회색 줄 제거(가득 찬 길이 8분에 근거가 없었음 — 눈금 규칙 없는 시각 요소는 폐기) + 붙잡을 수 있는 10분 설명을 표 각주에서 「유저가 붙잡으면」 쪽으로 옮겨 다시 씀 / 05 근황은 간격이 늘지 않고 두 번째에서 멈추는 것으로 정정, 각본 전환점 판단 주체가 모델임을 범례·본문에 반영 / time-and-memory.md에 「고정된 기준값」 절 신설 — 답장 텀·선톡·하루와 기록 3표에 값마다 출처(측정/판단/구현 잔재)와 결정 필요 여부 표시, 붙잡기 시간이 코드 12분·문서 10분으로 갈린 것도 여기 기록. **허브 피드백 2차(08-25)**: 붙잡기 시간 제한 폐기 — 일정 시작 후 10분(코드 12분) 안에만 조정 가능하던 규칙을 없애고, 답장 불가 블록에 온 메시지가 답을 기다리는 내용인지를 짧은 판정 한 콜로 가른다(개인=취소·사회=양해·공적=불가, 경과 시간 무관). 답장 텀에서 유일하게 모델을 부르는 자리라 04·08 카드의 범례와 호출 수도 수정 / 04 툴팁 예시 정정(회의는 틈틈이가 아니라 불가 — 예시를 회식으로) / 12분 상수 제거를 구현 잔손질 셋에 추가 / 즉답 0~2분은 설계상 짧은 쪽 몰림인데 코드가 균등(rand)이라 구현 때 skewLow로 맞춤. README는 허브 HTML로 교체·링크할 가능성이 있어 이번 정리에서 손대지 않음. **허브 피드백 3차(08-25)**: 선제 발화 용어를 **선톡**으로 통일(문서·허브 05 카드 전체). 종류를 여섯에서 다섯으로 재편 — 미리 만드는 둘(아침 선톡 / 안부 선톡=구 재연결) + 대화 중 셋(자리비움 선톡=구 예고+복귀 통합 / 근황 선톡 / 밤 인사 선톡=구 굿나잇, 이름 잠정). **아침 선톡의 "근거 있을 때만"을 폐기** — 어제 대화했으면 항상 보내고 챙길 것이 있으면 얹는다. 무응답 1일=아침 한 번 더 / 2일=아침 거르고 점심 한 통(시각 미정) / 3~13일=조용, 단 유저가 말해 둔 유저 일정이 있는 날 아침만 예외 한 통 / 14일=저녁 안부 선톡. 근황은 조건 셋(2시간+각본 전환점+연속 2회 taper)을 **4시간 무응답 하나**로 축소해 하루 한 통, 자리비움은 유저 발화 4시간 이내 게이트 유지(조건 없이 보내면 조용한 유저에게 알림만 쌓임) — 4시간이 대화 중 선톡 전체의 마지막 경계. 붙잡기 문단은 경과 시간 서술을 빼고 "개인·사회의 답장 불가 중 붙잡는 말이면 취소·미룸, 평범한 답장이면 그대로 유지"로 다시 씀. 판정 콜은 공적에서 미호출, 유저가 시간 두고 여러 번 보내면 묶음마다 한 번. 고정된 기준값 표도 선톡 이름으로 갱신(100~139분·2회 폐기, 점심 시각은 미정 표시) **허브 피드백 4차(08-25)**: 밤 인사 선톡 조건을 사용자 의도대로 정정 — 새벽 2~5시 창이 아니라 **밤 10시 이후 대화에서 유저가 자겠다는 말 없이 1시간 무응답**이면 발송, 미리 예약하지 않고 15분 틱마다 조건 재확인(유저가 새벽에 답하면 1시간 재계산, 유저가 잘 거라고 말하면 그 답장이 마지막 말이라 미발송). 자리비움 선톡 **하루 4회 상한 폐기**(일정 수가 곧 상한 + 4시간 게이트가 이미 대화 중일 때로 한정, 선톡 전체 6통 캡에서도 제외). 아침 선톡 툴팁에서 90분 유예 설명 제거(전송 실패 대비 장치라 카드에 둘 내용이 아님, 기준값 표에만 유지). 근황 선톡은 목적을 풀어 다시 씀(재촉하지 않고 자기 근황만 한 통 → 유저가 다시 말 걸 자리). 05 카드 foot 두 문단을 '층' 없이 재작성, aside 제목 '답이 없으면 물러난다'→'유저가 답이 없을 때', 확인 항목 '다른 종류가 겹치는가'→'다른 선톡 예약과 겹치는가' **허브 피드백 5차(08-25)**: 밤 인사 선톡 기준을 밤 10시→**자정**으로 물림(자정에 자는 사람이 10시부터 1시간 딴 일 하다 잘 자라는 인사를 받으면 그날 대화가 닫힘 — 기준값 표 00:00~05:00). 근황 선톡 뒤 무응답이면 그날 아무것도 더 보내지 않고 다음 날 아침 선톡으로 이어짐을 명시. 05 카드 foot 두 문단→한 문장(선톡도 대화 답장과 같은 내용을 담고 맨 끝 한 문단으로 종류를 알림, 쌓는 순서는 06 카드로 넘김), 캐시·비용 서술은 06 카드 aside에만 유지. 06 aside에 마지막 문단이 여섯 가지인 이유(자리비움만 나갈 때·돌아왔을 때 분리) 추가 **허브 피드백 6차(08-25)**: 데이터 지도 카드를 프롬프트 카드에 합쳐 여덟 → 일곱 카드(01 캐릭터 생성 / 02 하루 각본 / 03 답장 텀 / 04 선톡 / 05 프롬프트 조합 순서 / 06 새벽 정리 / 07 대화 한 번) — 데이터 지도 왼쪽 열이 프롬프트 순서를 그대로 그리고 있어 같은 그림이 두 번 나왔다. 저장 항목 설명·예시는 각 묶음 툴팁으로 흡수, 두 곁상자(삶을 만드는 데이터·운영과 측정)는 보존. 카드 제목 '프롬프트 쌓는 순서'→'프롬프트 조합 순서', 부제는 「답장 생성 프롬프트를 어떻게 구성하는가」, 열 제목 '쌓는 차례'→'데이터 순서'(부제에 상단·하단 원칙). '층' 어휘 폐기 → 잘 바뀌지 않는 / 하루 동안 같은 / 지금 얘기로 찾아낸 / 방금 생긴 데이터, 각 묶음 small은 실제 저장 항목 이름만. 05 foot의 06번 카드 안내 삭제, 카드 사이 참조·내비게이션 전부 새 번호로 **허브 피드백 7차(08-25)**: 합쳐온 설명이 저장 방식을 말하고 있어 05 카드 툴팁 전부를 "프롬프트에 무엇을 어떻게 꺼내 넣는가"로 재작성(씨앗=항목 이름과 값을 짝지어 넣음 / 유저 프로필=빈 항목 제외 / 최근 며칠 일기=항상, 그보다 오래된 것은 3번에서 주제 일치 시 — 기억 셀 지도의 읽는 조건을 그대로 문장화). **칸 이름 '우리 사이' → '캐릭터와 유저의 관계'**(문서·허브 전체). 묶음 이름 셋째·넷째 교체(지금 얘기로 찾아낸 → 대화 주제에 맞춰 꺼내는 / 방금 생긴 → 답장할 때마다 달라지는). 캐릭터의 삶을 만드는 데이터 곁상자 삭제(01 카드에서 이미 설명 + 프롬프트에 안 들어감), 운영과 측정 곁상자는 07 카드로 이동. 85%는 청구서가 아니라 로그의 캐시 재사용 토큰 수 × 0.1 단가 계산값임을 카드·문서 양쪽에 명시(첫 답장은 캐시 쓰기로 더 냄, 출력 요금 불변). time-and-memory.md 「프롬프트에 쌓는 순서」의 꼬리 설명에서 오늘 각본을 빼고 두 번째 묶음으로 정정 **허브 피드백 8차(08-25)**: **05 기억 저장 구조 카드 신설**(선톡과 프롬프트 조합 순서 사이, 뒤 카드 한 칸씩 밀려 여덟 장 — 06 프롬프트 조합 순서·07 새벽 정리·08 대화 한 번). 내용은 time-and-memory.md 「항목 키와 주제 태그」 이하 절 — 기록 한 줄의 모양(저장 항목·키·내용·태그·딸린 값) / 저장은 키로 자리 찾기·읽기는 태그로 모으기 / 밤 4단계(쪼개기→저장 항목·키 붙이기(기존 키 목록 동봉)→덮을지 판정→태그) / 이름 태그로 줄끼리 엮기(연결 테이블 없음) / 지시문 미저장·사실만 / 임베딩·위계 보류 이유. 06 카드 손질 넷: 유저 프로필=값 있는 것만, 태도 규칙을 stance 원문(프레임 존중·안정형·아부 금지)에 맞춤, 오늘 각본 설명은 02 카드 가리키기로 축약, 진행 중인 일은 끝나는 조건부터 서술. **최근 일기 = 3일치**(구현값 `getRecentDiaries(id, 3)`, 기준값 표에 구현 잔재·결정 필요로 등재, 셀 지도 읽는 조건도 갱신). 06 카드 어휘 '꺼낸다'→'가져온다' 통일. **툴팁 hover→클릭 토글**(바깥 클릭·Esc 닫힘, 열리면 표시가 '닫기'로) — 스크롤 시 사라지던 문제 **허브 피드백 9차(08-25)**: **05 카드 전면 재구성** — 카드 범위를 「대화에 필요한 데이터를 저장하고 주제별로 검색하는 방법」으로 확정하고 저장 항목 10가지를 카드에 명시(검색으로 골라 넣는 4=알게 된 유저 사실·주변 인물·진행 중인 일·지난 일기 / 항상 넣는 6=정체성·관계·유저 프로필·예정된 일·오늘·오늘 메모 / 최근 대화·지금 시각은 매 턴 계산값이라 이 구조 밖, 유저 성향은 운영 전용이라 미포함). 왼쪽=저장하는 데이터 구조 표(라벨 왼쪽 정렬)+두 동작(저장=키로 업데이트할 데이터 찾기 / 검색=태그로 관련 데이터 가져오기), 밤 4단계 처리는 오른쪽 곁상자→왼쪽 본문 이동. 저장 후를 '줄'이라 부르던 표현 폐기(데이터로), 연결 서술을 긍정형으로(이름 태그 하나로 인물·일정·일기 함께 검색), .cut 구분선의 문장 오용·기존 구조 대비 수치(71줄/43줄) 제거. **배치 이름을 새벽 정리로 통일**(밤 정리·새벽 하루 정리 혼용 정리, 실행 시각이 새벽이고 밤은 아직 대화 중이라 — 허브·time-and-memory.md 전체 반영, 코드와 이 파일의 밤 정리 표기는 구현 때 일괄 변경). **01 카드에 9월 달력 신설** — 운영 DB에서 월 리듬 실생성값(이벤트 6개·컨디션 시드 30일)을 읽기 전용으로 가져와 달력으로 그림. 기력=단일 색 점 진하기 3단계, 이벤트·그날 기분은 글자, 날짜 클릭 시 시드 4값(기력·기상·기분·이유) 툴팁. 카드 순서는 05 유지(01~04=캐릭터의 삶과 행동, 05~08=데이터 처리 — 06이 05의 검색 개념을 이어받아 앞이 맞다) **허브 피드백 10차(08-25)**: 05 카드를 저장 관리 방식 기준으로 재편 — 상단에 저장 이유(원문 통째 누적 대신 주제 단위 저장·업데이트로 저장량 최소화·검색 용이) 신설, 오른쪽 상자를 「키와 태그로 관리」(정체성·유저 사실·진행 중인 일 / 주변 인물=이름이 키 / 관계·유저 프로필=키 미리 고정) vs 「태그로만 관리」(지난 일기 / 예정된 일=날짜로 찾는 목록이라 키 없음 / 사람 이름 태그 설명 포함)로 교체, 항상/검색 구분은 프롬프트 기준이라 06 카드로 넘김. 핵심 흐름(검색은 전부 태그, 키는 저장 시 업데이트할 데이터 찾기 전용)을 표 밑에 명시. 용어 둘 변경: 딸린 값→**추가 정보**(코드 필드는 아직 없음, 구현 때 명명), 키 조각→**단어**(time-and-memory.md 전체 동기화, 문장 조각 의미는 유지). 키/태그 회색·자주 범례 폐지(다른 카드의 행위자 색과 충돌, 두 동작 다 k-ac로), 툴팁 있는 요소만 커서 포인터(.tip-host보다 뒤에 선언된 .aside·.step-b의 cursor:default가 이기던 특이도 버그 수정) **허브 피드백 11차(08-26)**: 05 카드 왼쪽을 키 기준 두 갈래로 재구성 — 「키를 가지는 데이터」(두 단어 키=정체성·유저 사실·진행 중인 일 / 사람 이름 키=주변 인물 / 미리 고정된 키=관계·유저 프로필) vs 「태그만 가지는 데이터」(지난 일기·예정된 일+사람 이름 태그)를 오른쪽 회색 상자에서 왼쪽 본문 표로 이동, 오른쪽은 사실뿐·태그 일치 둘만 유지. 저장 이유는 두 문장으로 압축해 카드 하단으로, 표 밑 핵심 흐름 문단은 검색 상자와 겹쳐 삭제. 예시 주어 정정(취소표를 알아보는 쪽은 유저가 아니라 우진). 새벽 정리 상자를 대화하는 동안 기준으로 재작성(문장을 가른다→사실 단위로 나눈다), 툴팁 정리(바뀌는 사실은 키 내부 데이터에 저장 / 내용과 관련된 태그를 뽑아 붙인다 / 후하게·딸려 나온다 표현 교체, 키로 관리하는 데이터·유저 성향 툴팁 삭제, 자른다→개수 제한). time-and-memory.md 동기화: 절 제목 「태그는 관련 주제를 최대한 붙인다」, 4단계 1번을 문장에서 사실을 나누는 것으로, 용어 표의 딸려 나오는지→함께 검색되는지 **허브 피드백 12차(08-26)**: 05 카드를 키·태그 두 꼭지로 재구성 — 저장/검색 보라 상자 둘 폐기, 맨 위에 저장 항목·데이터 한 건·키·태그 관계 소개 후 키 꼭지(용도→데이터 한 건 모양 예시→키를 가지는 데이터 표→키를 짓는 규칙 툴팁, 기존 키 목록 동봉 장치 포함)·태그 꼭지(용도→태그만 가지는 데이터 표→태그를 붙이는 규칙 툴팁) 순서. 예시 캡션을 사실 서술로 정정(유저가 추천한 영화 오디세이를 우진이가 취소표를 구해 보기로 함). 새벽 정리 4단계 상자는 07 카드와 겹쳐 05에서 삭제. 오른쪽 상자 재편 — 이렇게 저장하는 이유를 오른쪽으로 이동, 저장하는 것은 사실뿐→지시는 사실로 바꿔 저장으로 제목·예시 재작성, 검색은 태그 일치 상자는 태그 규칙 툴팁에 흡수(얹는다·상위 태그 표 문장 삭제). 카드 사이 참조 13곳 전부 클릭 이동 링크(.cref)로 + 부드러운 스크롤·고정 바 보정(scroll-margin) **허브 피드백 13차(08-26)**: 05 카드를 좌우 대칭 2단으로 재구성 — 오른쪽 회색 상자 열 폐기(왼쪽만 길어 배치가 어색 + 키·태그 구분이 안 보인다는 지적), 카드를 세 부분으로: 위=저장 이유 두 문장+데이터 한 건 예시 표(항목·값·쓰임 3열)+지시는 사실로 바꿔 저장 노드 / 가로줄 / 아래=키·태그 좌우 대칭(제목 한 글자 크게, 양쪽 다 문단→표→규칙 툴팁 동일 순서). 왼쪽 키 표는 저장 항목별 키 모양 4줄(두 단어·사람 이름·미리 고정·키 없음)로 통합해 태그만 가지는 데이터 표를 흡수, 오른쪽에 검색 과정 예시(뽑은 주제→찾은 데이터) 신설해 저장 예시와 대칭. 이렇게 저장하는 이유는 첫 문단으로, 지시는 사실로 바꿔 저장은 툴팁으로 이동. CSS 추가: .duo(1fr 1fr + 가운데 세로선)·.duo-hd/.duo-s·.hr·.tbl td.tu, 900px 미만에서 세로 적층+쓰임 열 숨김 **허브 피드백 14차(08-26)**: **색 뜻을 카드 8장 공통 6종으로 고정** — 보라=모델이 하는 일 / 회색=코드가 하는 일 / 마젠타=매일 새벽 정리 / 청록=프롬프트에 항상 들어감 / 앰버=대화 주제로 검색해서 넣음 / 남색=규칙과 설명. 히어로 아래 색 뜻 한 줄 신설, 카드별 범례·노드 색을 이 6종으로 재배치(02 각본 생성=마젠타·오늘 메모=보라, 05 규칙 노드=남색, 06 항상 3묶음=청록·검색 묶음=앰버, `.aside` 전체 남색, `.now` 표는 중립색). 05 카드는 「지시는 사실로 바꿔 저장」을 오른쪽 회색 상자로 되돌리고 예시 표 폭 축소, 키 표는 저장 항목 이름만 남기고(두 단어 키=정체성·유저 사실·진행 중인 일 / 사람 이름 키=주변 인물 / 미리 고정된 키=관계·유저 프로필) 설명은 규칙 툴팁으로 이동, 부정문(키가 없다) → 긍정문(업데이트할 일이 있는 항목만 키로 관리), 추가 정보 예시 **흘림 → 언급 선호하지 않음**(비유라 저장값으로 읽히지 않음), 철회가 끝난다 → 값을 고치기만 하면 된다. 설명용 산문·예시의 인물명은 전부 **캐릭터**로(실제 생성값 자리만 이름 유지). 의문형 제목·조건절 라벨 전면 교체(왜 두 단어인가→두 단어 조합의 키, 가져온 것을 몇 줄까지 넣나→프롬프트에 넣는 순서와 개수, 유저가 붙잡으면→유저가 붙잡을 때, 자는 시간에 연락이 오면→자는 시간에 온 연락) + 07·08 카드 단계 제목 12개 명사형 통일. 잔여 금지어도 정리(재료→데이터, 흘러간 기록→지낸 기록, 뱉는다→적는다, 남긴 줄→남긴 내용). **writing 스킬 원본 갱신**(`build-with-ai/plugin/skills/writing/SKILL.md` §1-7) — `~다` 종결·수동태만 막고 있어 의문형·조건절 종결이 반복 통과하던 구멍을 메움, §5 셀프 체크에도 한 줄 추가. 커밋·푸시는 사용자 결정 **허브 피드백 15차(08-26)**: 색 규칙 보정 둘 — 03 답장 텀·04 선톡의 2색 범례 삭제(카드 안에서 실제로는 한 색만 씀. 담긴 사실은 03=붙잡기 판정만 모델 호출(붙잡을 때 상자 각주), 04=판정은 코드·문안은 모델(카드 부제)에 이미 있음), 06 프롬프트 조합 순서의 청록·앰버 → **파랑·붉은색**(뜻은 그대로: 파랑=항상 들어감 / 붉은색=주제로 검색해서 넣음. `--tl`→`--bl`, `--am`→`--rd` 3테마 일괄). 원칙 재확인: 색을 모든 카드에 다 표시할 필요는 없고, **색을 나눠 설명하는 자리에서만 8장이 같은 뜻**을 쓴다. 05 카드에서 임베딩 문장 삭제(「나중에 얹을 자리」 메모가 허브로 넘어와 지금 하는 일처럼 읽힘 — 허브는 완성 설계만, 설계 문서 쪽 메모는 유지). 06 카드 넷: 「나 · 씨앗 정체성」→**캐릭터 정체성**(기본 설정은 01 카드가 코드 결정 4값에 이미 사용 중이라 05 저장 항목 이름으로 통일) / 3번 설명 「유저가 말한 문장 그대로」→「새벽 정리가 사실 한 줄로 정리해 둔 문장 그대로」(저장되는 건 원문이 아니라 추출된 사실 문장) / 개수 상자 「세 가지로 제한」→「세 가지 기준으로 골라 넣는다」(세 건으로 오독됨) + 셋째 라벨 「저장 항목별 개수 상한」, 부산·결혼식 예시 폐기(지난 결혼식을 꺼내는 게 자연스러울 수 있어 차단 예시로 부적합) → 순서 서술로 교체 / 「선톡도 같은 순서」 상자 삭제 → 카드 각주 두 문장으로 흡수(6가지 마지막 문단 사실 보존). **칸별 개수 상한 숫자는 미정** — 기준값 표에 결정 필요로 등재, 칸별로 두는 이유(일기·사실이 자리를 다 차지하는 것 방지)는 time-and-memory.md에 기록 **허브 피드백 16차(08-26)**: 설계 확정 셋 — ① **꺼낸 항목 전부에 갱신 날짜**(「오래된 것에만」의 기준에 근거가 없어 조건 폐기, 오늘 일인지 지난달 일인지는 모델이 지금 시각과 견줘 판단) ② **답장 순서 = 텀 결정 → 생성 → 대기 → 발송**(현 코드는 기다린 뒤 생성해 30분 뒤 답장도 방금 본 결로 쓰임. 미리 만들면 그 사이 일정을 각본에서 읽어 담고, 대기 중 유저가 더 보내면 폐기 후 텀부터 재계산) ③ **닫힌 목록 값은 영어 식별자로 저장**(답장 여건·활동 성격·저장 항목·누구 쪽·출처 — 지금은 `"즉답"|"짬짬이"|"불가"` 한글 리터럴이라 이름 하나 바꾸면 저장값·분기·프롬프트가 같이 걸림. 한글 이름은 표 한곳에서 붙이고, 키의 무엇·태그·값은 모델이 한국어로 짓는 자리라 한글 유지 — 짬짬이→틈틈이 19곳 치환이 이름 표 한 줄로 축소). **최근 대화 40개**를 기준값 표에 구현 잔재·결정 필요로 등재(말풍선 분할 탓에 실제 덮이는 대화 길이가 매번 다름). 개수 상한 숫자는 배포 후 결정으로 확정. 카드 수정: 05에서 「지시는 사실로 바꿔 저장」 상자 삭제(곁가지 설명이 저장 구조 이해를 방해) → 예시 표 단독 배치, 06의 주변 인물·진행 중인 일 설명에 남은 저장 관리 서술을 프롬프트 주입 관점으로 교체, 「이 자리에 두는 이유」를 프롬프트 맨 끝을 가장 강하게 반영한다로 재작성, 캐싱 각주 5문장→4문장, 「3번은」 번호 참조 삭제, 「지금 진행 중인 것부터」 중복 제거, 08 카드 묶음 이름 2개를 06과 동기화. 색: 프롬프트 전용 둘(파랑·붉은색)을 히어로 색 뜻에서 제거해 06 카드에만 유지(한 카드에만 쓰는 색이 맨 위에 있으면 다른 카드에서 찾게 됨), 파랑 `#0B5FFF`·붉은색 `#D91E36`으로 채도 상향(주황으로 보이던 문제), 규칙과 설명 남색 → 코드 회색보다 연한 중립 회색(`.lg-nu` opacity 제거로 두 회색 구분) **허브 피드백 17차(08-26)**: 카드 부제를 한 줄로 통일 — 02(계획·실제 뜻은 노드 셋이 설명)·03(두 단계·태그 둘은 본문 단계가 설명)·07(하는 일 목록은 도는 차례 6단계가 설명)에서 부제 뒷부분 삭제, 04는 부제 뒷부분(보낼지=코드·문장=모델)이 카드 안 다른 데 없어서 각주 첫 문장으로 이동. 01·05·06·08은 그대로. 06 카드 검색 색 **붉은색 → 청록**(파랑과 나란히 놓이는 자리에서 붉은기가 튐 — 둘 다 푸른 계열로 두고 색상만 가름: 파랑 `#0B5FFF` / 청록 `#0B7E96`, 3테마 일괄 + CSS 변수명 `--rd`→`--cy`) **허브 피드백 18차(08-26)**: 07 새벽 정리 카드 전면 재작성 — 열 제목 「도는 차례」→「실행 순서」, 부제는 앞 단계 결과를 다음 단계가 쓰기 때문에 순서대로 실행한다로 축약 / 1단계=코드만 하는 수집임을 명시하고 수집 대상 3종(오늘 메모·대화 원문·각본과 실제 기록)과 참고용 3종(저장 항목 이름·**이미 있는 키**·태그 목록)을 갈라 씀(키 목록이 빠져 있었음) / 범례에 **모델 호출은 단계마다 따로, 평소 하루 4번** 추가 / 2단계 제목을 「오늘 대화에서 기억으로 남길 내용 정리」로, 덮어쓰기 툴팁 삭제(05 카드와 중복) / 3단계=캐릭터 시점 일기임을 제목에 넣고 일기의 쓰임 대신 「일기에 적는 것」(한 일+그때 기분 → 다음 날 이어 말함)만 / 4단계 제목에 「해당하는 날만」 조건 전치, **다음 달 생성 기준 = 이번 달 남은 날 6일 이하** 명시, 한 달치 산출 설명은 01 카드 4단계로 cref / 5단계=미리 쓰는 선톡은 **아침·안부 둘**(나머지 셋은 대화 중 생성), 새벽 정리는 대화 중이어도 **미루지 않고 정시 실행**(하루 경계 5시가 이미 어제를 닫음), 각본 없이 시작한 하루는 임시 각본→정식 각본 교체(지나간 실제 기록은 보존) / 6단계=한 번에 저장하는 이유·실패 원인(응답 형식 불일치·저장 오류)·되돌린 뒤 다음 실행에서 백필까지 서술, 오늘 메모는 그날 하루만 쓰는 자리 / 곁상자 둘: 하루 경계 상자에 「새벽 5시~다음 날 새벽 5시」 첫 줄 추가·「센다」→「간주한다」·각주 둘째 문장 축약, 「건너뛰거나 실패할 때」→**「예외와 실패」**(3일째부터·14일째 기준 명시, 실행하지 못한 날의 원인과 백필, 두 번 처리 방지 이유), 봇 밖/봇 안 각주 삭제(서비스는 배치 하나로 갈 예정이라 설계에 남길 사실 아님). **01 카드 단계 번호 0~3 → 1~4**(혼자 0부터 시작), 달력 col-s의 3단계 참조도 4단계로. 06 카드 청록 채도 상향(회색으로 보임): `#0B7E96`→`#0895B2`, 다크 `#3FB9D1`→`#54D2EA`. 기준값 표에 두 줄 추가 — **05:40 실행 시각**·**6일 런웨이** 둘 다 구현 잔재·결정 필요 **허브 피드백 19차(08-26)**: 07 2단계 「낮에 저장하지 못한 약속」을 **「유저와 정한 약속」**으로 재작성 — 저장 시점이 아니라 유저와 나눈 약속이라는 것이 핵심이고, 실시간 대화에서 저장 항목·키를 판정하지 않고 오늘 메모에 그대로 적어 두는 이유가 08 카드에만 있어 07 툴팁이 맥락 없이 읽혔다(툴팁에 이유 한 문장 + 오늘 메모→예정된 일 예시 추가, 08 6단계 sd에도 같은 이유 명시). 07 3단계 툴팁에 **「대화가 없던 날」** 추가(각본만 보고 짧게 쓰며, 매일 쌓이므로 그날 일기가 그 날짜를 처리했다는 표시가 된다 — `quietDayPrompt`). 「예외와 실패」 부제 「만드는 것이 줄거나, 정해진 시각에 실행하지 못한 경우」→**「일부만 만드는 날, 실행하지 못한 날」**, 세 항목 보강(다음 날 새벽에 빠진 날짜 먼저 채우고 그날 몫 이어서·각본과 선톡은 지난 날짜 몫 미생성 / 답이 없어 각본을 안 만든 날도 유저가 말을 걸면 임시 각본 / 일기가 있으면 그 날짜 건너뜀). **잠수 중에도 일기를 매일 쓰는 비용**을 기준값 표에 결정 필요로 등재(캐릭터당 하루 opus 1콜, 유저 수 늘면 재검토). **아침 선톡의 오늘 먼저 연락 판정 수정을 구현 잔손질에 추가** — `dispatch.ts`가 `today 05:00:00` 이후 유저 메시지로 판정해서, 유저가 새벽 4시 30분에 일어나 말을 걸면 그 대화가 어제 논리일에 들어가 세 시간 뒤 아침 선톡이 그대로 나간다. 이 판정만 직전 유저 발화 시각 기준으로 바꾸고 침묵 일수 계산은 논리일 유지 **허브 피드백 20차(08-26)**: 코드 회색과 규칙·설명 회색이 구분 안 된다는 지적 → 색을 늘리지 않고 **형태로 분리** — `.aside`·`.node.k-ex`를 채움 없는 점선 테두리로 바꾸고 색 점 제거(색 점 + 채운 면 = 행위자, 점선 = 곁설명), 히어로 색 뜻에서 「규칙과 설명」 줄 삭제해 3색만 남김. 02 카드 **현재 시각 줄을 강조 표시로** — `.now td`를 코드 회색 단색 배경 + 반전 글자(테마별 `--on-nu` 신설: 라이트·퍼플 `#fff`, 다크 `#17171B`). **08 카드 제목 의인화 수정** — 「대화 한 번이 지나가는 길」→**「유저의 한마디부터 답장까지」**, 부제에서 앞 카드 참조 삭제, 열 제목 「유저가 보낸 순간부터」→「처리 순서」, 1단계에서 「유저가 나눠 보내는 습관에 맞춰」 삭제, 4단계 툴팁 제목 「같은 답장에 함께 넣는 이유」→「남길 내용을 답장과 같이 쓰는 이유」, 내비게이션 항목도 「한마디부터 답장까지」로(「답장 한 번」은 「답장 텀」과 겹쳐 폐기) **허브 피드백 21차(08-26)**: **09 데이터 지도 카드 신설**(아홉 장) — DB 저장 데이터를 세 묶음(대화에 쓰는 기억 9 / 캐릭터의 삶 3=한 달 이벤트·하루 컨디션·흐름 / 운영과 측정 4)으로만 표시, 세부는 05·06 카드가 담당. 08 카드의 「운영과 측정」 곁상자를 이 카드로 이동. 08 카드 4~6단계 재작성 — 「남길 것」이 무엇인지 안 밝힌 채 4·6단계에 흩어져 있어서 4단계=오늘 메모로 정리해 둘 내용을 답장 끝에 한 줄로 / 6단계=4번에서 적은 내용을 오늘 메모에 저장으로 연결, 맨 앞 표시를 붙이는 방법(정해진 낱말을 대괄호로 묶어 첫 줄)을 툴팁에 추가, 5단계 제목 「다듬어서 정한 시각에 발송」→「최종 답장본 만들기, 정한 시각에 발송」(큰따옴표·쉼표 예시 → 사람이 쓴 말투가 맞는지 확인), 각주의 「몇 분 간격」→3~15분. 곁상자 둘 손질 — 캐싱 설명을 「앞 묶음 두 개」에서 「되풀이되는 부분을 캐싱」으로 축약(앞 카드와 중복), 「새벽 정리는 이 길 밖에서 따로 돈다」 항목 삭제(비유 + 여기서 안 하는 일 서술), 「중간에 끊겼을 때」→「발송 전에 봇이 멈췄을 때」로 제목·본문 구체화(워터마크 = 답장을 보낸 유저 메시지 시각을 대화방마다 기록). **규칙·설명 상자 테두리 점선→실선**(옅은 회색 점선이 구분선으로 안 보임, 배경색이 이미 구분) **구현 세션 나누기(08-26)**: 구현은 세션 하나 = 단계 하나 = 이슈 하나 = 브랜치 하나로 간다. 9개 세션의 범위·참조 절·모델(Fable/Opus)과 세션 시작 프롬프트는 time-and-memory.md 「작업 세션 나누기」 표. 새 세션은 그 표의 몇 번 줄인지 지정받아야 하고, 지정 없이 문서만 읽으면 여러 단계를 한꺼번에 손댄다 **1번 세션 완료(08-26, 이슈 #14, 브랜치 docs/14-skeleton-key-audit)**: 운영 DB 누적 사실 54줄을 뼈대 키에 대조 — 빠진 키는 생활/식사 하나(혼밥 사실 3회 재기록, 각본의 점심 블록이 읽을 값 부재), 변하는 것 표에 추가. 한 줄짜리 미부착 셋(전공·입행 면접·일 습관)은 대화가 짓는 키로 수용, 유년 기억은 빈자리 없음 결정 유지. 줄별 판정은 `docs/skeleton-key-audit-2026-08-26.md`(gitignored, 실데이터 포함). 1단계 남은 확인이 닫혀 남은 설계 없음 — 다음은 2번 세션(스키마 마이그레이션, Opus) **2번 세션 완료(08-26, 이슈 #18, 브랜치 feat/18-schema-migration, PR #19)**: 스키마만 v1으로 옮겨 배포했다. 새 테이블 6개(memory_items·tags·areas·today_notes·day_actuals·pending_replies) 추가, 쓰던 테이블 13개의 컬럼 이름·저장 값 교체, 닫힌 목록 값은 영어 식별자 + CHECK. 테이블 정의를 `TABLES` 표 한곳에 모아 새 DB 생성과 이관이 같은 정의를 쓰고, 부팅 때 `user_version`<1 + 옛 컬럼이 있으면 트랜잭션 하나로 이관 후 1로 올린다(SQLite가 CHECK·UNIQUE를 나중에 못 붙여 테이블 재생성 방식). 판단 근거는 `docs/adr/0001-memory-schema-v1-migration.md`(docs 무시 규칙에 adr만 예외). 운영 사본 리허설 → 배포 전 DB 백업(`/home/ubuntu/db-backups/companion-2026-08-26-pre-v1.db*`) → 재빌드 배포, 운영 확인 완료(user_version 1, integrity ok, fk 0, 옛 테이블 행 수 보존, 새 테이블 비어 있음). 아직 새 테이블을 읽는 코드는 없다. 설계와 다르게 둔 것 셋: `user_profile.age_band` 유지(프롬프트가 읽는 중, 나이대→생년 계산 불가) / 새 프로필 컬럼 NULL 허용 / `schedules.origin`은 전 행 `conversation`, `user_knows`는 전 행 `unknown`으로 백필(3번 세션이 교정). `relationships.state_json`은 `legacy_state_json`으로 남겨 뒀다 — 다음은 3번 세션(데이터 이관, 사람 확인 필요) **3번 세션 완료(08-27, 이슈 #22, 브랜치 feat/22-data-migration, PR #23)**: v1 데이터를 운영 DB에 이관했다. 사람 확인을 마친 이관 후보(`docs/migration/candidates.json`, gitignored)를 `src/tools/migrate-v1-data.ts` apply로 적재하고, 미래 데이터(월 리듬 이벤트·컨디션 시드)는 운영 컨테이너 안 opus 1콜로 재생성(8/28~9/30, 이벤트 8·시드 34). 판단 지점 둘만 멈춰 처리: ① diff-legacy — 8/26 스냅샷 이후 운영에 쌓인 13건 중 9건 반영(기존 항목에 병합 3·신규 6), 나머지 4건은 이미 있는 항목과 open_loops 처분표가 담당해 제외 ② regen 플랜 — 제약 8개는 전부 충족했으나 추석 연휴(9/24~26)에 평일 업무 시드가 들어가 있어, 연휴 시드 3일과 가족 통화 이벤트 날짜(9/26→9/25), 시드 사유 1건을 손으로 교정한 뒤 적용. 최종 검증: memory_items 73(identity 36 / ongoing 4 / relationship 5 / user_fact 28), tags 110, areas 11, cast 13, 시드 7/1~9/30 하루 1건, 미래 일정 9건, user_version 2·integrity ok·fk 0, 봇 로그 error 패턴 0. 배포 전 백업 `/home/ubuntu/db-backups/companion-2026-08-27-pre-data.db`. open_loops 처분·리허설 기록 전문은 `docs/migration/REVIEW.md`(gitignored). 아직 새 테이블을 읽는 코드는 없다 — 다음은 4번 세션(새 모듈 셋과 잔손질, Opus) **4번 세션 완료(08-27, 이슈 #25, 브랜치 refactor/25-reply-timing-modules, PR #26)**: 새 모듈 넷을 만들고 답장 순서를 바꿨다. `src/labels.ts`(닫힌 목록 값 = 영어 식별자, 한국어 이름은 한곳) / `src/reply-timing.ts`(답장 텀 표 한 장 + 예외 둘, 붙잡기 판정 1콜, 35분 상한·12분 붙잡기 제한 폐기) / `src/pending.ts`(만든 답장을 행으로 남겨 정한 시각 발송, 재시작 이어받기, 추가 발화 시 폐기) / `src/memory.ts`(키로 저장·태그로 검색, 읽는 쪽 연결은 다음 세션). **답장 순서 = 텀 결정 → 생성 → 대기 → 발송** — 대기 90초 초과면 도착 예정 시각 한 줄을 코드가 실시간 꼬리에 적어 준다(RESPONSE_TIMING 상수는 삭제). 함께 정리: 아침 선톡 게이트를 논리일 05:00 → 직전 유저 발화 4시간 창 / `attention_override` 테이블·`capture.ts` 삭제 / 스키마 v3(pending_replies.kind·areas.note + plan_json 두 태그 식별자 치환). 검증은 타입체크 + 새 DB 스모크(스키마·기억 저장/검색·붙잡기 기록·꼬리 줄·대기 답장 발송/재무장/폐기·텀 표 각 칸). **남은 공백 둘**: ① 대화 중 사실 포착이 비어 있다 — `pending_replies.note_to_save` 배관만 깔고 지금은 null, 답장 프롬프트가 남길 한 줄을 만드는 건 5번 세션 ② 붙잡기 판정 콜은 로컬에 유효한 키가 없어 실제 응답 미검증(실패 시 일정 유지 폴백까지만 확인). 다음은 5번 세션(프롬프트 조립, Opus)
- [ ] ~~케미 축 단계 값 재점검~~ → **MBTI 우선으로 확정(08-21)**: 코드가 정하는 건 성별(유저 반대)·나이 범위(유저 ±4살, 성인 가드)·MBTI(16종 무작위)·애착유형(안정형 고정) 넷뿐이고, 대화 성격은 MBTI에 맞춰 생성 콜이 채운다. 선호 학습 대상도 케미 축(243조합)에서 MBTI로 이동. character-design §2·§7은 대체 표기. 이어서 확정: 성별=유저 반대 / 나이=유저 ±4살(성인 가드) / MBTI는 16종 안 추림, 애착유형과 묶지 않고 생성 콜이 한 사람으로 빚음 / 말투는 성격에서 도출하되 값으로 저장 / 취향→취미 / **뼈대는 세 층에서 두 층으로**(자리만 만들어두는 층 폐지, 학번·유년 장면 자리 삭제 — 나이에서 계산되는 값은 생년월일에서 계산해 말한다는 규칙으로 대체)
- [ ] 프로브 플래그 (교체 제안 / 침묵일)
- [ ] **적응형 답장 속도**(유저 리듬 맞춤·향후): 유저의 요일×시간대별 답장 속도 학습 + 세션 템포 반영 → '빠른 경로'(즉답·개인 틈틈이·진입창)의 대기 구간을 빠른 쪽으로 당김(상한만↓·하한 유지·당김폭 캡). **이벤트가 상한 — 불가·공적 불변.** 빠른 경로에만 곱하는 단일 boost 계수(이벤트 분기와 독립). 설계는 architecture.md·positioning.md#7에 기록. 데이터 쌓이면 구현
- [x] 분석 스크립트 — `src/tools/analyze.ts` 일별 신호 집계(선제시작·활동시간대·감정어·선톡 반응) + `--tag` LLM 정밀 태깅 (07-11)
- [x] 데모 백스테이지 뷰 — `src/tools/render-map.ts` 실DB→HTML(관계도·기억·온도·일기). **산출물은 사생활 데이터 포함, 커밋 금지** (07-11)

## 실행

```bash
yarn            # 설치
cp .env.example .env   # TELEGRAM_BOT_TOKEN, ANTHROPIC_API_KEY 채우기
yarn dev        # 로컬 기동 (long polling)
```

## 규칙

- **문체**: 사람이 읽는 한국어 산문(설계 문서·README·이슈·PR 본문·유저에게 보내는 답)은 `writing` 스킬을 먼저 로드하고 쓴다. 코드·주석은 대상이 아니다.
- **공개 톤**: 이 repo는 공개 전환 예정이다. 특정 서비스명 비교·개인 맥락·실제 대화 로그를 커밋하지 않는다. 사적 맥락은 전부 LOCAL-CONTEXT.md(gitignored)로.
- **보안**: `.env`, `*.db`, `logs/`, `data/` 커밋 금지 (gitignore 반영됨).
- **코드**: TypeScript strict + ESM(import에 `.js` 확장자), named export, kebab-case 파일명, any 금지. 커밋은 Conventional Commits 한글.
- **작업 단위**: 새 작업은 이슈부터 만들고 main에서 브랜치를 판다(`타입/이슈번호-영문요약`). 끝나면 PR 본문에 `Closes #이슈번호`를 넣어 올리고 `gh pr merge <번호> --merge --delete-branch`로 머지·브랜치 삭제까지 한 세션에서 끝낸다. 커밋 메시지의 `(#번호)`는 링크만 걸고 이슈를 닫지 않는다. 머지 전 사람 확인이 필요한 자리는 time-and-memory.md 「작업 세션 나누기」에 적혀 있다.
- **설계 일관성**: 캐릭터 stance(프레임 존중·안정형·아부 금지·무근거 핑 금지)는 character-design.md §5가 원본. 코드의 stance 문자열과 문서가 어긋나면 문서 기준으로 맞춘다.
- **모델**: 기본 `claude-sonnet-5` (환경변수 `MODEL`로 교체 가능).

## 아키텍처 요약

```
[Telegram] ←→ src/bot.ts (grammY long polling, 유저 분할입력 디바운스(유저별 텀 학습 20~40s) + 분할 발화 + 답장 순서 = 텀 결정(src/reply-timing.ts) → 생성 → 대기 → 발송(src/pending.ts) + 말풍선 후처리 polishBubble(src/bubble-polish.ts — 큰따옴표·한글 사이 쉼표 제거, 물음표 보정은 의문 전용 어미·끝머리 의문사만 보수적으로) + 부팅 시 놓친 답장 복구 recoverMissedReplies=워터마크 중복방지+최근 3h만(자정 경계 버그 회피) + 답장 앞머리 시스템 태그 일괄 제거 parseReplyTags(겹친 태그도 [남음] 인식) + 대화 기록 시간 마커 주입 toTurns/timeMarkerFor + 발송 종류 로그 kind=reply|recover|morning|away|catchup|goodnight + 선톡 틱 간 chat 단위 상호 배제 acquireProactive(대기 답장 있으면 선톡 접음) + 발송 자리는 setPendingSender로 pending.ts에 등록)
                ├─ /start → src/character.ts (현재: 고정 대표 캐릭터 DAEPYO_BIBLE. 랜덤 생성 코드는 확장용으로 보존)
                ├─ 메시지 → src/context.ts (**안정도 순 3층 조립** buildSystemBlocks: 불변층=바이블·정체성·규칙 / 일간층=유저 프로필·관계(압축 서술)·인물·아크·일정·일기 / 실시간 꼬리=오늘 각본·지금 시각·말투 판정 — 말투는 currentSpeechLevel이 최근 발화로 반말/존댓말 판정해 존댓말 회귀 방지, '냐' 어미 거친 반말 금지. 변하는 값은 전부 꼬리에 — 앞 두 층은 캐시) → src/llm.ts (앞 두 층 끝 cache_control 1h, usage cw/cr 로그) → 응답
                ├─ src/reply-timing.ts — 답장 텀 단일 소스: 두 태그 표 한 장(즉답 0~2분 짧은 쪽 몰림 / 틈틈이 개인 20s~2.5m·사회 30s~4m·공적 1~8m / 불가는 그 일정이 끝날 때)과 예외 둘(자는 중·이미 붙잡혀 접은 상태=즉답). 개인·사회 불가에 온 메시지만 붙잡기 판정 1콜(16토큰, 실패 시 일정 유지) → 붙잡혔으면 개인=취소·사회=미룸을 day_actuals에 기록. 상한 없음(3시간 회의면 3시간 뒤)
                ├─ src/pending.ts — 대기 답장: 만든 답장을 pending_replies에 남겨 정한 시각에 발송(재시도 3회), 부팅 때 resumePendingReplies가 이어받음, 유저가 말을 더 보내면 dropPendingReplies로 폐기 후 텀부터 재계산. 발송 함수는 bot.ts가 setPendingSender로 주입(순환 참조 회피)
                ├─ src/memory.ts — 새 저장 구조의 저장·검색: saveMemory(키=item_type·owner·area·subject로 자리 찾기)·searchMemories(태그 일치 → 항목 우선순위 → 최신순 → 항목별 상한)·memoryBlock(갱신 날짜 포함)·saveTodayNote. **읽는 쪽(context.ts·nightly.ts) 연결은 5~7번 세션 몫 — 지금은 자립 모듈**
                ├─ src/labels.ts — 닫힌 목록 값의 영어 식별자 ↔ 한국어 이름 단일 표(답장 여건·활동 성격·저장 항목·주인·출처·유저 인지). plan_json 안 두 태그는 DB CHECK가 못 닿아 3중 방어(읽는 자리 toResponsiveness/toActivityCategory + 생성 직후 normalize + v3 마이그레이션 migratePlanTags)
                ├─ src/day-plan.ts — 하루 각본: 밤 정리가 새 날 것을 사전 생성(대화가 먼저면 lazy). 그날 컨디션 시드(월 리듬)를 읽어 컨디션→기상→활동 연결, 어제 일기 실제 여파가 시드보다 우선. 블록별 **답장 여건(instant/intermittent/unavailable) × 활동 성격(personal/social/official)** — 두 축은 직교. 저장은 영어 식별자, 한국어 이름은 src/labels.ts 한곳(RESPONSIVENESS_NAME·ACTIVITY_CATEGORY_NAME). category는 옵셔널, 없으면 activity로 추론(blockCategory: 회의·시험·발표·업무=공적 / 친구·가족·병원·학원·회식=사회 / 나머지 솔로=개인)
                ├─ src/life-plan.ts — 월 리듬(중간 지평): 한 달치 이벤트 + 매일 컨디션 시드를 미리 생성(ensureMonthPlan), 항상 ~1개월 런웨이 롤링(ensureRhythmRunway). 이벤트↔컨디션 인과(회식 다음날 피곤 등), 급변 없는 파도. Opus, 밤 정리 배치가 생성
                ├─ src/nightly.ts — 밤 정리(새벽 5시 컷오프): gather/apply 분리. 기본=외부 스케줄러가 tools/nightly-read·write로 수행, 폴백=봇 내 05:40 크론(API·opus). 월 리듬(rhythmNeeded 신호→rhythm 출력)도 이 배치가 생성, 아크는 달력 경계(월요일=주·1일=달)에서 기존 흐름+최근 일기로 이어쓰기 갱신. 침묵 quiet/dormant면 일기·시드·리듬만(각본·선톡 생략), reconnect면 저녁 재연결 문안만 — gather가 silenceTier/silenceDays를 노출하고 apply가 게이트를 강제(외부 경로가 백오프를 몰라도 안전)
                ├─ src/proactive-policy.ts — 선제 발화 관제탑: silenceState(무응답 논리일 수→normal/quiet/reconnect/dormant)·proactiveAllowed. 유저 메시지가 오면 즉시 평상 복귀
                ├─ src/dispatch.ts — 아침 안부 디스패처(3분 틱, LLM 0): 밤에 준비된 문안을 창 안 발송, 유저가 그날 먼저 연락 시 skip, 침묵 백오프면 skip(재연결 문안은 통과). 창 놓치면 유예(창 종료 +90분, 시간대별 상한 11/14/22시) 내 발송, 넘기면 폐기 사유+시도 횟수(attempts/last_error) 기록
                ├─ src/followup.ts — 침묵 팔로업(15분 틱, 크론 0-4,8-23시): 관제탑 통과 시에만. 낮=유저 무응답 ~2h+각본 전환점이면 근황(연속 무응답 taper), 밤(새벽 2~5시)=유저가 잔다 말 없이 1h+ 잠수하면 굿나잇 1회. 경과시간은 Date.now 기준(getKstNow().getTime()은 +9h라 오산)
                └─ src/presence.ts — 자리 비움 예고(10분 틱, 크론 7-23시, 유저 발화 ≤4h일 때만 — 자기 발화로 창이 열리던 체인 차단): 곧 긴 불가(운동·샤워·외출, ≥20분)로 들어가기 직전/연속 불가 경계에서 "이제 러닝 갈게요 답 늦어요"/"막 왔어요 씻고 올게요" 선톡(블록당 1회, 하루 4회 상한). 예고하고 나간 일정이 끝나면(그 사이 유저 무응답 시) "이제 끝났어" 복귀 알림. 공적 불가(회의·시험)는 "끝나고 연락" 톤. **개인·사회 불가는 유저가 붙잡으면 reply-timing이 판정 한 콜로 가려 개인=취소·사회=미룸을 day_actuals에 기록→그 블록 즉답, 공적은 못 접음.** 접은 블록에는 예고 스킵(isHeldNow). 막연한 침묵→'알고 하는 기다림'
[SQLite] src/db.ts — TABLES 표 한곳에 정의, SCHEMA_VERSION 3(v1=새 테이블·컬럼 이관 / v3=pending_replies.kind·areas.note + plan_json 두 태그 식별자 치환) / characters / relationships(관계 + 이관 대기 중인 legacy_state_json) / memory_items(기억 한 건, 키=item_type·owner·area·subject) / tags(태그→기억·일기·일정) / areas(캐릭터가 쓰는 영역 이름) / today_notes(그날만 쓰는 메모) / day_actuals(각본과 달라진 블록) / pending_replies(만들어 둔 답장을 정한 시각에 발송, status=waiting|sent|failed|superseded) / user_preferences(매칭 전용, 캐릭터에 주입 금지) / user_profile(preferred_name·gender·birth_year·job·region + 옛 age_band) / diary_entries / day_plans(하루 각본, made_by=nightly|ondemand) / day_seeds(컨디션 시드: energy·wake_hint·mood·reason) / schedules(일정, owner=char|user, origin·user_knows·parent_kind) / cast_members(관계도, owner=char|user, relation_label·recent_note) / arcs(period=year|season|month|week) / scheduled_sends(미리 만든 선톡, kind=morning|checkin) / send_failures(kind=away|catchup|goodnight) / recovery_marks(복구 중복 방지 워터마크) / messages(기록, role=user|assistant, sent_at, meta.kind=reply|recover|morning|away|catchup|goodnight)
[모델] 실시간 대화=sonnet / 일기·추출·각본·월 리듬·아크·캐릭터 생성=opus(MODEL_DEEP) / 밤 정리 기본 경로는 외부 스케줄러라 API 미사용
```

핵심 설계 불변: 바이블(씨앗 정체성)은 매칭 순간 생성 후 큰 정체성 불변 — 자잘한 디테일은 대화로 쌓이되(누적 정체성) 한번 나온 것은 일관 유지(어긋나는 발명·유저 훼이크 추종 금지) / 선호는 매칭 시스템만 읽고 캐릭터에게 비공개 / 선제 연락은 근거 있을 때만 하루 1통, 유저가 오래 무응답이면 물러난다(3일 조용→14일차 저녁 재연결 1통→침묵) / 이별은 비가역 / 컨디션은 매일 독립 주사위가 아니라 월 단위로 미리 깔린 리듬(이벤트 인과+파도), 실제 산 하루가 그 위를 덮어씀 / **답장 텀은 블록의 두 태그(답장 여건 × 활동 성격) 표 한 장에서만 나온다 — 이벤트별 예외처리 금지(스파게티 방지). 개인=붙잡으면 취소, 사회=양해 구해 미룸, 공적=못 접음.** / 답장은 텀을 정한 뒤 바로 만들어 두고 정한 시각에 내보낸다(만드는 시점 ≠ 도착 시점, 대기 90초 넘으면 도착 예정 시각을 꼬리에 적어 줌)
