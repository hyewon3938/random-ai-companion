# random-ai-companion — 작업 가이드

나와 같은 시간을 살고 먼저 물어봐주는 AI 대화 상대 (캐릭터는 유저가 /start에서 적은 입력으로 만든다). 컨셉·가설·설계 총론은 [README.md](README.md), 규모와 비용 검토는 [scaling.md](scaling.md), 실행 환경과 외부 서비스 구성은 [infra.md](infra.md)가 단일 소스다.

세부 설계 문서는 `docs/` 아래에 두고 커밋하지 않는다(gitignored). 타겟·어필 포인트는 `positioning.md`, 설계 논리·층은 `architecture.md`, 캐릭터 속성 체계는 `character-design.md`, 수집·생성 데이터 카탈로그는 `data-model.md`. 로컬 전용 컨텍스트는 `LOCAL-CONTEXT.md` — 새 세션은 그 파일부터 읽을 것. 배포한 뒤 확인할 것은 `LOCAL-WATCH.md`에 모은다.

## 상태 (갱신 의무 — 세션이 끝나기 전 여기와 LOCAL-CONTEXT.md를 최신화한다)

> 항목은 제목·날짜·이슈 번호 한 줄로 적고, 판단 근거와 검증 내역은 이슈·PR 본문에 둔다. 끝난 항목은 여기서 지운다 — 무엇을 했는지는 닫힌 이슈로 찾는다.
>
> 여기에는 아직 안 한 일만 둔다. 배포한 뒤 확인할 것은 `LOCAL-WATCH.md`(gitignored)로 옮겼다 — 실제 대화·슬랙 게시와 대조해 적는 자리라 공개 repo에 둘 수 없고, 배포마다 늘어나서 이 파일의 크기 상한을 밀어낸다. **배포 전 필수**는 전달이 목적인 메모라 그 일을 마치면 지운다.

### 남은 작업

- [ ] **프롬프트·기억 구조 재설계** (08-19 착수) — 설계 원본은 repo 루트 `time-and-memory.md`, 설계 허브는 `docs/data-map.html`(gitignored). 세션 표는 time-and-memory.md 「작업 세션 나누기」. 9번까지와 11번은 끝났고, 10번(이슈 #262)의 남은 확인과 12번은 9/8에 V3로 합쳤다.
- [ ] **V2 — 유저 생성 캐릭터·기억 통합 (확정 08-27, 이슈 #28)**: 랜덤 매칭을 없애고 유저가 선택지와 서술형 입력으로 캐릭터를 만든다. 기억은 `memory_items` 세 항목(fact·ongoing·person) × 주인 둘, 관계는 `relationships` 컬럼 일곱 항목. 설계 원본은 time-and-memory.md 「V2」 절과 ADR 0002~0004, 저장 구조는 erd.md, 모듈 배치는 modules.md. 확정 뒤 덧붙인 세부는 time-and-memory.md 「확정 뒤에 덧붙인 것」에 있다.
- [ ] **V3 — 관계를 쌓는 캐릭터 (착수 09-08, 묶음 이슈 #326)**: 기획의 중심을 캐릭터의 하루에서 유저와 쌓는 관계로 옮긴다. 설계 원본은 repo 루트 `relationship.md`이고 구현 세션은 그 문서만 읽고 코드로 옮긴다. 결정은 ADR 0015~0017, 세션별 할 일과 시작 프롬프트는 `docs/v3-sessions.md`. 사람이 멈추는 자리는 #326 본문에 있다. 다음은 구현 6.
- [ ] **문화 스크립트 — 남은 둘 (09-12, 이슈 #405·#411)**: 표 `culture_scripts`와 새벽 정리 수집이 달마다 걸린 일의 절차와 번호 붙은 진행 중인 일을 싣는 #411까지 09-12에 운영에 들어갔다. 외부 지시서 패치도 같은 날 적용해서 다음 회차부터 실제 캐릭터의 월 리듬에 걸린다. 설계 원본은 `docs/service-strategy.md` §3. 명절이 달력에서 걸리게 하는 일은 #415(PR #416)로, 2027년 공휴일 표는 #423으로 끝냈다. 이미 만들어진 달에 소급 적용하는 일은 09-12에 끝냈다 — 이도훈에게 결혼 하나가 걸렸는데 결혼식 날짜가 DB 어디에도 없어서 한 행도 넣지 않았고, 날짜가 정해진 뒤에 다시 볼 항목은 LOCAL-WATCH.md에 있다. 남은 둘은 세션을 가른다 — 2단계(`character.ts`에서 이 표 읽기), 연말정산처럼 날짜가 아니라 1~3월이라는 시기에 걸리는 일의 조건 정하기
- [ ] **선톡과 답장을 연락 하나로 묶기 (09-11 설계, 설계 원본은 repo 루트 `outgoing.md`)** — 내보내는 연락 전부를 계기 발생 → 시각 정하기 → 메시지 작성 → 내보내기 네 단계로 지나가게 하고, 예약 표 한 장에 연락 한 건을 한 행으로 적는다. 종류마다 다른 것은 함수 3개(연락 계기·발송 판정·메시지 작성)와 값 2개(하루 상한 여부·유저가 말을 더 보낼 때 버릴지)로만 둔다. 적용 순서 8단계와 세션 9개 표는 그 문서에 있고, ADR 2건(표 하나로 묶기, 계기 조건과 발송 조건 갈라내기)이 세션 3·6에 딸린다. 이슈는 세션마다 따로 만든다
- [ ] **9/11 대화에서 받은 것 (이슈 #388·#390·#397)** — 기억 줄에 그 일이 있었던 시점 적기(#388), 오늘 의도로 둔 것을 답장이 썼는지 적기(#390), 게시함에서 프롬프트에 들어간 기억·일정 찾기(#397). 연락 개편과 독립이라 먼저 한다. #397은 기억 줄 모양을 건드려 #388과 한 세션이고, #390은 같은 플러팅·같은 물음이 하루에 두 번 나간 원인이라 #385 배포와 함께 봐야 한다.
- [ ] **답장이 자리 비운 구간 안에서 나가는 것 (09-10, 이슈 #381·#382)** — 블록이 끝나기 직전에 온 말에 표에서 뽑은 텀을 더하면 발송 시각이 다음 불가 구간 안으로 넘어간다. 텀을 경계에서 자르는 것이 #381, 다음 블록을 답장 프롬프트에 알리는 것이 #382이고 #382는 말투 규칙과 부딪히는 선을 먼저 정해야 한다. 아직 손대지 않았다
- [ ] **일정 중간에 연락해 달라는 요청 받기 (08-31 미뤄 둠)** — 집에 도착하면 한 번 연락해 달라는 말처럼 불가 구간 안의 한 시점을 짚는 요청을 받아 그 시각에 보내는 자리. 보낼 시각은 모델이 지어내지 않고 각본 블록의 경계에서 고른다. 9/7에 배포한 #308의 약속 신호가 같은 자리를 쓴다. 유저가 부탁하고 캐릭터가 하겠다고 답하면 그 약속으로 걸리므로, 며칠 보고 따로 만들 것이 남는지 정한다
- [ ] **캐릭터 일과의 시작과 끝을 대화 중에 알리기 (09-10, 이슈 #376)** — 출퇴근처럼 매일 하는 일의 시작과 끝에 한 줄 남긴다. 유저와 대화하고 있을 때만 나가고 하루 선톡 상한 밖이다. 각본 블록에 일과의 시작·끝 표시가 없는 것과, 직업이 자유 서술이라 코드가 보는 이름이 직장인을 전제하면 안 되는 것이 정할 자리다
- [ ] **선톡·특수 답장 문안에서 규칙층과 겹치는 줄 걷어내기 (08-29, 이슈 #89)** — 상황 문단이 `buildSystemBlocks` 3층을 그대로 타는데도 말풍선 개수·재촉 금지를 다시 적어 규칙이 서로 희석된다. 이슈 본문의 8곳 표는 낡았고 지금은 17곳이다
- [ ] **태그 이름을 저장 시점에 기존 목록과 대조하기 (08-29, 이슈 #142)** — 같은 주제에 이름이 둘 붙으면 한쪽 이름으로 검색할 때 다른 쪽 기억이 안 걸린다. 막고 있던 #139·#140이 끝나 지금 할 수 있다
- [ ] **적응형 답장 속도 (향후)** — 유저의 요일×시간대별 답장 속도를 배워 빠른 경로의 대기 상한만 당긴다. 불가·공적 구간은 그대로다. 설계는 architecture.md·positioning.md#7에 있다. 데이터 쌓이면 구현
- [ ] **답장에서 생각 과정을 끌지 정하기 (08-30 관측, 이슈 #214·#218)** — 끄는 값을 안 실으면 sonnet은 생각을 켜고, 그 텍스트는 안 돌아오면서 토큰만 출력으로 나간다. `llm_calls`에 쌓인 값의 분포를 보고 정한다

## 실행

```bash
yarn            # 설치
cp .env.example .env   # TELEGRAM_BOT_TOKEN, ANTHROPIC_API_KEY 채우기
yarn dev        # 로컬 기동 (long polling)
```

## 규칙

- **문체**: 사람이 읽는 한국어 산문(설계 문서·README·이슈·PR 본문·유저에게 보내는 답)은 `writing` 스킬을 먼저 로드하고 쓴다. 코드·주석은 대상이 아니다.
- **공개 톤**: 이 repo는 공개되어 있다. 특정 서비스명 비교·개인 맥락·실제 대화 로그를 커밋하지 않는다. 사적 맥락은 전부 LOCAL-CONTEXT.md(gitignored)로.
- **보안**: `.env`, `*.db`, `logs/`, `data/` 커밋 금지 (gitignore 반영됨).
- **코드**: TypeScript strict + ESM(import에 `.js` 확장자), named export, kebab-case 파일명, any 금지. 커밋은 Conventional Commits 한글. `src/*.ts`는 파일 맨 위 주석으로 시작한다 — 첫 줄이 한 문장 요약이고, 빈 주석 줄을 두고 그 아래에 자세한 설명을 적는다. 그 파일을 고치면 이 주석도 같은 커밋에서 고친다.
- **작업 단위**: 새 작업은 이슈부터 만들고 main에서 브랜치를 판다(`타입/이슈번호-영문요약`). 커밋을 마치면 PR을 올리기 전에 `/review-code`로 브랜치 diff를 리뷰하고, 나온 것은 같은 브랜치에서 고치거나 안 고친 까닭을 PR 본문에 적는다. 그다음 PR 본문에 `Closes #이슈번호`를 넣어 올리고 `gh pr merge <번호> --merge --delete-branch`로 머지·브랜치 삭제까지 한 세션에서 끝낸다. 머지한 자리에서 `v3-readme.md`에 한 줄 붙인다 — 지금 동작·V2와 다른 점·이슈 번호, 리드미에 안 갈 작업이면 「안 넣는 것」에. 커밋 메시지의 `(#번호)`는 링크만 걸고 이슈를 닫지 않는다. 끝난 작업은 이 파일에 남기지 않는다 — 무엇을 했는지는 닫힌 이슈로 찾으므로, 이슈 없이 작업하면 그 기록이 사라진다. 머지 전 사람 확인이 필요한 자리는 time-and-memory.md 「작업 세션 나누기」에 적혀 있다.
- **배포 뒤 확인 목록**: 배포하거나 머지를 마치면 `LOCAL-WATCH.md`(gitignored)에 묶음 하나를 새로 만든다 — 제목, 날짜와 이슈·PR 번호, 무엇을 바꿨는지 한 문단, 확인할 것을 하나씩 체크박스로. **작업이 끝난 뒤 대화하다가 볼 것이 더 생기면 그때마다 그 묶음에 항목을 붙인다.** 어느 묶음에도 안 걸리면 「묶음 없이 볼 것」에 적는다. 확인한 항목은 체크하고 무엇을 보고 그렇게 판단했는지 그 묶음의 확인 결과에 한 줄 남긴다. 다 끝난 묶음을 지우는 정리는 사용자가 요청할 때만 한다.
- **이 파일 크기**: 세션마다 컨텍스트에 통째로 들어가는 지시서라 짧게 유지한다. 상태 절에는 아직 안 한 일만 두고, 무엇을 왜 그렇게 했는지는 이슈·PR 본문에 남긴다. 배포 기록은 서버 경로가 들어가서 공개 repo에 두지 않고 LOCAL-HISTORY.md에 적는다. 상한 14,000자는 `.githooks/pre-commit`이 강제하고 12,000자를 넘으면 경고한다. 새 작업 디렉터리에서는 `git config core.hooksPath .githooks`를 한 번 실행해 켠다. 막히면 남은 작업의 설명을 깎지 말고 끝난 항목을 지운다 — 지울 것이 없으면 상한을 올린다.
- **자동 검사**: main 푸시와 PR마다 GitHub Actions가 `yarn typecheck`·`yarn test`를 돌린다(`.github/workflows/ci.yml`). 모델을 부르는 표기 규칙 평가(`yarn eval`)는 호출 비용이 붙고 통과율이 그날 응답에 따라 흔들려서 자동으로 돌리지 않는다. `src/prompts/reply.ts`·`src/eval/`·`src/reply-signal.ts`를 고친 PR에는 `eval` 라벨을 붙인다 — 라벨은 형식 레인만 돌린다(케이스 5개 × 3회, 호출 15건). 답이 객체로 오는지와 늘 넣는 칸이 오는지만 가르고, 표기·메모·플러팅 통과율은 그날 모델 답에 따라 흔들려서 합격 판정에 넣지 않는다. 케이스 28개 전체(`yarn eval`, 기본 5회면 호출 140건)는 말투 규칙을 크게 손본 뒤 Actions 탭에서 손으로 돌린다(`.github/workflows/eval.yml`). 테스트 파일은 `test/`에 둔다. `scripts/gen-modules.mjs`가 `src/` 아래 `.ts` 전부를 색인으로 훑는다.
- **설계 일관성**: 캐릭터 stance(프레임 존중·상대 자체를 좋아함·신경 쓰는 티)는 character-design.md §5가 원본. 코드의 stance 문자열과 문서가 어긋나면 문서 기준으로 맞춘다.
- **출력 규칙 단일 소스**: 캐릭터가 내보내는 모든 글에 공통으로 적용할 규칙(태도·대화 규칙·표기·말의 결)은 `src/prompts/reply.ts`의 고정 문안에서만 관리한다 — PERSON·SPEECH·EXEMPLARS·OUTPUT_FORMAT·FACT_CARE·NOTE_RULE. EXEMPLARS는 캐릭터 고유값이 없는 목표 말투 예시로, 금지 목록만으로는 안 잡히는 결(말풍선 끝 어미·추측형 말끝·반응어)을 보여준다. 캐릭터마다 다른 값으로 두면 생성 결과에 따라 규칙이 흔들리므로 정체성 항목에 넣지 않는다. 선톡 문안 프롬프트 9곳(nightly 아침·안부, followup 근황·굿나잇·달래기·살피기, presence 자리비움·복귀, glance 틈새 한 줄)도 전부 `buildSystemBlocks`(3층+상황 문단)를 타므로 같은 규칙이 자동으로 들어간다. 큰 결을 바꿀 일이 생기면 이 블록들을 고친다. 다만 답장이 내보내는 **형식**(JSON 객체)은 규칙층이 아니라 `src/reply-signal.ts`가 갖는다 — 선톡 문안 9곳은 같은 3층을 쓰되 자기 형식으로 답하므로, 규칙층에 넣으면 두 형식이 부딪힌다.
- **영역 표와 파일 색인**: 어느 파일이 어느 영역인지는 areas.md의 영역 표가 단일 소스다. `node scripts/gen-modules.mjs`가 그 표를 아키텍처 요약에 옮겨 적고, 각 파일 맨 위 주석의 첫 줄로 areas.md의 파일 색인을 다시 쓴다. 손으로 고치지 않고, 요약 주석을 고치거나 파일을 새로 만들면 표에 넣은 뒤 이 명령을 돌려 함께 커밋한다. 색인이 밀렸거나 요약 주석이 없거나 표에 없는 파일이 있으면 커밋 훅이 막는다.
- **모델**: 기본 `claude-sonnet-5` (환경변수 `MODEL`로 교체 가능).

## 아키텍처 요약

코드는 같이 바뀌는 정도와 의존 방향으로 묶은 영역 7개로 관리한다. 어떤 변경이 어느 영역으로 가는지, 고칠 때 같이 볼 곳과 리팩토링 순서는 areas.md에 있고, 파일마다 한 줄 요약도 그 문서의 파일 색인에 있다. 실행 단위로 묶어 본 그림과 답장 경로 흐름도는 modules.md, 표와 컬럼의 뜻은 erd.md에 있다.

모델은 실시간 대화에 sonnet, 일기·추출·각본·월 리듬·아크·캐릭터 생성에 opus(`MODEL_DEEP`)를 쓴다. 새벽 정리의 기본 경로는 외부 스케줄러가 맡아서 API를 쓰지 않는다.

<!-- modules:start -->

> 이 표는 `node scripts/gen-modules.mjs`가 areas.md의 영역 표에서 옮겨 쓴다. 손으로 고치지 않는다. 파일마다 한 줄 요약은 areas.md의 파일 색인에 있다.

| 영역 | 여기로 오는 변경 | 파일 |
| --- | --- | --- |
| 1. 기반과 저장 | 기준값·이름표·시각 계산, 표와 컬럼, 모델 호출 방식 | config, kst, labels, thresholds, db, db/*, llm |
| 2. 기억 | 무엇을 저장하고 무엇을 꺼내 쓰는지 | memory, recall, tag-pick, user-profile |
| 3. 캐릭터의 삶 | 캐릭터 생성, 삶의 큰 흐름, 월 리듬, 하루 각본, 일정 | character, arcs, life-plan, day-plan, schedule-dedupe |
| 4. 대화 생성 | 무슨 말을 어떤 텀으로 하는지, 오늘 먼저 말을 걸어도 되는지 | context, context/*, prompts/reply, prompts/relationship, turns, reply-signal, reply-ask, reply-compose, reply-promise, user-state, relationship-update, speech-level, reply-timing, proactive-policy |
| 5. 실행과 발송 | 텔레그램과 주고받기, 예약 발송, 선톡 틱 4개, 크론표 | index, bot, pending, presence, glance, followup, dispatch, proactive-send |
| 6. 새벽 정리 | 하루를 닫는 배치 전부 | nightly, relationship-stage, prompts/nightly, nightly-trace, tools/nightly-read, tools/nightly-write, tools/run-nightly |
| 7. 관측과 운영 | 슬랙 게시, 피드백 수집, 손으로 돌리는 도구, 평가, 테스트, CI | trace, trace/*, reply-trace, feedback, tools/*, eval/* |

<!-- modules:end -->

핵심 설계 불변: 캐릭터는 유저 입력으로 생성(V2)하고 생성 때 정한 큰 정체성은 불변(origin=creation 행은 저장 함수가 수정 거부) — 자잘한 디테일은 대화로 쌓이되(누적 정체성) 한번 나온 것은 일관 유지(어긋나는 발명·유저 훼이크 추종 금지) / 잘 통하는 것은 관계 컬럼(rapport)으로 캐릭터에게 공개하고, user_preferences는 유저 단위 선호 자리 / 선제 연락은 근거 종류 넷(의도·일정·달래기·약속) 가운데 하나를 반드시 갖고 관계 단계가 정한 하루 상한(1단계 4통~4단계 6통, 의도 근거는 따로 1~3통) 안에서 나가며, 유저가 오래 무응답이면 물러난다(3일 조용→14일차 저녁 재연결 1통→침묵) / 이별은 비가역 / 컨디션은 매일 독립 주사위가 아니라 월 단위로 미리 깔린 리듬(이벤트 인과+파도), 실제 산 하루가 그 위를 덮어씀 / **답장 텀은 블록의 두 태그(답장 여건 × 활동 성격) 표 한 장에서만 나온다 — 이벤트별 예외처리 금지(스파게티 방지). 개인=붙잡으면 취소, 사회=양해 구해 미룸, 공적=못 접음.** / 답장은 즉답·틈틈이면 텀을 정한 뒤 바로 만들어 정한 시각에 내보내고, 불가 구간이면 만들지 않고 구간 끝에 깨어나 쌓인 메시지를 한 번에 읽고 몰아 답한다(몰아 답장은 만드는 시점=보내는 시점이라 일정을 막 끝내고 이제 봤다는 전제가 사실이 된다)
