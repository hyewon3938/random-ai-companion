# random-ai-companion — 작업 가이드

나와 같은 시간을 살고 먼저 물어봐주는 AI 대화 상대 (캐릭터는 유저가 /start에서 적은 입력으로 만든다). 컨셉·가설·설계 총론은 [README.md](README.md), 규모와 비용 검토는 [scaling.md](scaling.md), 실행 환경과 외부 서비스 구성은 [infra.md](infra.md)가 단일 소스다.

세부 설계 문서는 `docs/` 아래에 두고 커밋하지 않는다(gitignored). 타겟·어필 포인트는 `positioning.md`, 설계 논리·층은 `architecture.md`, 캐릭터 속성 체계는 `character-design.md`, 수집·생성 데이터 카탈로그는 `data-model.md`. 로컬 전용 컨텍스트는 `LOCAL-CONTEXT.md` — 새 세션은 그 파일부터 읽을 것.

## 상태 (갱신 의무 — 세션이 끝나기 전 여기와 LOCAL-CONTEXT.md를 최신화한다)

> 항목은 제목·날짜·이슈 번호 한 줄로 적고, 판단 근거와 검증 내역은 이슈·PR 본문에 둔다. 끝난 항목은 여기서 지운다 — 무엇을 했는지는 닫힌 이슈로 찾는다.
>
> 항목은 **남은 작업 → 배포·확인 대기** 순으로 지난다. 배포·확인 대기에는 아직 볼 것이 남은 것만 둔다. **배포 전 필수**는 전달이 목적인 메모라 그 일을 마치면 지운다. 확인할 것이 다 끝나면 항목을 지운다. 배포하고 일주일이 지나도 그 상황이 오지 않아 못 본 항목은 남은 작업으로 되돌릴지 지울지 그 자리에서 정한다. 볼 것이 하나도 안 남았는데 아직 여기 있으면 그건 정리가 밀린 것이다.

### 남은 작업

- [ ] **프롬프트·기억 구조 재설계** (08-19 착수) — 설계 원본은 repo 루트 `time-and-memory.md`, 설계 허브는 `docs/data-map.html`(gitignored). 세션 표는 time-and-memory.md 「작업 세션 나누기」. 9번까지와 11번은 끝났고, 10번(이슈 #262)의 남은 확인과 12번(마무리)은 9/8에 V3로 합쳤다(10번은 V3 첫 캐릭터로, 12번은 기획 B에서).
- [ ] **V2 — 유저 생성 캐릭터·기억 통합 (확정 08-27, 이슈 #28)**: 랜덤 매칭을 없애고 유저가 선택지와 서술형 입력으로 캐릭터를 만든다. 기억은 `memory_items` 세 항목(fact·ongoing·person) × 주인 둘, 관계는 `relationships` 컬럼 일곱 항목. 설계 원본은 time-and-memory.md 「V2」 절과 ADR 0002~0004, 저장 구조는 erd.md, 모듈 배치는 modules.md. 확정 뒤 덧붙인 세부는 time-and-memory.md 「확정 뒤에 덧붙인 것」에 있다.
- [ ] **V3 — 관계를 쌓는 캐릭터 (착수 09-08, 묶음 이슈 #326)**: 기획의 중심을 캐릭터의 하루에서 유저와 쌓는 관계로 옮긴다. 설계 원본은 repo 루트 `relationship.md`(기획 B, 이슈 #327)이고 구현 세션은 그 문서만 읽고 코드로 옮긴다. 결정은 ADR 0015~0017, 세션별 할 일과 시작 프롬프트는 `docs/v3-sessions.md`(gitignored). 사람이 멈추는 자리는 #326 본문에 있다. 다음은 구현 1(스키마 v9).
- [ ] **실제 작품의 없는 장면을 말하지 않게 하기 (09-05, 이슈 #287)** — 각본에 실제 작품 블록이 들어갈 때 답장 경로 밖에서 작품마다 한 번 검색해 짧은 사실 카드를 만들고, FACT_CARE에 적힌 것만 말하라는 규칙을 더한다. 말투 작업 뒤에 한다
- [ ] **일정 중간에 연락해 달라는 요청 받기 (08-31 미뤄 둠)** — 집에 도착하면 한 번 연락해 달라는 말처럼 불가 구간 안의 한 시점을 짚는 요청을 받아 그 시각에 보내는 자리. 보낼 시각은 모델이 지어내지 않고 각본 블록의 경계에서 고른다. 이슈 #210·#212가 배포됐으니 새 규칙으로 만든 각본을 하루 보고 착수한다. 9/7에 배포한 #308의 약속 신호가 같은 자리를 쓴다. 유저가 부탁하고 캐릭터가 하겠다고 답하면 그 약속으로 걸리므로, 며칠 보고 따로 만들 것이 남는지 정한다
- [ ] **적응형 답장 속도**(유저 리듬 맞춤·향후): 유저의 요일×시간대별 답장 속도 학습 + 세션 템포 반영 → '빠른 경로'(즉답·개인 틈틈이·진입창)의 대기 구간을 빠른 쪽으로 당김(상한만↓·하한 유지·당김폭 캡). **이벤트가 상한 — 불가·공적 불변.** 빠른 경로에만 곱하는 단일 boost 계수(이벤트 분기와 독립). 설계는 architecture.md·positioning.md#7에 기록. 데이터 쌓이면 구현
- [ ] **답장에서 생각 과정을 끌지 정하기 (08-30 관측)** — `src/llm.ts`가 끄는 값을 안 실으면 sonnet은 생각을 켜고, 그 텍스트는 안 돌아오면서 토큰만 출력으로 나간다. 상한을 다 쓰고 144자만 돌아온 날이 있어 2,000으로 올렸다(이슈 #214). 9/1 답장 4건은 전부 `blocks=thinking:1,text:1`에 출력 167~369토큰·글자 59~124자다. 끌지는 #218(PR #273)을 배포해 `llm_calls`에 값이 며칠 쌓인 뒤 분포를 보고 정한다

### 배포·확인 대기

> 무엇을 왜 그렇게 했는지는 이슈·PR 본문에 있다. 여기에는 아직 볼 것만 남긴다.
> 호출의 중단 사유·블록 종류와 예고를 접은 사유는 9/4 배포부터 DB에 쌓인다(`llm_calls`·`trace_events`).

- [ ] **새벽 자리 비움 예고와 아침 선톡의 컨디션 서술 (09-09 배포, 이슈 #329, PR #330)** — 자리 비움 예고 틱이 07~23시만 돌아 자정에 시작하는 불가 블록이 예고 없이 지나갔다. 크론을 하루 전체로 열었다. 아침 선톡은 상태만 말하고 늦게 잔 까닭은 상대가 물을 때 답하며, 답장 규칙층의 탓하지 않기를 컨디션까지 넓혔다. relationship.md의 틈새 한 줄 조건은 남은 시간 30분·단계 조건 제거·공적 블록 제외로 고쳤고 코드는 구현 5가 만든다. 볼 것: 자정에 시작하는 불가 블록에 예고가 나가는지, 아침 선톡이 늦게 잔 까닭을 유저와 엮지 않는지
- [ ] **유저와 만나는 일정 금지와 웃음 표기 개수 (09-08 배포, 이슈 #322·#324, PR #323·#325)** — 프롬프트 3곳과 새벽 정리 외부 지시서에 상대와 만나는 이벤트·블록·계획을 만들지 않는 규칙을 넣었고, 9/26의 만나는 이벤트와 월 흐름의 약속 문장은 운영 DB에서 손으로 고쳤다. 웃음은 ㅋ 3개 이상·ㅎ 2개 이상만 쓰는 규칙을 규칙층에 넣었다. #321도 같이 올렸다. 볼 것: 9/9부터 각본에 상대와 만나는 블록이 없는지, 10월 리듬과 주 흐름에 상대와 만날 계획이 없는지, 답장에 ㅋ·ㅋㅋ·ㅎ 하나가 안 나오는지
- [ ] **답이 끊긴 날에도 낮에 한 통 더 (09-07 배포, 이슈 #314, PR #315)** — 답이 없는 첫날부터 아침 선톡 네 시간 뒤에 근황 한 통이 나가고, 이틀째는 아침 한 통과 점심 한 통이 나간다. 점심 문안은 새벽이 아니라 팔로업 틱이 그 시각의 각본을 보고 만든다. 볼 것: 어제 대화가 끊긴 날 근황이 아침 선톡보다 먼저 나가지 않는지, 이틀째 12:05~12:50에 점심 한 통이 나가고 그날 근황이 겹치지 않는지, 사흘째부터 조용한지
- [ ] **슬랙 게시에서 상대 상태·연락 약속·바뀐 자리 보기 (09-07 배포, 이슈 #312, PR #313)** — 답장 게시에 상대 상태 줄이 이전 → 지금으로 적히고, 약속은 잡힘·미룸·보냄·거둠 같은 단계마다 그 약속을 한 답장의 스레드에 달리며, 새벽 정리 게시의 관계·기억·진행 중인 일은 전문 하나 안에서 빠진 말을 `[-…-]`, 더한 말을 `{+…+}`로 표시한다. 볼 것: 상태가 바뀐 답장 게시에 이전 값이 같이 적히는지, 약속 행이 걸릴 때와 나갈 때 스레드에 단계가 붙는지, 달래기·약속 문안 머리에 근거가 붙는지, 9/8 새벽 게시가 두 전문 대신 표시 하나로 나오는지
- [ ] **캐릭터가 말한 연락 약속을 코드가 지키기 (09-07 배포, 이슈 #308, PR #310)** — 답장 신호의 promise 칸에 무엇을 마치고 연락하겠다고 했는지 적히면 코드가 각본 블록 경계에서 시각을 골라 `pending_replies`에 promise 행을 걸고, 그 시각에 모델을 다시 불러 보낼지까지 정하게 한다. 붙잡기 판정 결과도 답장 프롬프트에 상황 문단으로 들어간다. 볼 것: 약속을 말한 답장 뒤 promise 행이 생기고 블록 끝 시각에 나가는지, 즉답 구간의 약속이 다음 블록 끝으로 잡히는지, 같은 시각에 몰아 답장 깨우기가 있으면 약속 행이 폐기되는지, 취소한 일정에 가겠다고 말하지 않는지
- [ ] **상대의 지금 상태를 답장마다 판정하기 (09-07 배포, 이슈 #309, PR #311, ADR-0014)** — 태그 고르기와 나란히 도는 판정 호출이 상대 상태를 한 줄로 정해 바뀔 때만 `relationships`의 컬럼 4개에 적고, 답장 프롬프트 끝에 붙인다. 캐릭터 때문에 안 좋은 상태면 웃음 표기를 안 쓰고, 그 채로 30분 조용하면 달래기 선톡이 한 번 나가며, 새벽 정리가 값을 받아 비운다. 볼 것: 대화 뒤 컬럼 4개가 채워지고 상태가 안 바뀐 턴에는 갱신이 없는지, 프롬프트에 상태 절이 붙는지, 안 좋을 때 ㅋㅋㅋ가 안 나오는지, 달래기가 30분 뒤 한 번만 나가는지, 다음 새벽 정리 뒤 값이 비고 이유가 feelings나 cautions에 남는지
- [ ] **말투 예시와 상대 자체를 좋아하는 태도 (09-06 배포, 이슈 #281·#285, PR #283)** — 목표 말투 예시 13쌍과 상대 자체를 좋아하는 상태·기다렸다는 말·가벼운 질투·대화가 이어지게 닫기를 규칙층에 넣었고(ADR-0012), 캐릭터 정체성 값 4개(대화 성격·애착유형·웃음·호감)도 9/6에 같은 결로 바꿨다. 볼 것: 왜 좋으냐는 물음에 편하다는 말 없이 자기 쪽 이유로 답하는지, 말풍선 끝 어미가 셋 이상 겹치지 않는지, 오·ㅎㅎ가 안 나오는지, 시키는 말로 대화를 닫지 않는지, `llm_calls`의 읽기 방식 분포에서 본문 그대로가 늘지 않는지
- [ ] **자다 깬 시각과 어젯밤 잠 (09-06 배포, 이슈 #288·#289, PR #291·#292)** — 자다 깬 답장은 각본과 달라진 기록의 깬 시각을 그대로 쓰고, 피곤한지는 잠든 시각에 6시간을 더한 기준과 기상 시각을 견줘 정한다. 볼 것: 자다 깬 답장이 깬 시각을 어림하지 않는지, 각본 프롬프트에 어젯밤 잠 절이 붙는지, 늦게 자고 늦게 일어난 주말에 피곤한 티가 없는지
- [ ] **연락 텀과 오랜만에 온 연락 (09-06·09-07 배포, 이슈 #284·#316, PR #286·#318)** — 캐릭터 마지막 말과 유저 다음 말 사이가 3시간 이상이면 그 텀을 적고 성격대로 한 마디 티를 내게 한다. 여섯 시간 넘게 조용하다 낮에 온 연락과 스무 시간 넘게 지나 온 연락은 긴 텀으로 세어 기다렸다는 말을 붙인다. 볼 것: 하루 종일 조용하다 저녁에 말을 걸면 기다렸다는 말이 나오는지, 자고 일어난 아침 인사와 짧은 텀의 바빴던 날에는 안 나오는지, 문장이 매번 같지 않은지, 몇 마디 뒤에 꺼낸 뒤 같은 말을 되풀이하지 않는지
- [ ] **진행 중인 일을 각본대로 옮기기 (09-05 배포, 이슈 #276, PR #279)** — 유저가 아는 캐릭터 쪽 일만 행 번호를 달고 각본에 들어가고, 새벽 정리가 어제 각본의 그 블록을 보고 값을 한 걸음 옮긴다. 끝나면 사실로 옮긴다. 9/6 각본에 이미 다 읽은 책이 다시 들어가 옮기는 논리가 틀렸을 수 있다. 볼 것: 9/7 새벽 트레이스에 진행 중인 일 두 건 다 글이 붙고 값이 옮겨지는지, 오늘 각본의 진행 중인 일 블록에 source ongoing과 번호가 붙는지, 완독 뒤 사실로 옮겨져 각본에서 빠지는지
- [ ] **캐릭터가 말한 일정 시각 (09-05 배포, 이슈 #277·#278, PR #280·#282)** — 답장이 이번에 정해 말한 자기 일정의 시각을 오늘 메모로 남기고, 새벽 정리가 이미 저장된 일정 줄의 시각을 그 값으로 고친다. 9/5 각본은 대화에서 나온 두 시 반을 일기를 거쳐 받았지만 일정 32번 줄의 time_hint는 아직 오후다. 볼 것: 캐릭터가 시각을 말한 날 `today_notes`에 그 값이 적히는지, 새벽 정리 트레이스에 일정 시각 고침이 남는지, 다음 날 각본에 같은 시각으로 들어가는지
- [ ] **근황 선톡의 침묵 조건 (09-05 배포, 이슈 #274, PR #275)** — 유저와 캐릭터 둘 다 마지막 말이 네 시간 지나야 나간다. 볼 것: 자리 비움 복귀 인사 뒤 네 시간 안에 근황이 안 나가는지
- [ ] **9/4에 고친 넷 (09-04 배포, 이슈 #264·#265·#267·#269)** — 볼 것: 유저 말이 없을 때의 자리 비움 예고에도 자리를 비운다는 말이 있는지, 새벽 정리가 유저 사실의 앞 값을 지우지 않고 합치는지, 결과 문장에 건너뛴 일정 건수가 붙는지, 발송이 끊겼을 때 답장이 1·2·5·10분에 다시 나가고 선톡이 다음 틱에 다시 나가는지.
- [ ] **9/1~9/2 배포분 (이슈 #238·#241·#249·#251·#252·#255·#257·#259)** — 볼 것: 몰아 답장 기록의 시간 표시, plain·salvage·empty 답장의 재생성, 웃음이 붙는 자리, 발송 포기 뒤 복구 틱 재발송, 자리 비움 예고에 무슨 일인지와 답 미룬다는 말, 자는 시간 첫 답장 2분 30초, 갓 시작한 대화의 형식 깨짐 빈도. 9/8에 캐릭터가 정태오로 바뀌어 처음부터 다시 쌓인다
- [ ] **배포했는데 그 상황이 아직 안 온 것 (08-30~09-01, 이슈 #145·#164·#174·#194·#216)** — 틈틈이 블록 붙잡기, 자리비움 예고 접기, 객체 밖 신호, 달래기 선톡. 발현 0건. 조건이 오는 날 본다

## 실행

```bash
yarn            # 설치
cp .env.example .env   # TELEGRAM_BOT_TOKEN, ANTHROPIC_API_KEY 채우기
yarn dev        # 로컬 기동 (long polling)
```

## 규칙

- **문체**: 사람이 읽는 한국어 산문(설계 문서·README·이슈·PR 본문·유저에게 보내는 답)은 `writing` 스킬을 먼저 로드하고 쓴다. 코드·주석은 대상이 아니다.
- **공개 톤**: 이 repo는 공개되어 있다. 특정 서비스명 비교·개인 맥락·실제 대화 로그를 커밋하지 않는다. 사적 맥락은 전부 LOCAL-CONTEXT.md(gitignored)로.
- **보안**: `.env`, `*.db`, `logs/`, `data/` 커밋 금지 (gitignore 반영됨).
- **코드**: TypeScript strict + ESM(import에 `.js` 확장자), named export, kebab-case 파일명, any 금지. 커밋은 Conventional Commits 한글. `src/*.ts`는 파일 맨 위 주석으로 시작한다 — 첫 줄이 한 문장 요약이고, 빈 주석 줄을 두고 그 아래에 자세한 설명을 적는다. 그 파일을 고치면 이 주석도 같은 커밋에서 고친다.
- **작업 단위**: 새 작업은 이슈부터 만들고 main에서 브랜치를 판다(`타입/이슈번호-영문요약`). 커밋을 마치면 PR을 올리기 전에 `/review-code`로 브랜치 diff를 리뷰하고, 나온 것은 같은 브랜치에서 고치거나 안 고친 까닭을 PR 본문에 적는다. 그다음 PR 본문에 `Closes #이슈번호`를 넣어 올리고 `gh pr merge <번호> --merge --delete-branch`로 머지·브랜치 삭제까지 한 세션에서 끝낸다. 커밋 메시지의 `(#번호)`는 링크만 걸고 이슈를 닫지 않는다. 끝난 작업은 이 파일에 남기지 않는다 — 무엇을 했는지는 닫힌 이슈로 찾으므로, 이슈 없이 작업하면 그 기록이 사라진다. 머지 전 사람 확인이 필요한 자리는 time-and-memory.md 「작업 세션 나누기」에 적혀 있다.
- **이 파일 크기**: 세션마다 컨텍스트에 통째로 들어가는 지시서라 짧게 유지한다. 상태 절에는 아직 볼 것이 남은 항목만 두고, 무엇을 왜 그렇게 했는지는 이슈·PR 본문에 남긴다. 배포 기록은 서버 경로가 들어가서 공개 repo에 두지 않고 LOCAL-HISTORY.md에 적는다. 상한 12,000자는 `.githooks/pre-commit`이 강제하고 10,000자를 넘으면 경고한다. 새 작업 디렉터리에서는 `git config core.hooksPath .githooks`를 한 번 실행해 켠다. 막히면 상한을 올리지 말고 상태 절을 먼저 줄인다.
- **자동 검사**: main 푸시와 PR마다 GitHub Actions가 `yarn typecheck`·`yarn test`를 돌린다(`.github/workflows/ci.yml`). 모델을 부르는 표기 규칙 평가(`yarn eval`)는 호출 비용이 붙고 통과율이 그날 응답에 따라 흔들려서 자동으로 돌리지 않는다. `src/prompts/reply.ts`·`src/eval/`·`src/reply-signal.ts`를 고친 PR에는 `eval` 라벨을 붙여 평가를 돌리고 통과율을 확인한 뒤 머지한다(`.github/workflows/eval.yml`). 테스트 파일은 `test/`에 둔다. `scripts/gen-modules.mjs`가 `src/` 아래 `.ts` 전부를 색인으로 훑는다.
- **설계 일관성**: 캐릭터 stance(프레임 존중·신경 쓰는 티·무근거 핑 금지)는 character-design.md §5가 원본. 코드의 stance 문자열과 문서가 어긋나면 문서 기준으로 맞춘다.
- **출력 규칙 단일 소스**: 캐릭터가 내보내는 모든 글에 공통으로 적용할 규칙(태도·대화 규칙·표기·말의 결)은 `src/prompts/reply.ts`의 고정 문안에서만 관리한다 — PERSON·SPEECH·EXEMPLARS·OUTPUT_FORMAT·FACT_CARE·NOTE_RULE. EXEMPLARS는 캐릭터 고유값이 없는 목표 말투 예시로, 금지 목록만으로는 안 잡히는 결(말풍선 끝 어미·추측형 말끝·반응어)을 보여준다. 캐릭터마다 다른 값으로 두면 생성 결과에 따라 규칙이 흔들리므로 정체성 항목에 넣지 않는다. 선톡 문안 프롬프트 7곳(nightly 아침·안부, followup 근황·굿나잇·달래기, presence 자리비움·복귀)도 전부 `buildSystemBlocks`(3층+상황 문단)를 타므로 같은 규칙이 자동으로 들어간다. 큰 결을 바꿀 일이 생기면 이 블록들을 고친다. 다만 답장이 내보내는 **형식**(JSON 객체)은 규칙층이 아니라 `src/reply-signal.ts`가 갖는다 — 선톡 문안 6곳은 같은 3층을 쓰되 자기 형식으로 답하므로, 규칙층에 넣으면 두 형식이 부딪힌다.
- **영역 표와 파일 색인**: 어느 파일이 어느 영역인지는 areas.md의 영역 표가 단일 소스다. `node scripts/gen-modules.mjs`가 그 표를 아키텍처 요약에 옮겨 적고, 각 파일 맨 위 주석의 첫 줄로 areas.md의 파일 색인을 다시 쓴다. 손으로 고치지 않고, 요약 주석을 고치거나 파일을 새로 만들면 표에 넣은 뒤 이 명령을 돌려 함께 커밋한다. 색인이 밀렸거나 요약 주석이 없거나 표에 없는 파일이 있으면 커밋 훅이 막는다.
- **모델**: 기본 `claude-sonnet-5` (환경변수 `MODEL`로 교체 가능).

## 아키텍처 요약

코드는 같이 바뀌는 정도와 의존 방향으로 묶은 영역 7개로 관리한다. 어떤 변경이 어느 영역으로 가는지, 고칠 때 같이 볼 곳과 리팩토링 순서는 areas.md에 있고, 파일마다 한 줄 요약도 그 문서의 파일 색인에 있다. 실행 단위로 묶어 본 그림과 답장 경로 흐름도는 modules.md, 표와 컬럼의 뜻은 erd.md에 있다.

모델은 실시간 대화에 sonnet, 일기·추출·각본·월 리듬·아크·캐릭터 생성에 opus(`MODEL_DEEP`)를 쓴다. 새벽 정리의 기본 경로는 외부 스케줄러가 맡아서 API를 쓰지 않는다.

<!-- modules:start -->

> 이 표는 `node scripts/gen-modules.mjs`가 areas.md의 영역 표에서 옮겨 쓴다. 손으로 고치지 않는다. 파일마다 한 줄 요약은 areas.md의 파일 색인에 있다.

| 영역 | 여기로 오는 변경 | 파일 |
| --- | --- | --- |
| 1. 기반과 저장 | 기준값·이름표·시각 계산, 표와 컬럼, 모델 호출 방식 | config, kst, labels, thresholds, db, db/*, llm |
| 2. 기억 | 무엇을 저장하고 무엇을 꺼내 쓰는지 | memory, recall, tag-pick, user-profile |
| 3. 캐릭터의 삶 | 캐릭터 생성, 삶의 큰 흐름, 월 리듬, 하루 각본, 일정 | character, arcs, life-plan, day-plan, schedule-dedupe |
| 4. 대화 생성 | 무슨 말을 어떤 텀으로 하는지, 오늘 먼저 말을 걸어도 되는지 | context, context/*, prompts/reply, turns, reply-signal, reply-ask, reply-compose, reply-promise, user-state, relationship-update, speech-level, reply-timing, proactive-policy |
| 5. 실행과 발송 | 텔레그램과 주고받기, 예약 발송, 선톡 틱 3개, 크론표 | index, bot, pending, presence, followup, dispatch, proactive-send |
| 6. 새벽 정리 | 하루를 닫는 배치 전부 | nightly, prompts/nightly, nightly-trace, tools/nightly-read, tools/nightly-write, tools/run-nightly |
| 7. 관측과 운영 | 슬랙 게시, 피드백 수집, 손으로 돌리는 도구, 평가, 테스트, CI | trace, trace/*, reply-trace, feedback, tools/*, eval/* |

<!-- modules:end -->

핵심 설계 불변: 캐릭터는 유저 입력으로 생성(V2)하고 생성 때 정한 큰 정체성은 불변(origin=creation 행은 저장 함수가 수정 거부) — 자잘한 디테일은 대화로 쌓이되(누적 정체성) 한번 나온 것은 일관 유지(어긋나는 발명·유저 훼이크 추종 금지) / 잘 통하는 것은 관계 컬럼(rapport)으로 캐릭터에게 공개하고, user_preferences는 유저 단위 선호 자리 / 선제 연락은 근거 있을 때만 하루 1통, 유저가 오래 무응답이면 물러난다(3일 조용→14일차 저녁 재연결 1통→침묵) / 이별은 비가역 / 컨디션은 매일 독립 주사위가 아니라 월 단위로 미리 깔린 리듬(이벤트 인과+파도), 실제 산 하루가 그 위를 덮어씀 / **답장 텀은 블록의 두 태그(답장 여건 × 활동 성격) 표 한 장에서만 나온다 — 이벤트별 예외처리 금지(스파게티 방지). 개인=붙잡으면 취소, 사회=양해 구해 미룸, 공적=못 접음.** / 답장은 즉답·틈틈이면 텀을 정한 뒤 바로 만들어 정한 시각에 내보내고, 불가 구간이면 만들지 않고 구간 끝에 깨어나 쌓인 메시지를 한 번에 읽고 몰아 답한다(몰아 답장은 만드는 시점=보내는 시점이라 일정을 막 끝내고 이제 봤다는 전제가 사실이 된다)
