# V3 리드미에 쓸 변경 기록

V3 기능이 다 들어간 뒤에 리드미를 새로 쓰려고 그때까지의 변경을 모아 두는 문서다. 한 줄에 지금 이렇게 동작한다는 것과 V2는 이랬다는 것, 이슈 번호 세 가지를 적는다. 절 제목은 지금 리드미의 것을 그대로 쓰고, 지금 리드미에 들어갈 자리가 없는 것은 「관계 단계」 절에 모은다. 9/8부터 쌓는다. 그 앞의 변경은 지금 리드미에 이미 들어 있다.

쌓는 규칙 3가지:

- 머지한 자리에서 한 줄 붙이고, 이미 적은 줄은 다시 정리하지 않는다.
- 이슈 본문에 있는 설명을 옮겨 오지 않는다. 자세한 것은 이슈 번호로 찾는다.
- V3 리드미를 다 쓰면 이 문서는 통째로 지운다.

<br>

## 관계 단계

지금 리드미의 절 9개에 들어갈 자리가 없어서 새 절로 뽑는다.

- 유저와의 사이를 1~4단계로 두고, 단계마다 캐릭터가 하는 말의 수위와 아직 안 하는 표현이 정해진다. 관계 표에 단계 번호와 그 단계에 들어간 날짜를 적고, 처음·반응 점수·관계 의도·열림 신호 표 4개를 새로 만들었다.
  V2에는 단계가 없어서 사이가 가까워져도 말의 수위가 그대로였다. #331 #353
- 답장 프롬프트의 불변층에 지금 단계에서 하는 것과 아직 안 하는 것을 적은 블록이 들어가고, 실시간 꼬리에 며칠째인지와 이번 답장에서 쓴 플러팅을 적는 절이 붙는다.
  V2의 프롬프트에서 관계를 설명하는 자리는 관계 데이터 7칸뿐이었다. #353
- 단계를 넘길 문턱은 코드가 세고, 찬 날만 모델이 넘길지 정한다. 유저가 그날 첫 메시지를 건 날은 문턱에서 빼고, 캐릭터가 자기 얘기를 먼저 연 날을 함께 센다.
  V2에는 넘어갈 문턱이 없었다. #355 #393
- 캐릭터를 끝내는 도구가 활성 캐릭터를 종료 상태로 바꾸고 걸려 있던 예약 답장을 폐기한다. 대화를 읽는 함수가 채팅 번호만 보던 것을 캐릭터 번호까지 보게 고쳐서, 같은 채팅에서 캐릭터를 새로 시작해도 이전 대화가 섞이지 않는다.
  V2는 캐릭터를 끝내는 자리가 없었다. #331
- 유저와는 메시지로만 이어진 사이라 캐릭터가 만나는 자리를 먼저 꺼내지 않고, 같이 하자는 말은 각자 따로 하고 서로에게 알리는 약속으로 만든다. 같은 요일에 각자 서점에 다녀와 책 하나씩 추천하기 같은 것이고, 유저가 먼저 만나자고 하면 그 말은 받는다. 관계 의도를 적는 프롬프트와 답장 프롬프트에 같은 선을 그었다.
  V2에는 관계 의도가 없었고, 만나는 계획을 막는 선은 캐릭터의 삶을 만드는 프롬프트 3곳에만 있었다. #417

<br>

## 1. 캐릭터 생성

- 온보딩에서 말투와 원하는 방식(주 결 1개·섞는 결 0~2개), 결점까지 8칸을 받는다. 원하는 방식과 결점은 생성 때 정한 값으로 굳어져 대화로 바뀌지 않고, 성격·말투·첫 인사에 섞여 들어간다.
  V2는 5칸을 받았고 원하는 방식과 결점이 없었다. #333
- 출발 설정을 소개로 만나 몇 번 본 사이이고 캐릭터가 먼저 빠진 상태로 고정했다. 관계 행은 1단계로 시작하고 첫 인사도 1단계 수위를 넘지 않는다.
  V2는 출발 설정이 캐릭터마다 달랐다. #333
- 쓰고 있는 캐릭터가 있으면 새로 만들 수 없고, 끝낸 캐릭터만 있을 때 다시 만들 수 있다.
  V2는 이 확인이 없었다. #333

<br>

## 2. 캐릭터의 연락 방식

- 캐릭터가 먼저 거는 말은 근거 4가지(오늘 의도·일정·달래기·약속) 가운데 하나를 반드시 갖고 나가고, 하루 상한은 관계 단계를 따라 1단계 4통에서 4단계 6통까지 늘어난다.
  V2는 근거 없이도 나갔고 상한이 하나였다. #357
- 새벽 정리가 적어 둔 오늘 의도 가운데 아직 안 쓴 줄이 있으면 그것으로 먼저 말을 건다.
  V2에는 의도라는 것이 없었다. #357
- 아침에 보내는 용건 없는 연락은 2단계부터 나가고 1단계에서는 막는다. 보내지 않기로 판정한 자리는 같은 아침에 다시 묻지 않는다.
  V2는 단계와 무관하게 나갔다. #359
- 상대가 자기 일로 안 좋은 상태에서 30분 답이 없으면 살피는 연락 한 통이 나간다. 그 일이 마음에 걸린다는 것만 전하고 상대 말을 되풀이하거나 조언하지 않으며, 하루 상한 밖이라 잠 블록에도 나간다.
  V2에는 달래기 하나뿐이었다. #361
- 답할 수 없는 시간이 이어질 때는 첫 구간이 끝나는 자리에 사이 예고를, 마지막 구간이 끝나는 자리에 복귀 인사를 보낸다. 몰아서 답한 뒤에는 그 구간이 끝날 때 다시 말을 건다.
  V2는 구간마다 복귀 인사가 나가서 집에 왔다는 말이 하루에 여러 번 반복됐다. #341
- 자리를 비운 사이에 뭐 해?·아직이야? 같은 확인하는 말이 오면 지금 하는 일과 끝나는 시각을 한 줄로 답한다. 블록마다 1번이고 하루 상한 밖이다.
  V2는 구간이 끝날 때까지 아무 답이 없었다. #339
- 자리 비움 예고를 거는 크론이 하루 24시간을 돌아서 자정에 시작하는 일정에도 예고가 나간다.
  V2는 07~23시만 돌았다. #329
- 아침에 거는 말은 컨디션만 전하고, 늦게 잔 까닭은 상대가 물을 때 답한다.
  V2는 컨디션을 말하면서 유저와 새벽까지 이야기해서라고 엮었다. #329
- 하루 각본을 만드는 프롬프트에서 답할 수 없는 구간을 적극적으로 넣으라는 지시를 상한과 같은 방향으로 맞췄다.
  V2는 그 지시와 상한 3개가 서로 반대여서 하루 9개까지 나왔다. #335
- 각본 블록의 활동 성격이 정해진 3가지 밖의 값으로 저장되던 것을 막았다. 답장 텀 표가 모르는 값을 받으면 맞는 칸을 못 찾는다.
  V2부터 있던 문제다. #319

<br>

## 3. 기억 저장 구조

- 캐릭터가 본 영화·드라마마다 새벽 정리가 사실 카드를 만들어 두고, 그 작품이 오늘 각본에 있는 날만 답장 프롬프트가 읽는다. 카드에 없는 장면·인물은 말하지 않고, 카드가 없는 작품은 아는 척하지 않는다.
  V2는 제목만 갖고 있어서 없는 장면을 지어냈다. #287
- 기억과 일정에 상대가 그 사실을 아는지 적는 칸을 실제로 갱신한다. 캐릭터가 상대에게 직접 말한 일정은 안다는 쪽으로 바뀐다.
  V2는 이 칸이 만들 때 값 그대로 굳어 있었다. #345
- 답장을 적을 때 그 턴의 메모를 기록 행 번호와 함께 남기고, 대화 기록의 캐릭터 발화도 답장과 같은 모양으로 적는다.
  V2는 대화가 쌓이면 메모가 거의 안 나왔다. #346
- 결혼·장례·명절처럼 한국에서 지나가는 순서가 정해진 일 10가지의 절차를 캐릭터와 무관한 공통 표에 두고, 이 달 재료에 그 이름이 걸렸을 때만 해당 이벤트의 단계를 월 리듬 프롬프트에 싣는다. 청첩장 모임과 모바일 청첩장을 가르는 기준, 축의금 시세, 명절 승차권 대기열처럼 절차에 딸린 관행도 같이 적었다. 역할은 코드가 고르지 않고 모델이 문장을 보고 고른다.
  V2는 결혼한다는 한 줄만 있고 그 앞뒤로 무엇을 하는지가 없어서, 상견례도 청첩장도 없이 결혼식 당일만 나왔다. #405
- 공휴일 달력에 음력 명절과 대체공휴일을 넣고 날짜마다 이름을 적었다. 하루에 붙는 이름표도 공휴일(휴무) 하나가 아니라 추석(휴무)·한글날(휴무)처럼 어느 날인지 밝히고, 그 달의 공휴일을 월 리듬 재료에 함께 넣어서 아크에도 진행 중인 일에도 명절이 안 적힌 캐릭터도 추석·설날이 든 달에 명절 절차를 받는다.
  V2는 양력 고정일 8일만 있어서 추석 당일이 평일로 읽혔고, 달력에만 있는 일이라 명절 절차도 걸릴 자리가 없었다. #415
- 한 답장이 오늘 메모를 여러 건 남긴다. 상대 쪽 사실과 캐릭터가 처음 꺼낸 자기 이야기가 한 턴에 같이 나오면 둘 다 한 행씩 저장되고, 이미 적힌 것과 같은 주제라도 동네·학교 이름처럼 더 좁은 사실이면 따로 남는다.
  V2는 메모 칸이 한 줄이라 한쪽이 밀려났고, 주제가 같으면 새로 나온 사실도 이미 적힌 것으로 읽혔다. #399
- 기억 한 줄에 그 일이 실제로 있었던 날을 적고, 줄 끝 괄호에 (9/10에 있었던 일 · 9/12 갱신)처럼 있었던 날과 고친 날을 갈라 보여준다. 날짜는 새벽 정리가 대화에서 뽑고, 같은 일을 다시 적을 때 이 칸을 안 넘기면 처음 적힌 날이 그대로 남는다. 있었던 날이 없는 줄에서는 시점을 단정하지 말라는 규칙도 그 줄을 싣는 절마다 붙였다.
  V2는 마지막으로 고친 날만 있어서, 며칠 전 일을 오늘 다시 말하면 날짜가 오늘로 바뀌어 방금 있었던 일로 읽혔다. #388

<br>

## 4. 프롬프트 구조 설계와 관리

- 답장 객체의 메모·플러팅·처음 3칸은 값이 없어도 null로 싣는다.
  V2는 해당 없는 칸을 빼라고 시켜서 플러팅·처음 태그가 한 건도 안 남았다. #385
- 웃음 표기의 개수(ㅋ는 3개 이상, ㅎ는 2개 이상)를 캐릭터 정체성 값에서 출력 규칙층으로 올렸다.
  V2는 캐릭터마다 달라서 생성 결과에 따라 흔들렸다. #324
- 캐릭터의 월 리듬·하루 각본·삶의 흐름을 만드는 프롬프트가 유저와 만나는 일정을 만들지 못하게 막았다. 유저와 이어지는 것은 메시지뿐이다.
  V2는 유저와 서점에 가는 이벤트를 지어냈다. #322
- 답장 말투 규칙층에 4가지를 더했다 — 상대를 보내는 말로 대화를 닫지 않기, 속상한 말을 되돌려주는 공감과 지지 선언 금지, 이따 또 연락하겠다는 말로 지금 대화를 미루지 않기, 말머리 오로 시작하는 리액션 금지.
  V2의 규칙층에는 없었다. #364 #368 #373
- 작품 이야기를 받는 방식을 세 갈래로 나눴다 — 오늘 보는 중이면 어디까지 봤는지만, 안 본 작품이면 제목·장르·평판까지 말하고 재밌게 봤는지 되묻고, 처음 듣는 제목이면 어떤 건지 묻는다.
  V2는 사실 카드가 없는 작품을 전부 한 덩어리로 묶어서 제목만 들어본 작품도 본 것처럼 감상을 말했다. #404
- 보는 중인데 사실 카드가 아직 없는 작품도 장르와 무슨 얘기인지 한 줄까지는 말한다. 장면·대사·결말·등장인물만 막는다.
  V2는 카드가 없으면 그 작품 내용을 통째로 막아서, 캐릭터가 지금 보고 있는 것보다 안 본 유명 작품을 더 잘 아는 것처럼 답했다. #407
- 답장 말투 규칙층에 5가지를 더했다 — 말풍선 하나는 50자까지 쓰고 넘으면 나누기, 상대가 어떤 상태인지 진단해 읽어 주는 위로 금지, 한 답장에서 같은 내용 두 번 말하기 금지, 안쓰럽다처럼 내려다보는 말과 손에서 못 놓는다 같은 번역투 금지, 출신·거주 같은 서류 낱말 금지. 말풍선 개수 상한 5개도 길이 규칙과 같은 자리에 적었다. 6개를 넘으면 코드가 뒷부분을 마지막 하나로 합쳐서, 길다고 나눈 것이 도로 혼자 긴 말풍선으로 돌아오기 때문이다.
  V2의 규칙층에는 없었고, 9/11 대화에서 45자 뒤에 70자가 한 번에 오는 답장과 상대를 정리해 주는 위로가 같은 날 나왔다. #389
- 잘된 일이나 뜻밖의 소식을 듣고 놀라는 자리에서도 말풍선을 오로 열지 않는다. 그 자리를 규칙에서 이름으로 부르고 거기서 쓸 반응어(헐·대박·진짜?)와 방금 들은 것을 그대로 되묻는 법을 함께 줬고, 첫 글자만 보면 가려지는 금지라 보내기 전 점검에도 같은 줄을 뒀다.
  V2에는 말머리 오를 막는 줄이 아예 없었고, 표기 층에만 두고 배포한 뒤에는 잘된 일을 들은 자리에서만 3회 가운데 3회가 다시 오로 열었다. #387
- 캐릭터가 말하는 앞일은 저장된 일정 안에서만 나온다. 며칠 뒤·다음 주에 뭐 하냐고 물으면 [다가오는 일정]·[지금 얘기와 관련 있는 일정]·[너의 오늘 하루]에 적힌 것만 말하고 없는 날짜·시각·장소는 붙이지 않으며, 이 대화에서 새로 정한 약속은 메모로 남는다. 프롬프트가 읽어 오는 앞일의 범위도 건수가 아니라 오늘부터 14일로 정하고, 답장 경로와 게시함 도구가 같은 함수를 불러 화면에서 뺀 행과 프롬프트에 실린 행이 갈리지 않게 했다.
  V2는 앞일을 물었을 때 쓸 지시가 없어서 저장에 없는 약속을 지어냈고, 읽어 오는 범위가 건수뿐이라 캐릭터가 아는 앞일의 끝이 달마다 달랐다. #398

<br>

## 5. 새벽 정리

- 새벽 정리가 어제 처음 한 것을 확정하고 오늘 의도(무엇을 물어볼지·무엇을 흘릴지)를 적는다. 아침에 거는 말이 그 의도를 따른다.
  V2의 새벽 정리는 기억·관계·각본만 다뤘다. #355

<br>

## 6. 유저의 한마디부터 답장까지

- 답할 수 없는 시간에 온 말이 지금 대화를 요청하는 것인지 판정할 때, 답 없이 이어 보낸 통 수와 기다린 시간을 함께 넣는다. 자?·있어?처럼 짧게 이어 보낸 말도 요청으로 걸린다.
  V2는 마지막 한 통만 보고 판정해서 놓쳤다. #336

<br>

## 7. 답장 생성 파이프라인 계측과 관측

- 몰아서 답하는 경로가 지나는 자리 10곳과 답장이 예외로 끝난 자리 4곳을 게시함에 쌓는다.
  V2는 이 갈래들이 슬랙에 아무것도 안 남겨서 답장이 안 나간 날 원인을 못 찾았다. #379
- 슬랙에 남긴 지적에 처리 여부를 적는다. 도구가 안 끝난 것만 날짜별로 보여주고, 처리 표시를 찍으면 그 슬랙 글에 체크가 달린다.
  V2는 지적을 모으기만 하고 무엇을 고쳤는지 적을 자리가 없었다. #400
- 게시함에 적는 기억 이름을 프롬프트 줄과 같은 글자(상대 · 일 · ○○)로 맞춰서, 본문에서 복사한 이름으로 스레드를 검색하면 그 기억이 실린 답장이 걸린다. 이번 호출이 읽은 다가오는 일정도 참고한 일정 줄에 주인·날짜와 함께 적고, 앞일이 없는 날에는 0건이라고 적는다.
  V2는 게시함이 저장한 꼴 그대로 적어서 프롬프트 줄과 글자가 달랐고, 다가오는 일정은 하루 한 번 굳는 덩이에 들어가 스레드 끝 덩이에도 본문에도 안 남았다. #397

<br>

## 8. 답장 품질 평가 (eval)

- 평가를 레인 2개로 갈랐다. PR에 붙는 형식 레인은 케이스 5개를 3회 돌려 답이 객체로 오는지와 늘 넣는 칸이 오는지만 보고, 케이스 26개 전체는 말투 규칙을 크게 손본 뒤 손으로 돌린다.
  V2는 PR마다 26개를 5회 돌려서 한 번에 호출 130건이 나갔다. #391

<br>

## 9. 시스템 구조와 기술 선택

- 텔레그램으로 보내는 연결에 주소 자동 선택 시간 상한을 3초로 지정했다. Node 기본값 250ms 안에 접속이 안 끝나서, 풀에 살아 있던 소켓이 닫히는 순간부터 발송이 전부 실패했다.
  V2부터 있던 문제다. #363

<br>

## 10. 작업과 문서 관리

> 지금 리드미에 없는 절이다. 위 줄들과 달리 변경 기록이 아니라 리드미에 그대로 옮길 수 있게 쓴 본문이다.

혼자 만들고 코드는 대부분 AI와 함께 짜는데, 작업 한 건마다 새 대화로 시작하니까 이 프로젝트에서 무엇을 어디에 적기로 했는지가 매번 다시 설명해야 하는 것이 된다. 그래서 문서마다 맡을 내용을 하나씩 정하고, 지켜야 할 규칙은 사람이 기억하는 대신 커밋 훅과 CI가 막게 했다. 아래에서 말하는 세션은 그 대화 한 건이다.

### 작업 한 건의 흐름

이슈를 먼저 만들고 main에서 브랜치를 파며, 커밋을 마치면 PR을 올리기 전에 브랜치 diff를 리뷰한다. 나온 지적은 같은 브랜치에서 고치고, 안 고친 것은 그 까닭을 PR 본문에 적는다. PR은 `Closes #이슈번호`로 올려서 머지와 브랜치 삭제까지 한 세션 안에서 끝낸다.

끝난 작업은 지시서에 남기지 않고 닫힌 이슈로 찾기 때문에, 이슈 없이 짠 코드는 왜 그렇게 짰는지가 어디에도 안 남는다.

### 문서마다 맡은 내용

| 문서 | 여기에만 적는 것 |
|---|---|
| README | 컨셉·가설·설계 총론 |
| areas.md | 어느 파일이 어느 영역에 속하는지, 파일마다 한 줄 요약 |
| modules.md · erd.md | 실행 단위로 묶어 본 그림과 답장 경로 · 표와 컬럼의 뜻 |
| ADR | 되돌리기 어려운 판단과 그때 놓고 고민한 대안 |
| CLAUDE.md | 세션이 지켜야 할 규칙과 아직 안 한 작업 |

같은 사실을 두 문서에 적지 않는다. 한쪽만 고쳐졌을 때 어느 쪽이 맞는지 알 수 없어서다.

### 사람 대신 커밋 훅과 CI가 막는 것

- 파일 색인이 코드와 다르면 커밋이 막힌다. 영역 표가 단일 소스고 `scripts/gen-modules.mjs`가 그 표에서 색인과 아키텍처 요약을 다시 쓰므로, 요약 주석이 없거나 표에 없는 파일도 같이 걸린다.
- CLAUDE.md는 세션마다 통째로 읽히는 지시서라 14,000자 상한을 커밋 훅이 강제하고 12,000자를 넘으면 경고한다.
- main 푸시와 PR마다 GitHub Actions가 타입 검사와 단위 테스트를 돌린다(9/12 기준 903건).
- 모델을 불러야 하는 말투 평가는 호출 비용이 붙고 통과율이 그날 답에 따라 흔들려서, PR에는 형식만 보는 가벼운 레인을 붙이고 케이스 전체는 말투 규칙을 크게 손본 뒤 손으로 돌린다.

### 배포 뒤에 볼 것

프롬프트를 고친 변경은 테스트로 맞고 틀림이 안 갈리고 며칠 실제 대화를 봐야 알 수 있어서, 배포할 때마다 그 변경에서 확인할 것을 항목으로 적어 두고 실제 대화·슬랙 기록과 대조해 하나씩 결과를 적는다. 배포 건마다 다른 자리에 적어 두면 무엇이 아직 안 끝났는지 알 수 없어서 한 문서에 묶음으로 쌓고, 작업을 마친 뒤에 볼 것이 더 생기면 그때마다 그 묶음에 붙인다. 확인 결과에는 실제 대화 내용이 들어가므로 이 문서는 커밋하지 않는다.

### 설계 문서를 먼저 쓰는 순서

큰 변경은 설계 원본 문서를 먼저 쓰고 세션 단위로 쪼갠 다음, 구현 세션이 그 문서만 읽고 코드로 옮긴다. 설계와 구현을 한자리에서 같이 하면 중간에 방향이 바뀐 것을 알아채지 못한 채 코드만 늘어나서다. 되돌리기 어려운 판단은 그 자리에서 ADR로 따로 남긴다.

<br>

## 안 넣는 것

리드미를 쓰는 세션이 다시 열어 보지 않아도 되는 이슈다.

- #262 새 기억 구조로 읽은 대화 확인 — 확인 작업
- #327 V3 운영 설계와 설계 원본 — 설계 문서
- #343 플러팅을 모든 결의 기본으로 두기 — 설계 문서
- #347 슬랙 프롬프트 비교의 표시 겹침 — 게시 표기
- #350 유저가 어떤 사람인지 말해 주는 플러팅 더하기 — 설계 문서, 코드는 #353
- #369 #372 설렘의 수를 플러팅으로 부르기 — 이름 통일, 리드미에서도 플러팅으로 쓴다
- #377 답장 게시의 관계·열림 줄 가르기 — 게시 표기
- #383 백업 도구가 남긴 곁파일 치우기 — 도구 정리
- #411 월 리듬 수집에 절차 싣기 — 봇 밖 생성 경로에 같은 재료를 넘기는 배선, 동작은 위 #405 줄에 있다
- #423 공휴일 표에 2027년 채우기 — 해마다 손으로 채우는 표에 한 해를 더한 것, 동작은 위 공휴일 달력 줄에 있다
